window.QUIZ_BANK = window.QUIZ_BANK || {};
window.QUIZ_BANK.container = [
  /* ---------------- EASY ---------------- */
  { diff: 'easy', type: 'short', q: '실행 중인 Docker 컨테이너 목록을 보는 명령어는?', answer: 'docker ps', accept: ['docker container ls', 'docker container list'],
    explain: '`docker ps` 는 실행 중인 컨테이너만, `docker ps -a` 는 종료된 것까지 보여줍니다.',
    example: '배포 후 `docker ps` 로 컨테이너 STATUS 가 `Up` 인지, PORTS 매핑이 맞는지 확인합니다.' },
  { diff: 'easy', type: 'short', q: '종료된 컨테이너를 포함해 모든 컨테이너를 나열하는 명령어는?', answer: 'docker ps -a', accept: ['docker ps --all', 'docker container ls -a', 'docker container ls --all'],
    explain: '`-a` (all). Exited 상태의 컨테이너는 디스크를 계속 차지하므로 `docker container prune` 으로 정리합니다.',
    example: '`docker run` 직후 컨테이너가 사라졌다면 `docker ps -a` 로 Exit Code 를 확인하고 `docker logs <id>` 를 봅니다.' },
  { diff: 'easy', type: 'short', q: '`nginx` 이미지를 백그라운드로 실행하며 호스트 8080 → 컨테이너 80 포트로 연결하는 명령어는?', answer: 'docker run -d -p 8080:80 nginx', accept: ['docker run -p 8080:80 -d nginx', 'docker run -dp 8080:80 nginx', 'docker run -d -p 8080:80 nginx:latest', 'docker container run -d -p 8080:80 nginx'],
    explain: '`-d` detached(백그라운드), `-p 호스트:컨테이너` 포트 매핑. `--name` 으로 이름을 붙이면 관리가 편합니다.',
    example: '로컬 개발에서 DB 띄우기: `docker run -d --name pg -e POSTGRES_PASSWORD=pw -p 5432:5432 postgres:16`.' },
  { diff: 'easy', type: 'mcq', q: '컨테이너 `web` 의 로그를 실시간으로 보는 명령어는?', options: ['docker logs -f web', 'docker tail web', 'docker attach --logs web', 'docker inspect web'], answer: 0,
    explain: '`docker logs` 는 컨테이너의 stdout/stderr 를 보여줍니다. `-f` follow, `--tail 100` 마지막 100줄, `--since 10m` 시간 필터.',
    example: '컨테이너가 재시작을 반복하면 `docker logs --tail 50 web` 로 크래시 원인을 확인합니다.' },
  { diff: 'easy', type: 'short', q: '실행 중인 컨테이너 `web` 안에서 bash 셸을 대화형으로 실행하는 명령어는?', answer: 'docker exec -it web bash', accept: ['docker exec -it web /bin/bash', 'docker exec -ti web bash', 'docker exec -it web sh', 'docker exec -it web /bin/sh'],
    explain: '`exec` 는 실행 중인 컨테이너에 새 프로세스를 실행. `-i` 표준입력 유지, `-t` TTY 할당. alpine 기반은 bash 가 없어 `sh` 를 씁니다.',
    example: '설정 파일이 제대로 마운트됐는지 `docker exec -it web cat /etc/nginx/nginx.conf` 로 확인합니다.' },
  { diff: 'easy', type: 'ox', q: 'Docker 이미지는 읽기 전용 레이어들의 집합이고, 컨테이너는 그 위에 쓰기 가능한 레이어를 얹은 것이다.', answer: true,
    explain: '이미지 레이어는 불변이며 여러 컨테이너가 공유합니다. 컨테이너의 변경은 최상위 쓰기 레이어에만 기록되고, 컨테이너 삭제 시 사라집니다.',
    example: '컨테이너 안에서 파일을 수정해도 재생성하면 사라지므로 영속 데이터는 볼륨(`-v`)을 써야 합니다.' },
  { diff: 'easy', type: 'short', q: '현재 디렉터리의 Dockerfile 로 `myapp:1.0` 태그 이미지를 빌드하는 명령어는?', answer: 'docker build -t myapp:1.0 .', accept: ['docker build --tag myapp:1.0 .', 'docker build . -t myapp:1.0', 'docker image build -t myapp:1.0 .'],
    explain: '마지막 `.` 은 빌드 컨텍스트(Dockerfile 과 COPY 대상 파일이 있는 경로). `-t name:tag` 로 태그 지정. `-f` 로 다른 Dockerfile 지정 가능.',
    example: 'CI 에서 `docker build -t registry.example.com/myapp:${GIT_SHA} .` 후 `docker push`.' },
  { diff: 'easy', type: 'mcq', q: '컨테이너를 삭제하는 명령어는? (컨테이너명: web, 이미 중지됨)', options: ['docker rmi web', 'docker rm web', 'docker delete web', 'docker kill web'], answer: 1,
    explain: '`docker rm` 은 컨테이너 삭제, `docker rmi` 는 이미지 삭제입니다. 실행 중인 컨테이너는 `docker rm -f` 로 강제 삭제할 수 있습니다.',
    example: '`docker run --rm ...` 으로 실행하면 종료 시 자동 삭제되어 일회성 작업에 편합니다.' },
  { diff: 'easy', type: 'short', q: 'Kubernetes 클러스터의 모든 네임스페이스에 있는 Pod 를 나열하는 kubectl 명령어는?', answer: 'kubectl get pods -A', accept: ['kubectl get pods --all-namespaces', 'kubectl get po -A', 'kubectl get pod -A', 'kubectl get po --all-namespaces'],
    explain: '`-A` = `--all-namespaces`. 기본은 현재 컨텍스트의 네임스페이스만. `-n NAMESPACE` 로 특정 네임스페이스 지정. `-o wide` 로 노드·IP 표시.',
    example: '클러스터 전체 상태 점검: `kubectl get pods -A | grep -v Running` 으로 비정상 Pod 를 찾습니다.' },
  { diff: 'easy', type: 'ox', q: 'Kubernetes 에서 Pod 는 하나 이상의 컨테이너를 포함하며, 같은 Pod 의 컨테이너는 네트워크 네임스페이스(IP, 포트)를 공유한다.', answer: true,
    explain: 'Pod 내 컨테이너는 `localhost` 로 서로 통신하고 같은 IP 를 가집니다. 볼륨도 공유 가능. 사이드카 패턴(로그 수집기, 프록시)이 이를 활용합니다.',
    example: 'Istio 는 각 Pod 에 Envoy 프록시 사이드카를 넣어 앱 코드 수정 없이 트래픽을 가로챕니다.' },
  { diff: 'easy', type: 'short', q: 'Pod `api-7d9f` 의 로그를 실시간으로 보는 kubectl 명령어는?', answer: 'kubectl logs -f api-7d9f', accept: ['kubectl logs api-7d9f -f', 'kubectl logs --follow api-7d9f'],
    explain: '`kubectl logs` 는 컨테이너 stdout/stderr. 다중 컨테이너 Pod 는 `-c 컨테이너명`. `--previous` 로 크래시 전 컨테이너 로그, `-l app=api` 로 레이블 선택.',
    example: 'CrashLoopBackOff 원인 파악: `kubectl logs api-7d9f --previous`.' },
  { diff: 'easy', type: 'mcq', q: 'YAML 매니페스트 파일 `deploy.yaml` 을 클러스터에 적용하는 명령어는?', options: ['kubectl run -f deploy.yaml', 'kubectl apply -f deploy.yaml', 'kubectl start deploy.yaml', 'kubectl load deploy.yaml'], answer: 1,
    explain: '`apply` 는 선언적으로 리소스를 생성/갱신합니다(없으면 생성, 있으면 diff 적용). `create` 는 이미 있으면 에러. `-f` 에 디렉터리나 URL 도 가능.',
    example: 'GitOps 파이프라인: `kubectl apply -f k8s/` 로 디렉터리 전체 적용. `kubectl diff -f` 로 사전 검토.' },
  { diff: 'easy', type: 'short', q: 'Deployment `api` 의 replica 수를 5로 변경하는 kubectl 명령어는?', answer: 'kubectl scale deployment api --replicas=5', accept: ['kubectl scale deploy api --replicas=5', 'kubectl scale deployment/api --replicas=5', 'kubectl scale --replicas=5 deployment api', 'kubectl scale --replicas=5 deployment/api', 'kubectl scale deploy/api --replicas=5'],
    explain: '`scale` 로 즉시 replica 조정. 선언적 관리에서는 매니페스트의 `spec.replicas` 를 수정해 apply 하는 것이 정석입니다. HPA 가 있으면 HPA 가 덮어씁니다.',
    example: '이벤트 트래픽 대비 수동 확장: `kubectl scale deploy api --replicas=20`.' },
  { diff: 'easy', type: 'ox', q: 'Dockerfile 의 `CMD` 와 `ENTRYPOINT` 는 동일한 기능이며 서로 교체해서 써도 차이가 없다.', answer: false,
    explain: '`ENTRYPOINT` 는 고정 실행 파일, `CMD` 는 기본 인수(또는 ENTRYPOINT 없을 때 기본 명령). `docker run 이미지 인수` 로 CMD 는 덮어쓰이지만 ENTRYPOINT 는 `--entrypoint` 로만 바꿉니다.',
    example: '`ENTRYPOINT ["python", "app.py"]` + `CMD ["--port", "8000"]` → 실행 시 `docker run img --port 9000` 으로 인수만 교체.' },
  { diff: 'easy', type: 'short', q: 'Docker Compose 로 정의된 서비스들을 백그라운드에서 시작하는 명령어는?', answer: 'docker compose up -d', accept: ['docker-compose up -d', 'docker compose up --detach'],
    explain: '`up` 은 네트워크·볼륨·컨테이너 생성 후 시작. `-d` 백그라운드. `--build` 로 이미지 재빌드. 종료는 `docker compose down` (볼륨까지 지우려면 `-v`).',
    example: '로컬 개발 환경: `docker compose up -d db redis` 로 의존 서비스만 띄우고 앱은 IDE 에서 실행.' },
  { diff: 'easy', type: 'mcq', q: '사용하지 않는 이미지, 중지된 컨테이너, 미사용 네트워크를 한 번에 정리하는 명령어는?', options: ['docker clean', 'docker system prune', 'docker rm -a', 'docker gc'], answer: 1,
    explain: '`docker system prune` 은 dangling 이미지, 중지 컨테이너, 미사용 네트워크, 빌드 캐시를 삭제. `-a` 는 태그 있는 미사용 이미지까지, `--volumes` 는 볼륨까지.',
    example: 'CI 러너 디스크 부족 시 `docker system prune -af --volumes` 를 주기적으로 실행합니다. 볼륨 삭제는 데이터 손실 주의.' },
  { diff: 'easy', type: 'short', q: '로컬에 있는 Docker 이미지 목록을 보는 명령어는?', answer: 'docker images', accept: ['docker image ls', 'docker image list'],
    explain: 'REPOSITORY, TAG, IMAGE ID, SIZE 를 보여줍니다. `<none>` 태그는 dangling 이미지로 `docker image prune` 대상입니다.',
    example: '`docker images | grep myapp` 로 빌드된 태그 확인, `docker images --format "{{.Repository}}:{{.Tag}} {{.Size}}"` 로 크기 정렬.' },
  { diff: 'easy', type: 'ox', q: 'Kubernetes Service 의 `ClusterIP` 타입은 클러스터 외부에서 직접 접근할 수 없다.', answer: true,
    explain: 'ClusterIP 는 클러스터 내부 가상 IP. 외부 노출은 `NodePort`, `LoadBalancer`, 또는 `Ingress` 를 사용합니다. 디버깅 시 `kubectl port-forward` 로 임시 접근 가능.',
    example: '내부 마이크로서비스 간 통신은 ClusterIP + DNS(`svc-name.namespace.svc.cluster.local`)로, 외부 API 는 Ingress 로 노출.' },

  /* ---------------- NORMAL ---------------- */
  { diff: 'normal', type: 'short', q: '호스트의 `./data` 디렉터리를 컨테이너의 `/var/lib/postgresql/data` 에 마운트해 `postgres` 이미지를 실행하는 명령어는? (백그라운드 실행)', answer: 'docker run -d -v ./data:/var/lib/postgresql/data postgres', accept: ['docker run -d -v $(pwd)/data:/var/lib/postgresql/data postgres', 'docker run -v ./data:/var/lib/postgresql/data -d postgres', 'docker run -d -v $PWD/data:/var/lib/postgresql/data postgres', 'docker run -d --mount type=bind,source=./data,target=/var/lib/postgresql/data postgres', 'docker run -d -v "$(pwd)/data:/var/lib/postgresql/data" postgres'],
    explain: '`-v 호스트경로:컨테이너경로` 는 bind mount. 이름만 쓰면(`-v pgdata:/var/...`) named volume. 구버전 Docker 는 상대경로를 못 받아 `$(pwd)` 가 필요합니다.',
    example: '운영에서는 named volume 을 권장: `docker volume create pgdata && docker run -v pgdata:/var/lib/postgresql/data postgres`.' },
  { diff: 'normal', type: 'mcq', q: 'Dockerfile 에서 `COPY` 대신 `ADD` 를 써야 하는 경우로 적절한 것은?', options: ['일반 파일 복사', '로컬 tar 파일을 자동으로 풀어서 넣고 싶을 때', '디렉터리 복사', '빌드 속도를 높이고 싶을 때'], answer: 1,
    explain: '`ADD` 는 로컬 tar 자동 추출과 URL 다운로드 기능이 있지만, 그 외에는 `COPY` 가 명시적이라 권장됩니다. URL 은 `RUN curl` 이 캐시 제어에 유리합니다.',
    example: 'Best practice: `COPY package*.json ./` → `RUN npm ci` → `COPY . .` 순으로 레이어 캐시를 최대한 활용.' },
  { diff: 'normal', type: 'short', q: 'Pod `api-7d9f` 의 이벤트, 상태, 컨테이너 정보 등 상세 내용을 보는 kubectl 명령어는?', answer: 'kubectl describe pod api-7d9f', accept: ['kubectl describe po api-7d9f', 'kubectl describe pods api-7d9f', 'kubectl describe pod/api-7d9f'],
    explain: '`describe` 는 리소스 상세와 최근 Events 를 보여줍니다. ImagePullBackOff, FailedScheduling, OOMKilled 같은 원인을 여기서 찾습니다.',
    example: 'Pod 가 Pending 이면 `describe` 의 Events 에 "Insufficient cpu" 나 "node(s) had taint" 같은 스케줄링 실패 사유가 나옵니다.' },
  { diff: 'normal', type: 'mcq', q: 'Pod 상태 `CrashLoopBackOff` 의 의미는?', options: ['이미지를 받을 수 없음', '컨테이너가 시작 후 반복적으로 종료되어 재시작 간격이 늘어나는 중', '노드에 리소스가 부족해 스케줄 불가', 'Pod 가 삭제 중'], answer: 1,
    explain: '컨테이너가 계속 죽으면 kubelet 이 10s, 20s, 40s ... 최대 5분까지 지수 백오프로 재시작합니다. 원인은 앱 크래시, 잘못된 커맨드, 설정/시크릿 누락 등.',
    example: '`kubectl logs POD --previous` 로 죽기 직전 로그를 보고, `kubectl describe pod` 로 Exit Code(137=OOM/SIGKILL, 1=앱 에러)를 확인합니다.' },
  { diff: 'normal', type: 'short', q: 'Deployment `api` 의 컨테이너 `app` 이미지를 `myapp:2.0` 으로 업데이트하는 kubectl 명령어는?', answer: 'kubectl set image deployment/api app=myapp:2.0', accept: ['kubectl set image deploy/api app=myapp:2.0', 'kubectl set image deployment api app=myapp:2.0', 'kubectl set image deploy api app=myapp:2.0'],
    explain: '`set image 리소스/이름 컨테이너=이미지`. 롤링 업데이트가 시작되고 `kubectl rollout status deployment/api` 로 진행을 확인합니다.',
    example: 'CI/CD: `kubectl set image deployment/api app=registry/api:${SHA} && kubectl rollout status deployment/api --timeout=120s`.' },
  { diff: 'normal', type: 'short', q: 'Deployment `api` 를 직전 리비전으로 롤백하는 kubectl 명령어는?', answer: 'kubectl rollout undo deployment/api', accept: ['kubectl rollout undo deploy/api', 'kubectl rollout undo deployment api', 'kubectl rollout undo deploy api'],
    explain: '`rollout undo` 는 이전 ReplicaSet 으로 되돌립니다. `--to-revision=N` 으로 특정 리비전, `rollout history` 로 목록 확인.',
    example: '배포 후 에러율 급증 → `kubectl rollout undo deployment/api` 로 수 초 내 복구. `revisionHistoryLimit` 이 롤백 가능 범위를 정합니다.' },
  { diff: 'normal', type: 'ox', q: 'Kubernetes ConfigMap 은 민감 정보를 저장하기 위해 기본적으로 암호화되어 있다.', answer: false,
    explain: 'ConfigMap 은 평문입니다. 민감 정보는 Secret 을 쓰지만 Secret 도 기본은 base64 인코딩일 뿐 암호화가 아닙니다. etcd 암호화(EncryptionConfiguration)나 외부 시크릿 매니저가 필요합니다.',
    example: 'DB 비밀번호는 Secret + RBAC 제한 + etcd 암호화, 또는 External Secrets Operator 로 AWS Secrets Manager 에서 주입합니다.' },
  { diff: 'normal', type: 'mcq', q: '`docker run --restart=unless-stopped` 의 동작은?', options: ['컨테이너가 종료되면 절대 재시작하지 않음', '실패(비정상 종료) 시에만 재시작', '사용자가 명시적으로 stop 하지 않는 한 항상 재시작 (도커 데몬 재시작 포함)', '5회까지만 재시작'], answer: 2,
    explain: '`no`(기본), `on-failure[:N]`, `always`, `unless-stopped`. `always` 는 수동 stop 후에도 데몬 재시작 시 다시 뜨지만, `unless-stopped` 는 수동 stop 상태를 기억합니다.',
    example: '단일 서버 운영 컨테이너에 `--restart=unless-stopped` 를 붙여 서버 재부팅 시 자동 복구.' },
  { diff: 'normal', type: 'short', q: '로컬 포트 5432 를 Pod `pg-0` 의 5432 포트로 포워딩하는 kubectl 명령어는?', answer: 'kubectl port-forward pg-0 5432:5432', accept: ['kubectl port-forward pod/pg-0 5432:5432', 'kubectl port-forward pg-0 5432', 'kubectl port-forward pods/pg-0 5432:5432'],
    explain: '`port-forward 리소스 로컬:원격`. Service 나 Deployment 도 가능(`svc/pg`). 디버깅·로컬 접속용이며 운영 트래픽 용도는 아닙니다.',
    example: '클러스터 내부 DB 에 로컬 GUI 툴로 접속: `kubectl port-forward svc/postgres 15432:5432` 후 localhost:15432 로 연결.' },
  { diff: 'normal', type: 'mcq', q: 'Docker 이미지 크기를 줄이는 방법으로 가장 효과적인 것은?', options: ['`RUN` 을 여러 줄로 나누기', 'Multi-stage build 로 빌드 도구를 최종 이미지에서 제외', '`latest` 태그 사용', '`EXPOSE` 지시어 제거'], answer: 1,
    explain: 'Multi-stage build 는 빌드 스테이지에서 컴파일하고 최종 스테이지에는 산출물만 COPY 합니다. 그 외: alpine/distroless 베이스, `RUN` 을 `&&` 로 합쳐 레이어 줄이기, `.dockerignore`.',
    example: 'Go 앱: `FROM golang:1.22 AS build` → `RUN go build` → `FROM gcr.io/distroless/static` → `COPY --from=build /app/bin /` → 1GB → 10MB.' },
  { diff: 'normal', type: 'short', q: '레이블 `app=api` 를 가진 Pod 만 나열하는 kubectl 명령어는?', answer: 'kubectl get pods -l app=api', accept: ['kubectl get po -l app=api', 'kubectl get pods --selector app=api', 'kubectl get pods --selector=app=api', 'kubectl get pods -l=app=api', 'kubectl get pod -l app=api'],
    explain: '`-l` / `--selector` 로 레이블 필터. 여러 개는 `-l app=api,tier=backend`. Service 와 Deployment 가 Pod 를 찾는 것도 같은 레이블 셀렉터 방식입니다.',
    example: '특정 앱 Pod 만 재시작: `kubectl delete pods -l app=api` (Deployment 가 새로 만듦).' },
  { diff: 'normal', type: 'ox', q: 'Kubernetes 에서 `requests` 는 스케줄링 시 보장되는 최소 리소스이고, `limits` 는 컨테이너가 사용할 수 있는 최대치다.', answer: true,
    explain: '스케줄러는 requests 합이 노드 용량을 넘지 않게 배치합니다. CPU limit 초과는 스로틀링, 메모리 limit 초과는 OOMKill 입니다.',
    example: '`resources: {requests: {cpu: 250m, memory: 256Mi}, limits: {memory: 512Mi}}`. CPU limit 은 불필요한 스로틀링을 유발할 수 있어 생략하는 팀도 많습니다.' },
  { diff: 'normal', type: 'short', q: 'Kubernetes 에서 현재 사용 중인 컨텍스트(클러스터/사용자/네임스페이스 조합)를 확인하는 명령어는?', answer: 'kubectl config current-context', accept: ['kubectl config get-contexts', 'kubectl config view --minify'],
    explain: '`~/.kube/config` 에 여러 클러스터가 있을 때 `current-context` 로 현재 대상 확인. `use-context NAME` 으로 전환, `set-context --current --namespace=NS` 로 기본 네임스페이스 변경.',
    example: '운영 클러스터에 실수로 명령을 날리는 사고 방지: 프롬프트에 컨텍스트를 표시(kube-ps1)하거나 `kubectl config current-context` 를 습관화.' },
  { diff: 'normal', type: 'mcq', q: 'Dockerfile 에서 `RUN apt-get update && apt-get install -y curl` 처럼 한 `RUN` 에 합치는 이유는?', options: ['문법상 필수', '캐시된 오래된 패키지 목록으로 install 되는 문제 방지 + 레이어 수 감소', '빌드 속도가 항상 빨라짐', 'root 권한이 필요해서'], answer: 1,
    explain: '`RUN apt-get update` 를 따로 두면 그 레이어가 캐시되어 나중에 추가한 패키지가 오래된 인덱스로 설치 실패할 수 있습니다. 끝에 `&& rm -rf /var/lib/apt/lists/*` 로 이미지도 줄입니다.',
    example: '`RUN apt-get update && apt-get install -y --no-install-recommends curl ca-certificates && rm -rf /var/lib/apt/lists/*`.' },
  { diff: 'normal', type: 'short', q: '실행 중인 컨테이너 `web` 의 상세 정보(IP, 마운트, 환경변수 등)를 JSON 으로 보는 명령어는?', answer: 'docker inspect web', accept: ['docker container inspect web'],
    explain: '`inspect` 는 모든 메타데이터를 JSON 으로 출력. `--format` 으로 필요한 값만 추출합니다.',
    example: '컨테이너 IP 만: `docker inspect -f "{{range.NetworkSettings.Networks}}{{.IPAddress}}{{end}}" web`.' },
  { diff: 'normal', type: 'mcq', q: 'Kubernetes `Ingress` 리소스의 역할은?', options: ['Pod 간 네트워크 정책 정의', 'HTTP/HTTPS 트래픽을 호스트·경로 기반으로 Service 에 라우팅', '외부 IP 를 노드에 할당', '클러스터 내부 DNS 제공'], answer: 1,
    explain: 'Ingress 는 규칙만 정의하며, 실제 동작은 Ingress Controller(nginx, traefik, ALB 등)가 합니다. TLS 종료도 여기서 처리합니다.',
    example: '`api.example.com/v1` → `api-svc:8080`, `/static` → `web-svc:80` 을 하나의 로드밸런서로 처리. cert-manager 와 결합해 Let\'s Encrypt 자동 갱신.' },
  { diff: 'normal', type: 'short', q: 'Pod `api-7d9f` 안의 컨테이너에서 대화형 셸(sh)을 여는 kubectl 명령어는?', answer: 'kubectl exec -it api-7d9f -- sh', accept: ['kubectl exec -it api-7d9f -- /bin/sh', 'kubectl exec -it api-7d9f -- bash', 'kubectl exec -it api-7d9f -- /bin/bash', 'kubectl exec -ti api-7d9f -- sh', 'kubectl exec -it pod/api-7d9f -- sh'],
    explain: '`--` 뒤가 컨테이너에서 실행할 명령. 다중 컨테이너는 `-c NAME`. distroless 이미지는 셸이 없어 `kubectl debug` 를 씁니다.',
    example: 'DNS 확인: `kubectl exec -it api-7d9f -- nslookup postgres.default.svc.cluster.local`.' },
  { diff: 'normal', type: 'ox', q: 'Docker 의 기본 bridge 네트워크에서 컨테이너들은 컨테이너 이름으로 서로를 DNS 조회할 수 있다.', answer: false,
    explain: '**기본** `bridge` 네트워크는 자동 DNS 가 없습니다(레거시 `--link` 필요). `docker network create` 로 만든 사용자 정의 bridge 네트워크는 이름 기반 DNS 를 제공합니다. Compose 는 자동으로 사용자 정의 네트워크를 만듭니다.',
    example: '`docker network create appnet` → `docker run --network appnet --name db postgres` → 다른 컨테이너에서 `db:5432` 로 접속.' },

  /* ---------------- HARD ---------------- */
  { diff: 'hard', type: 'short', q: 'Deployment `api` 의 롤링 업데이트 진행 상황을 완료될 때까지 지켜보는 kubectl 명령어는?', answer: 'kubectl rollout status deployment/api', accept: ['kubectl rollout status deploy/api', 'kubectl rollout status deployment api', 'kubectl rollout status deploy api'],
    explain: '새 ReplicaSet 의 Pod 가 Ready 될 때까지 블로킹하며 `progressDeadlineSeconds` 초과 시 실패를 반환합니다. CI 에서 배포 성공/실패 판정에 사용합니다.',
    example: '`kubectl rollout status deployment/api --timeout=180s || kubectl rollout undo deployment/api` 로 자동 롤백.' },
  { diff: 'hard', type: 'mcq', q: 'Pod 의 `livenessProbe` 와 `readinessProbe` 의 차이로 옳은 것은?', options: ['둘 다 실패 시 컨테이너를 재시작한다', 'liveness 실패 → 컨테이너 재시작, readiness 실패 → Service 엔드포인트에서 제외', 'readiness 실패 → 컨테이너 재시작, liveness 실패 → 트래픽 제외', '둘은 동일하며 이름만 다르다'], answer: 1,
    explain: 'liveness 는 "살아있는가"(데드락 감지), readiness 는 "트래픽 받을 준비가 됐는가"(워밍업, 의존성 대기). `startupProbe` 는 느린 시작 앱을 위해 liveness 를 유예합니다.',
    example: 'DB 연결이 잠시 끊겼을 때 liveness 가 실패하면 재시작 폭풍이 나므로, DB 의존성은 readiness 에만 넣는 것이 안전합니다.' },
  { diff: 'hard', type: 'short', q: '컨테이너 `web` 의 메모리 사용량을 512MB 로 제한하고 CPU 를 1.5 코어로 제한해서 `nginx` 를 백그라운드 실행하는 명령어는?', answer: 'docker run -d --name web --memory=512m --cpus=1.5 nginx', accept: ['docker run -d --name web -m 512m --cpus=1.5 nginx', 'docker run -d --name web --memory 512m --cpus 1.5 nginx', 'docker run -d --memory=512m --cpus=1.5 --name web nginx', 'docker run --name web -d --memory=512m --cpus=1.5 nginx', 'docker run -d -m 512m --cpus=1.5 --name web nginx', 'docker run -d --name web --memory=512m --cpus 1.5 nginx', 'docker run -d --name web -m 512m --cpus 1.5 nginx'],
    explain: '`--memory`/`-m` 은 cgroup 메모리 한도(초과 시 OOM kill), `--cpus` 는 CFS quota 기반 CPU 제한. `docker stats` 로 실제 사용량 확인.',
    example: '한 컨테이너의 메모리 누수가 호스트 전체를 죽이지 않도록 운영 컨테이너에는 항상 메모리 제한을 겁니다.' },
  { diff: 'hard', type: 'mcq', q: 'Kubernetes 에서 `Deployment` 대신 `StatefulSet` 을 써야 하는 경우는?', options: ['무상태 웹 서버를 여러 개 띄울 때', 'Pod 마다 고정된 이름·네트워크 ID·전용 스토리지가 필요할 때 (DB, Kafka 등)', '노드마다 하나씩 실행해야 할 때', '한 번 실행하고 끝나는 배치 작업'], answer: 1,
    explain: 'StatefulSet 은 `pg-0, pg-1` 처럼 순서 있는 이름, 안정적인 DNS(headless Service), Pod 별 PVC(volumeClaimTemplates)를 제공합니다. 노드당 하나는 DaemonSet, 일회성은 Job.',
    example: 'PostgreSQL 클러스터: `pg-0` 이 primary, `pg-1/2` 가 replica. 재시작해도 같은 이름과 볼륨을 유지합니다.' },
  { diff: 'hard', type: 'short', q: 'Node `worker-2` 를 새 Pod 스케줄링에서 제외하고, 기존 Pod 를 안전하게 다른 노드로 옮기는(퇴거) 명령어는?', answer: 'kubectl drain worker-2 --ignore-daemonsets', accept: ['kubectl drain worker-2 --ignore-daemonsets --delete-emptydir-data', 'kubectl drain worker-2 --delete-emptydir-data --ignore-daemonsets', 'kubectl drain worker-2 --ignore-daemonsets --force'],
    explain: '`drain` = `cordon`(스케줄 금지) + Pod 퇴거(evict). DaemonSet Pod 는 옮길 수 없으므로 `--ignore-daemonsets` 필요. 작업 후 `kubectl uncordon worker-2`.',
    example: '노드 커널 업그레이드: `drain` → 패치/재부팅 → `uncordon`. PodDisruptionBudget 이 있으면 최소 가용 수를 지키며 천천히 퇴거됩니다.' },
  { diff: 'hard', type: 'ox', q: 'Dockerfile 에서 `ENV` 로 설정한 값은 빌드 시점뿐 아니라 실행되는 컨테이너에도 환경변수로 남지만, `ARG` 는 빌드 시점에만 존재한다.', answer: true,
    explain: '`ARG` 는 `docker build --build-arg` 로 받는 빌드 변수로 최종 이미지에 남지 않습니다(단, 레이어 히스토리엔 남을 수 있어 비밀 값 전달에는 부적합). `ENV` 는 이미지 메타데이터에 포함됩니다.',
    example: '비밀은 `RUN --mount=type=secret,id=npmrc` (BuildKit) 로 전달해 레이어에 남지 않게 합니다.' },
  { diff: 'hard', type: 'short', q: 'Kubernetes 에서 Pod 별 CPU/메모리 실시간 사용량을 보는 kubectl 명령어는? (네임스페이스: prod)', answer: 'kubectl top pods -n prod', accept: ['kubectl top pod -n prod', 'kubectl top po -n prod', 'kubectl -n prod top pods', 'kubectl -n prod top pod'],
    explain: '`kubectl top` 은 metrics-server 가 설치되어 있어야 동작합니다. `top nodes` 로 노드 단위 확인. `--containers` 로 컨테이너별 분리.',
    example: 'HPA 가 스케일하지 않을 때 `kubectl top pods` 로 실제 사용량이 임계값을 넘는지, metrics-server 가 살아있는지 확인합니다.' },
  { diff: 'hard', type: 'mcq', q: '컨테이너 Exit Code 137 의 의미는?', options: ['앱이 예외로 종료', 'SIGKILL(9) 로 종료됨 — 보통 OOM 또는 강제 종료 (128+9)', '명령을 찾을 수 없음', '정상 종료'], answer: 1,
    explain: '시그널로 종료되면 128 + 시그널 번호. 137 = SIGKILL(OOMKill 또는 `docker kill`), 143 = SIGTERM(정상 종료 요청). 126 = 실행 권한 없음, 127 = 명령 없음.',
    example: '`kubectl describe pod` 에 `Last State: Terminated, Reason: OOMKilled, Exit Code: 137` 이면 메모리 limit 을 올리거나 누수를 잡습니다.' },
  { diff: 'hard', type: 'short', q: 'Kubernetes Secret `db-cred` 의 키 `password` 값을 디코드해서 출력하는 명령어는?', answer: 'kubectl get secret db-cred -o jsonpath="{.data.password}" | base64 -d', accept: ["kubectl get secret db-cred -o jsonpath='{.data.password}' | base64 -d", 'kubectl get secret db-cred -o jsonpath={.data.password} | base64 -d', 'kubectl get secret db-cred -o jsonpath="{.data.password}" | base64 --decode', "kubectl get secret db-cred -o jsonpath='{.data.password}' | base64 --decode", 'kubectl get secret db-cred -o go-template="{{.data.password | base64decode}}"'],
    explain: 'Secret 의 `.data` 는 base64 인코딩. `-o jsonpath` 로 특정 필드 추출 후 `base64 -d`. `kubectl view-secret` 플러그인(krew)도 편리합니다.',
    example: '앱이 인증 실패할 때 Secret 에 개행 문자가 섞였는지 확인: `... | base64 -d | xxd` 로 끝에 `0a` 가 있는지 봅니다 (`echo -n` 누락이 원인).' },
  { diff: 'hard', type: 'mcq', q: 'Kubernetes `NetworkPolicy` 를 생성했는데 아무 효과가 없다. 가장 가능성 높은 원인은?', options: ['NetworkPolicy 는 beta 라 기본 비활성', 'CNI 플러그인(예: Flannel 기본)이 NetworkPolicy 를 지원하지 않음', 'Pod 를 재시작하지 않음', 'Ingress 가 없음'], answer: 1,
    explain: 'NetworkPolicy 는 API 오브젝트일 뿐 실제 강제는 CNI 가 합니다. Calico, Cilium, Weave 는 지원하지만 순수 Flannel 은 미지원입니다.',
    example: '기본 거부 정책: `podSelector: {}` + `policyTypes: [Ingress]` 를 네임스페이스에 적용한 뒤 필요한 통신만 허용하는 zero-trust 구성.' },
  { diff: 'hard', type: 'short', q: 'Docker 이미지 `myapp:1.0` 의 레이어별 생성 명령과 크기 히스토리를 보는 명령어는?', answer: 'docker history myapp:1.0', accept: ['docker image history myapp:1.0', 'docker history --no-trunc myapp:1.0'],
    explain: '각 레이어를 만든 Dockerfile 명령과 크기를 역순으로 보여줍니다. 어떤 레이어가 이미지를 비대하게 하는지 찾을 때 유용. `dive` 툴은 파일 단위까지 보여줍니다.',
    example: '`docker history` 에서 `COPY . .` 레이어가 500MB 면 `.dockerignore` 에 `node_modules`, `.git` 이 빠진 것.' },
  { diff: 'hard', type: 'ox', q: 'Kubernetes 에서 `emptyDir` 볼륨은 Pod 가 다른 노드로 재스케줄되어도 데이터가 유지된다.', answer: false,
    explain: '`emptyDir` 은 Pod 수명과 같습니다. 컨테이너 재시작은 견디지만 Pod 삭제/재스케줄 시 사라집니다. 영속은 PersistentVolumeClaim 을 씁니다.',
    example: '`emptyDir` 은 사이드카 간 임시 파일 공유, 캐시, `medium: Memory` 로 tmpfs 스크래치 공간에 적합합니다.' },
  { diff: 'hard', type: 'short', q: '네임스페이스 `prod` 의 모든 리소스(Pod, Service, Deployment 등)를 한 번에 보는 kubectl 명령어는?', answer: 'kubectl get all -n prod', accept: ['kubectl -n prod get all', 'kubectl get all --namespace prod', 'kubectl get all --namespace=prod', 'kubectl get all -n=prod'],
    explain: '`get all` 은 주요 워크로드 리소스를 보여주지만 ConfigMap, Secret, Ingress, PVC 등은 **포함되지 않습니다**. `kubectl api-resources` 로 전체 종류 확인.',
    example: '배포 검증: `kubectl get all -n prod -l app=api` 로 Deployment/ReplicaSet/Pod/Service 가 모두 정상인지 한눈에 봅니다.' },
  { diff: 'hard', type: 'mcq', q: 'Dockerfile 의 `HEALTHCHECK` 지시어로 정의한 검사가 실패하면 컨테이너 상태는?', options: ['컨테이너가 자동 재시작됨', '`docker ps` 에 `unhealthy` 로 표시되지만 자동 재시작은 하지 않음', '컨테이너가 즉시 종료됨', '아무 변화 없음'], answer: 1,
    explain: 'Docker 단독으로는 상태 표시만 합니다. Compose 의 `depends_on: condition: service_healthy` 나 Swarm 이 이를 활용합니다. Kubernetes 는 HEALTHCHECK 을 무시하고 자체 probe 를 씁니다.',
    example: '`HEALTHCHECK --interval=30s CMD curl -f http://localhost/health || exit 1` 로 Compose 에서 DB 가 준비된 후 앱을 시작하게 합니다.' },

  /* ---------------- EXTREME ---------------- */
  { diff: 'extreme', type: 'mcq', q: 'Kubernetes Pod 가 `Terminating` 상태에서 수십 분째 삭제되지 않는다. 다음 중 가장 흔한 원인은?', options: ['이미지가 너무 큼', 'finalizer 가 남아 있거나, 노드가 NotReady 여서 kubelet 이 종료 확인을 못 함', 'Service 가 아직 참조 중', 'replica 가 1 이라 삭제 불가'], answer: 1,
    explain: 'finalizer 는 삭제 전 정리 작업을 보장하는 메커니즘으로, 담당 컨트롤러가 죽으면 영원히 남습니다. `kubectl patch pod X -p \'{"metadata":{"finalizers":null}}\'` 로 제거하거나, 노드 장애 시 `--force --grace-period=0` 을 씁니다.',
    example: '네임스페이스가 Terminating 에서 멈추는 것도 같은 원리(내부 리소스의 finalizer 또는 응답 없는 APIService).' },
  { diff: 'extreme', type: 'short', q: 'Pod `api-7d9f` 에 디버깅용 임시 컨테이너(ephemeral container)를 `busybox` 이미지로 붙여 셸을 여는 kubectl 명령어는?', answer: 'kubectl debug -it api-7d9f --image=busybox', accept: ['kubectl debug -it api-7d9f --image=busybox -- sh', 'kubectl debug api-7d9f -it --image=busybox', 'kubectl debug -it api-7d9f --image busybox', 'kubectl debug -it pod/api-7d9f --image=busybox', 'kubectl debug -it api-7d9f --image=busybox --target=app'],
    explain: '`kubectl debug` 는 실행 중인 Pod 에 임시 컨테이너를 추가합니다(Pod 재시작 없음). `--target=컨테이너` 로 대상 컨테이너의 프로세스 네임스페이스를 공유하면 `ps` 로 해당 프로세스를 볼 수 있습니다.',
    example: 'distroless 이미지(셸 없음)에서 네트워크 문제 진단: `kubectl debug -it api --image=nicolaka/netshoot --target=app` 후 `tcpdump`, `curl` 사용.' },
  { diff: 'extreme', type: 'mcq', q: 'Docker 컨테이너 안에서 PID 1 로 실행되는 애플리케이션이 `docker stop` 시 10초 후 강제 종료되는(SIGKILL) 문제의 원인과 해결로 옳은 것은?', options: ['앱이 느려서. 타임아웃을 늘리면 됨', '셸 form(`CMD node app.js`)으로 실행돼 PID 1 이 `sh` 가 되어 SIGTERM 을 앱에 전달하지 않음. exec form(`CMD ["node","app.js"]`) 이나 `tini` 사용', 'Docker 버그', 'SIGTERM 은 컨테이너에 전달되지 않음'], answer: 1,
    explain: '셸 form 은 `/bin/sh -c "node app.js"` 로 실행되어 sh 가 PID 1 이 되고, sh 는 시그널을 자식에게 전달하지 않습니다. 또 PID 1 은 기본 시그널 핸들러가 없어 앱 자체가 SIGTERM 핸들러를 구현해야 합니다.',
    example: 'graceful shutdown 이 안 되어 롤링 배포 중 진행 중 요청이 끊기는 사고. `ENTRYPOINT ["tini", "--"]` 또는 `docker run --init` 으로 해결.' },
  { diff: 'extreme', type: 'short', q: 'Kubernetes 에서 `kubectl` 로 현재 사용자가 `prod` 네임스페이스의 Pod 를 삭제할 권한이 있는지 확인하는 명령어는?', answer: 'kubectl auth can-i delete pods -n prod', accept: ['kubectl auth can-i delete pod -n prod', 'kubectl auth can-i delete pods --namespace prod', 'kubectl auth can-i delete pods --namespace=prod', 'kubectl -n prod auth can-i delete pods'],
    explain: '`auth can-i 동사 리소스` 는 RBAC 를 실제로 평가해 yes/no 를 반환합니다. `--as=user` 나 `--as=system:serviceaccount:ns:sa` 로 다른 주체의 권한도 검사, `--list` 로 전체 목록.',
    example: 'CI 서비스어카운트 권한 검증: `kubectl auth can-i create deployments -n prod --as=system:serviceaccount:ci:deployer`.' },
  { diff: 'extreme', type: 'ox', q: 'Kubernetes 에서 CPU `limits` 를 설정하면 실제 사용량이 limit 미만이어도 CFS 쿼터 주기(기본 100ms) 안에서 버스트 사용 시 스로틀링이 발생할 수 있다.', answer: true,
    explain: 'CFS 는 100ms 마다 `limit × 100ms` 만큼의 CPU 시간을 할당합니다. 멀티스레드 앱이 짧은 순간 여러 코어를 쓰면 쿼터를 조기 소진해 나머지 주기 동안 멈춥니다. 평균 사용량은 낮아도 p99 지연이 튑니다.',
    example: '`container_cpu_cfs_throttled_periods_total` 메트릭이 높으면 CPU limit 을 올리거나 제거합니다. 많은 팀이 CPU 는 requests 만 설정합니다.' },
  { diff: 'extreme', type: 'mcq', q: 'kube-apiserver 가 응답하지만 새 Pod 가 어느 노드에도 스케줄되지 않고 `Pending` 이며 Events 에 아무 메시지도 없다. 우선 확인할 컴포넌트는?', options: ['kubelet', 'kube-scheduler', 'kube-proxy', 'CoreDNS'], answer: 1,
    explain: '스케줄링 실패 사유("Insufficient cpu" 등)조차 없다는 것은 스케줄러가 Pod 를 처리하지 않았다는 뜻입니다. `kubectl get pods -n kube-system | grep scheduler` 와 리더 선출 상태를 확인합니다. `spec.schedulerName` 오타도 같은 증상입니다.',
    example: '자체 구축 클러스터에서 컨트롤 플레인 인증서 만료로 scheduler 만 죽어 신규 배포가 전부 Pending 이 된 사례.' },
];
