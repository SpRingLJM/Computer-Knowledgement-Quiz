/* office — 정답 명령어 풀이 (문제 ID → 조각별 설명, 순서 규칙)
 * 결과 화면의 "명령어 풀이" 칸에 표시된다. 조각은 정답 문자열의 부분 문자열이며 tools/validate.js 가 검증한다. */
window.QUIZ_PARTS = window.QUIZ_PARTS || {};
Object.assign(window.QUIZ_PARTS, {
 "office-001": {
  "parts": [
   [
    "ctrl",
    "`Ctrl`(Control, 컨트롤) 키: 키보드 왼쪽 아래 구석에 있는 보조 키로, 혼자서는 아무 일도 하지 않고 다른 키와 함께 눌러 단축 기능을 만듭니다. 맥에서는 이 자리에 `Command`(⌘) 키를 씁니다."
   ],
   [
    "+",
    "`+` 기호: 실제로 누르는 키가 아니라 두 키를 함께 누른다는 뜻의 표기입니다."
   ],
   [
    "s",
    "`S` 키: 저장(Save)의 첫 글자입니다. 지금 열린 문서를 현재 이름 그대로 저장하며, 처음 저장하는 문서라면 파일 이름을 정하는 창이 뜹니다."
   ]
  ],
  "order": "`Ctrl` 을 먼저 누른 채로 유지하고, 그 상태에서 `S` 를 짧게 누른 뒤 손을 뗍니다. `S` 를 먼저 누르면 그냥 글자(또는 키 본래 기능)가 입력되므로 순서가 중요합니다."
 },
 "office-002": {
  "parts": [
   [
    "ctrl",
    "`Ctrl`(Control, 컨트롤) 키: 키보드 왼쪽 아래 구석에 있는 보조 키로, 혼자서는 아무 일도 하지 않고 다른 키와 함께 눌러 단축 기능을 만듭니다. 맥에서는 이 자리에 `Command`(⌘) 키를 씁니다."
   ],
   [
    "+",
    "`+` 기호: 실제로 누르는 키가 아니라 두 키를 함께 누른다는 뜻의 표기입니다."
   ],
   [
    "z",
    "`Z` 키: 실행 취소(Undo)입니다. 약자는 아니고, 자주 쓰도록 `X`·`C`·`V` 와 나란히 왼손 가까이에 배치된 키입니다. 누를 때마다 한 단계씩 이전 상태로 돌아갑니다."
   ]
  ],
  "order": "`Ctrl` 을 먼저 누른 채로 유지하고, 그 상태에서 `Z` 를 짧게 누른 뒤 손을 뗍니다. `Z` 를 먼저 누르면 그냥 글자(또는 키 본래 기능)가 입력되므로 순서가 중요합니다."
 },
 "office-009": {
  "parts": [
   [
    "ctrl",
    "`Ctrl`(Control, 컨트롤) 키: 키보드 왼쪽 아래 구석에 있는 보조 키로, 혼자서는 아무 일도 하지 않고 다른 키와 함께 눌러 단축 기능을 만듭니다. 맥에서는 이 자리에 `Command`(⌘) 키를 씁니다."
   ],
   [
    "+",
    "`+` 기호: 실제로 누르는 키가 아니라 두 키를 함께 누른다는 뜻의 표기입니다."
   ],
   [
    "f",
    "`F` 키: 찾기(Find)의 첫 글자입니다. 누르면 검색 칸이 열리고, 단어를 입력하면 화면에서 그 단어가 있는 곳을 표시해 줍니다."
   ]
  ],
  "order": "`Ctrl` 을 먼저 누른 채로 유지하고, 그 상태에서 `F` 를 짧게 누른 뒤 손을 뗍니다. `F` 를 먼저 누르면 그냥 글자(또는 키 본래 기능)가 입력되므로 순서가 중요합니다."
 },
 "office-028": {
  "parts": [
   [
    "ctrl",
    "`Ctrl`(Control, 컨트롤) 키: 키보드 왼쪽 아래 구석에 있는 보조 키로, 혼자서는 아무 일도 하지 않고 다른 키와 함께 눌러 단축 기능을 만듭니다. 맥에서는 이 자리에 `Command`(⌘) 키를 씁니다."
   ],
   [
    "+",
    "`+` 기호: 실제로 누르는 키가 아니라 두 키를 함께 누른다는 뜻의 표기입니다."
   ],
   [
    "a",
    "`A` 키: 전체(All)의 첫 글자입니다. 파일 탐색기에서는 지금 폴더의 모든 파일을, 문서에서는 모든 글자를 한 번에 선택합니다."
   ]
  ],
  "order": "`Ctrl` 을 먼저 누른 채로 유지하고, 그 상태에서 `A` 를 짧게 누른 뒤 손을 뗍니다. `A` 를 먼저 누르면 그냥 글자(또는 키 본래 기능)가 입력되므로 순서가 중요합니다."
 }
});
