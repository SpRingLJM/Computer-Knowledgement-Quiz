window.QUIZ_BANK.network.push(
  /* ---------------- EASY ---------------- */
  { diff: 'easy', type: 'mcq', q: 'SSH 의 기본 포트 번호는?', options: ['21', '22', '23', '25'], answer: 1,
    explain: '22 SSH, 21 FTP 제어, 23 Telnet(평문, 사용 금지), 25 SMTP. 포트를 바꿔도 보안이 크게 좋아지진 않지만 스캐너 잡음은 줄어듭니다.',
    example: 'Security Group 에서 22 를 사무실 IP 로만 제한하거나 Session Manager 로 대체.' },
  { diff: 'easy', type: 'short', q: '`https://example.com` 의 HTML 본문을 파일 `page.html` 로 저장하는 curl 명령어는?', answer: 'curl -o page.html https://example.com', accept: ['curl https://example.com -o page.html', 'curl -so page.html https://example.com', 'curl -s -o page.html https://example.com', 'curl https://example.com > page.html', 'wget -O page.html https://example.com'],
    explain: '`-o 파일명` 으로 저장, `-O` 는 URL 의 파일명 그대로. `-L` 로 리다이렉트 따라가기(기본은 안 따라감).',
    example: '설치 스크립트 다운로드: `curl -fsSL https://get.docker.com -o get-docker.sh` (`-f` 는 HTTP 오류 시 실패 처리).' },
  { diff: 'easy', type: 'ox', q: 'HTTP 상태 코드 5xx 는 서버 측 오류, 4xx 는 클라이언트 측 요청 오류를 뜻한다.', answer: true,
    explain: '4xx 는 요청 자체의 문제(권한, 경로, 형식), 5xx 는 서버가 요청을 처리하지 못함. 모니터링 SLO 는 보통 5xx 비율로 정의합니다.',
    example: '배포 후 5xx 급증은 롤백 트리거, 4xx 급증은 클라이언트 변경(앱 배포)이나 공격 의심.' },
  { diff: 'easy', type: 'short', q: '도메인 `example.com` 의 네임서버(NS) 레코드를 조회하는 dig 명령어는?', answer: 'dig example.com NS', accept: ['dig NS example.com', 'dig example.com NS +short', 'dig +short NS example.com', 'dig +short example.com NS'],
    explain: 'NS 레코드는 그 도메인을 권위 있게 응답하는 서버입니다. 도메인 이전 시 등록기관의 NS 와 호스팅 존의 NS 가 일치해야 합니다.',
    example: 'Route 53 으로 옮긴 뒤 `dig example.com NS +short` 가 `awsdns-*` 를 반환하는지 확인.' },
  { diff: 'easy', type: 'mcq', q: '`192.168.0.0/16` 대역에 포함되는 IP 는?', options: ['192.169.1.1', '192.168.200.5', '10.168.0.1', '172.16.8.8'], answer: 1,
    explain: '/16 은 앞 두 옥텟(192.168)이 고정. 192.168.0.0 ~ 192.168.255.255 가 범위.',
    example: '보안 그룹에 `192.168.0.0/16` 을 허용하면 사무실 내부망 전체를 허용하는 셈입니다.' },
  { diff: 'easy', type: 'short', q: '리눅스에서 시스템이 사용하는 DNS 리졸버 설정 파일 경로는?', answer: '/etc/resolv.conf', accept: ['cat /etc/resolv.conf', 'resolv.conf'],
    explain: '`nameserver`, `search`, `options ndots:` 등이 있습니다. systemd-resolved 환경에서는 `127.0.0.53` 스텁이 있고 실제 서버는 `resolvectl status` 로 봅니다.',
    example: '컨테이너 안에서 DNS 가 안 될 때 `cat /etc/resolv.conf` 로 nameserver 가 클러스터 DNS(10.96.0.10 등)인지 확인.' },
  { diff: 'easy', type: 'ox', q: 'HTTP 메서드 GET 은 데이터를 조회하고, POST 는 서버에 데이터를 제출한다. GET 요청은 본문(body)이 아닌 URL 쿼리로 인자를 전달하는 것이 일반적이다.', answer: true,
    explain: 'GET 은 멱등·캐시 가능, POST 는 비멱등. PUT(전체 교체, 멱등), PATCH(부분 수정), DELETE 도 있습니다.',
    example: '검색은 `GET /search?q=x`(북마크·캐시 가능), 결제는 `POST /orders`.' },
  { diff: 'easy', type: 'short', q: '호스트의 기본 게이트웨이로 가는 경로를 포함해 `8.8.8.8` 까지 어떤 인터페이스·게이트웨이가 사용되는지 보는 명령어는?', answer: 'ip route get 8.8.8.8', accept: ['ip r get 8.8.8.8'],
    explain: '라우팅 테이블 전체를 읽지 않아도 특정 목적지의 실제 결정을 보여줍니다. `src` 로 소스 IP 도 확인.',
    example: '다중 NIC/VPN 환경에서 트래픽이 어느 인터페이스로 나가는지 검증.' },
  { diff: 'easy', type: 'mcq', q: '`ping` 이 사용하는 프로토콜은?', options: ['TCP', 'UDP', 'ICMP', 'HTTP'], answer: 2,
    explain: 'ICMP Echo Request/Reply. ICMP 는 포트가 없으므로 "포트가 열렸는지" 확인에는 쓸 수 없습니다. 그건 `nc -zv` 나 `curl`.',
    example: 'ping 은 되는데 서비스 접속이 안 되면 L3 는 정상, L4(포트/방화벽) 이상을 의심.' },
  { diff: 'easy', type: 'short', q: '리눅스에서 ARP 캐시(IP ↔ MAC 매핑)를 보는 iproute2 명령어는?', answer: 'ip neigh', accept: ['ip n', 'ip neighbor', 'ip neighbour', 'ip neigh show', 'arp -a', 'arp -n'],
    explain: '`REACHABLE`, `STALE`, `FAILED` 상태를 보여줍니다. `FAILED` 가 많으면 같은 L2 에 상대가 없거나 IP 충돌.',
    example: '같은 서브넷의 서버가 ping 이 안 될 때 `ip neigh` 에서 MAC 이 해석되는지 확인.' },
  { diff: 'easy', type: 'mcq', q: '클라이언트가 인증서 오류 없이 HTTPS 사이트에 접속하려면 서버 인증서가 갖춰야 할 조건이 **아닌** 것은?', options: ['신뢰된 CA 체인으로 서명됨', '유효 기간 내', '도메인 이름이 SAN/CN 과 일치', '서버 IP 가 인증서에 포함됨'], answer: 3,
    explain: 'IP 는 필수가 아닙니다(IP SAN 도 가능하지만 드묾). 흔한 실패: 만료, 중간 인증서 누락, `www.` 유무 불일치, 자체 서명.',
    example: '`curl -v https://host` 출력의 `SSL certificate verify` 줄로 어느 조건이 실패했는지 확인.' },
  { diff: 'easy', type: 'ox', q: '`0.0.0.0/0` 은 "모든 IPv4 주소" 를 의미하며, 라우트 테이블에서는 기본 경로(default route)를 뜻한다.', answer: true,
    explain: '보안 그룹에서 `0.0.0.0/0` 허용은 인터넷 전체 개방. IPv6 는 `::/0`.',
    example: '퍼블릭 서브넷 라우트 테이블: `0.0.0.0/0 → igw-xxx`, 프라이빗: `0.0.0.0/0 → nat-xxx`.' },
  { diff: 'easy', type: 'short', q: 'HTTP 요청에 `Authorization: Bearer TOKEN` 헤더를 붙여 `https://api.example.com/me` 를 호출하는 curl 명령어는?', answer: 'curl -H "Authorization: Bearer TOKEN" https://api.example.com/me', accept: ["curl -H 'Authorization: Bearer TOKEN' https://api.example.com/me", 'curl https://api.example.com/me -H "Authorization: Bearer TOKEN"', 'curl -s -H "Authorization: Bearer TOKEN" https://api.example.com/me', 'curl --oauth2-bearer TOKEN https://api.example.com/me'],
    explain: '`-H` 로 임의 헤더 추가. Basic 인증은 `-u user:pass`. 토큰을 셸 이력에 남기지 않으려면 환경변수나 `-K` 설정 파일 사용.',
    example: 'JWT 만료 여부 확인: 401 이면 토큰 갱신, 403 이면 권한 문제.' },
  { diff: 'easy', type: 'mcq', q: '스위치와 라우터의 기본 역할 차이는?', options: ['스위치는 L3, 라우터는 L2', '스위치는 같은 네트워크 내 MAC 기반 전달(L2), 라우터는 네트워크 간 IP 기반 전달(L3)', '둘 다 동일', '라우터는 무선 전용'], answer: 1,
    explain: 'L3 스위치는 라우팅도 하지만 개념적 구분은 유지됩니다. 클라우드에서는 VPC 라우터가 서브넷 간 라우팅을 담당.',
    example: '같은 서브넷 통신은 ARP+스위칭, 다른 서브넷은 게이트웨이(라우터)를 거칩니다.' },

  /* ---------------- NORMAL ---------------- */
  { diff: 'normal', type: 'short', q: '`https://api.example.com` 에 요청하면서 DNS 를 무시하고 강제로 `10.0.1.5` 로 연결(호스트 헤더·SNI 는 유지)하는 curl 명령어는?', answer: 'curl --resolve api.example.com:443:10.0.1.5 https://api.example.com', accept: ['curl --resolve api.example.com:443:10.0.1.5 https://api.example.com/', 'curl -v --resolve api.example.com:443:10.0.1.5 https://api.example.com', 'curl --resolve "api.example.com:443:10.0.1.5" https://api.example.com'],
    explain: '`--resolve host:port:ip` 는 DNS 전환 전에 새 서버를 테스트하거나 로드밸런서 뒤 특정 인스턴스를 직접 검증할 때 씁니다. `-H "Host: ..."` 만으로는 TLS SNI 가 맞지 않습니다.',
    example: '블루/그린 전환 전 그린 환경을 실제 도메인·인증서로 검증.' },
  { diff: 'normal', type: 'mcq', q: 'TCP 연결 종료 시 `FIN_WAIT_2` 상태가 오래 남는 경우의 의미는?', options: ['서버가 SYN 을 못 받음', '우리 쪽은 FIN 을 보냈으나 상대가 자기 FIN 을 보내지 않음(half-close) — 상대 앱이 소켓을 닫지 않음', '라우팅 실패', '정상적으로 연결 유지 중'], answer: 1,
    explain: '`tcp_fin_timeout` 이 이 상태의 타임아웃(기본 60s)입니다. 상대 앱의 소켓 누수나 close 누락을 의심.',
    example: '프록시 뒤 앱이 응답 후 소켓을 닫지 않아 FIN_WAIT_2 가 쌓이고 fd 를 소진하는 사례.' },
  { diff: 'normal', type: 'short', q: '`example.com` 의 TXT 레코드(SPF 등)를 조회하는 dig 명령어는?', answer: 'dig example.com TXT', accept: ['dig TXT example.com', 'dig example.com TXT +short', 'dig +short example.com TXT', 'dig +short TXT example.com', 'dig -t TXT example.com'],
    explain: 'TXT 는 SPF(`v=spf1 ...`), DKIM, DMARC(`_dmarc.example.com`), 도메인 소유 검증(ACM, Google) 에 사용됩니다.',
    example: 'ACM 인증서 발급 대기 중이면 `dig _abc123.example.com CNAME` 으로 검증 레코드가 조회되는지 확인.' },
  { diff: 'normal', type: 'mcq', q: '서브넷 마스크 `255.255.255.192` 의 CIDR 표기는?', options: ['/24', '/25', '/26', '/27'], answer: 2,
    explain: '192 = 11000000 → 마지막 옥텟 2비트 → 24+2 = /26 (64개 주소). 224 = /27, 240 = /28, 128 = /25.',
    example: 'AZ 마다 /26 서브넷(59개 사용 가능)으로 소규모 VPC 를 나누는 계획.' },
  { diff: 'normal', type: 'short', q: '`curl` 로 리다이렉트를 따라가며 최종 URL 까지 이동하고, 상세 요청/응답 과정을 보는 명령어는? (URL: http://example.com)', answer: 'curl -vL http://example.com', accept: ['curl -Lv http://example.com', 'curl -v -L http://example.com', 'curl -L -v http://example.com', 'curl -vL http://example.com/', 'curl -vsL http://example.com', 'curl -svL http://example.com'],
    explain: '`-L` 은 3xx 를 따라감(`--max-redirs` 로 제한), `-v` 는 요청 헤더(`>`), 응답 헤더(`<`), TLS 핸드셰이크를 출력. `-w "%{url_effective}"` 로 최종 URL 만.',
    example: 'HTTP→HTTPS 리다이렉트 체인 검증, 무한 리다이렉트 루프 진단.' },
  { diff: 'normal', type: 'ox', q: 'ICMP 를 방화벽에서 전부 차단하면 ping 만 안 되는 것이 아니라 Path MTU Discovery 도 실패해 큰 패킷 전송 문제가 생길 수 있다.', answer: true,
    explain: 'ICMP Type 3 Code 4 (Fragmentation Needed) 는 PMTUD 에 필수입니다. 최소한 이 메시지와 Time Exceeded 는 허용해야 합니다.',
    example: '"보안상 ICMP 전체 차단" 정책 후 VPN 을 통한 파일 다운로드가 멈추는 사례.' },
  { diff: 'normal', type: 'short', q: '리눅스 서버의 모든 인터페이스에서 특정 호스트 `10.0.0.5` 와 주고받는 패킷만 캡처하는 tcpdump 명령어는?', answer: 'tcpdump -i any host 10.0.0.5', accept: ['tcpdump -i any -n host 10.0.0.5', 'sudo tcpdump -i any host 10.0.0.5', 'tcpdump -ni any host 10.0.0.5', 'tcpdump -i any -nn host 10.0.0.5', 'sudo tcpdump -i any -n host 10.0.0.5'],
    explain: '`-i any` 는 모든 인터페이스(컨테이너 veth 포함), `host` 는 src 또는 dst. `src host`/`dst host` 로 방향 지정, `and port 443` 로 결합.',
    example: '특정 클라이언트의 요청이 서버까지 오는지 확인: `tcpdump -i any -n host 203.0.113.5 and port 443`.' },
  { diff: 'normal', type: 'mcq', q: 'HTTP 응답 헤더 `ETag` 와 요청 헤더 `If-None-Match` 의 조합이 하는 일은?', options: ['압축 협상', '조건부 요청 — 리소스가 안 바뀌었으면 304 Not Modified 로 본문 없이 응답해 대역폭 절약', '인증', 'CORS 허용'], answer: 1,
    explain: 'ETag 는 리소스 버전 식별자. `Last-Modified`/`If-Modified-Since` 도 같은 역할. `Cache-Control: no-cache` 와 함께 쓰면 매번 재검증하되 변경 없으면 304.',
    example: '`curl -I` 로 ETag 를 받은 뒤 `curl -H \'If-None-Match: "abc"\'` 로 304 확인.' },
  { diff: 'normal', type: 'short', q: '로컬에서 `9090` 포트를 열어 원격 `bastion` 을 통해 SOCKS5 프록시로 사용하는 SSH 명령어는?', answer: 'ssh -D 9090 bastion', accept: ['ssh -D 9090 user@bastion', 'ssh -N -D 9090 bastion', 'ssh -ND 9090 bastion', 'ssh -fN -D 9090 bastion', 'ssh -fND 9090 bastion', 'ssh -N -D 9090 user@bastion'],
    explain: '`-D` 는 동적 포트 포워딩. 브라우저나 `curl --socks5-hostname localhost:9090` 로 사내망의 여러 호스트에 접근할 수 있어 `-L` 을 여러 개 만들 필요가 없습니다.',
    example: '프라이빗 서브넷의 내부 대시보드(Grafana, Kibana)를 브라우저 SOCKS 설정으로 접근.' },
  { diff: 'normal', type: 'mcq', q: 'DNS 레코드 타입 중 IPv6 주소를 가리키는 것은?', options: ['A', 'AAAA', 'PTR', 'SRV'], answer: 1,
    explain: 'A = IPv4, AAAA = IPv6, PTR = 역방향(IP→이름), SRV = 서비스 위치(포트 포함), CAA = 인증서 발급 CA 제한.',
    example: '듀얼스택 서비스는 A 와 AAAA 를 모두 두고, `curl -6`/`-4` 로 각각 테스트.' },
  { diff: 'normal', type: 'short', q: 'IP `203.0.113.10` 의 역방향 DNS(PTR) 를 조회하는 dig 명령어는?', answer: 'dig -x 203.0.113.10', accept: ['dig -x 203.0.113.10 +short', 'dig +short -x 203.0.113.10', 'host 203.0.113.10', 'nslookup 203.0.113.10'],
    explain: '`-x` 가 `10.113.0.203.in-addr.arpa PTR` 조회로 변환합니다. 메일 서버는 PTR 이 없으면 스팸으로 분류되기 쉽습니다.',
    example: '접근 로그의 IP 가 어느 조직(크롤러, 클라우드)인지 빠르게 확인.' },
  { diff: 'normal', type: 'ox', q: 'TCP keepalive 는 유휴 연결이 아직 살아 있는지 주기적으로 확인하는 기능이며, 리눅스 기본값은 2시간 후 첫 probe 를 보낸다.', answer: true,
    explain: '`net.ipv4.tcp_keepalive_time`(7200s), `_intvl`(75s), `_probes`(9). 앱이 `SO_KEEPALIVE` 를 켜야 동작합니다. NAT/LB 의 idle timeout 보다 짧게 설정해야 연결이 끊기지 않습니다.',
    example: 'DB 커넥션이 NAT 뒤에서 조용히 끊겨 첫 쿼리가 멈추는 문제 → keepalive_time 을 300s 로 낮춤.' },
  { diff: 'normal', type: 'short', q: '리눅스에서 TCP 연결 상태별 개수를 요약해 보는 명령어는?', answer: 'ss -s', accept: ['ss -tan | awk \'{print $1}\' | sort | uniq -c', 'netstat -s', "ss -tan | awk '{print $1}' | sort | uniq -c | sort -rn", 'ss -tan state established | wc -l'],
    explain: '`ss -s` 는 TCP estab/timewait/orphaned 등 요약을 즉시 보여줍니다. 상태별 상세 집계는 `ss -tan | awk \'{print $1}\' | sort | uniq -c`.',
    example: '연결 폭주 시 ESTAB 이 급증하는지, TIME-WAIT 이 쌓이는지 한눈에 파악.' },
  { diff: 'normal', type: 'mcq', q: 'CORS 오류 "No \'Access-Control-Allow-Origin\' header" 를 브라우저 콘솔에서 봤다. 어디를 고쳐야 하는가?', options: ['브라우저 설정', '프론트엔드 코드', 'API 서버(또는 그 앞의 프록시/게이트웨이)의 응답 헤더', 'DNS'], answer: 2,
    explain: 'CORS 는 서버가 허용 출처를 응답 헤더로 선언하는 브라우저 보안 정책입니다. `Access-Control-Allow-Origin`, 비단순 요청은 `OPTIONS` preflight 도 처리해야 합니다. curl 은 CORS 를 검사하지 않으므로 "curl 은 되는데 브라우저만 안 됨" 이 전형적 증상.',
    example: 'API Gateway/nginx 에서 preflight OPTIONS 에 204 와 허용 헤더를 반환하도록 설정.' },
  { diff: 'normal', type: 'short', q: '`ss` 로 현재 ESTABLISHED 상태의 TCP 연결 중 목적지 포트가 443 인 것만 보는 명령어는?', answer: "ss -tan state established '( dport = :443 )'", accept: ['ss -tan state established "( dport = :443 )"', "ss -tn state established '( dport = :443 )'", 'ss -tan | grep ESTAB | grep :443', 'ss -tan state established dport = :443', "ss -tan state established '( dport = :https )'"],
    explain: 'ss 의 필터 문법: `state STATE`, `( dport = :PORT )`, `src`/`dst`. 따옴표는 셸의 괄호 해석을 막기 위함.',
    example: '서버가 외부 API 로 얼마나 많은 연결을 열고 있는지, 특정 업스트림으로의 연결 수 확인.' },
  { diff: 'normal', type: 'ox', q: 'UDP 는 연결 상태가 없으므로, 방화벽/NAT 은 UDP "세션" 을 마지막 패킷 이후 타임아웃(보통 30초~수분)으로 추적한다.', answer: true,
    explain: 'stateful 방화벽은 UDP 도 5-튜플로 항목을 만들지만 FIN 이 없어 타임아웃에 의존합니다. 긴 간격의 UDP 통신(VoIP, VPN)은 keepalive 가 필요.',
    example: 'WireGuard 의 `PersistentKeepalive = 25` 가 NAT 항목 유지 목적입니다.' },
  { diff: 'normal', type: 'short', q: 'nginx 설정 파일 문법을 검사(적용하지 않고)하는 명령어는?', answer: 'nginx -t', accept: ['sudo nginx -t', 'nginx -T'],
    explain: '`-t` 는 파일 존재·문법·include 를 검사, `-T` 는 검사 후 전체 설정을 덤프. 검사 통과 후 `nginx -s reload` 또는 `systemctl reload nginx` 로 무중단 적용.',
    example: 'CI 에서 설정 변경 PR 마다 `nginx -t` 를 돌려 잘못된 설정 배포를 방지.' },

  /* ---------------- HARD ---------------- */
  { diff: 'hard', type: 'short', q: 'HTTP/2 로 `https://example.com` 에 요청하고 사용된 프로토콜 버전을 출력하는 curl 명령어는?', answer: 'curl -sI --http2 https://example.com -w "%{http_version}\\n" -o /dev/null', accept: ['curl -s --http2 -o /dev/null -w "%{http_version}" https://example.com', "curl -s --http2 -o /dev/null -w '%{http_version}' https://example.com", 'curl -sI --http2 https://example.com', 'curl -I --http2 https://example.com', 'curl -s --http2 -w "%{http_version}" -o /dev/null https://example.com', 'curl --http2 -sI https://example.com'],
    explain: '`-I` 출력의 첫 줄 `HTTP/2 200` 으로도 확인됩니다. `--http1.1`, `--http3` 로 강제. ALPN 협상 실패 시 HTTP/1.1 로 폴백됩니다.',
    example: 'CDN/LB 가 HTTP/2 를 종단하고 오리진에는 1.1 로 가는 구성이 흔하므로, 오리진 직접 테스트와 구분.' },
  { diff: 'hard', type: 'mcq', q: '`tcpdump` 캡처에서 클라이언트 SYN 에 서버가 즉시 `RST` 로 응답한다. 의미는?', options: ['방화벽이 패킷을 드롭', '해당 포트에 리스닝 프로세스가 없음 (connection refused)', '네트워크 혼잡', 'TLS 오류'], answer: 1,
    explain: 'RST = 포트 닫힘(즉시 실패, "Connection refused"). 드롭 = 응답 없음(타임아웃). 이 차이로 "방화벽 문제" 와 "서비스가 안 떠 있음" 을 구분합니다.',
    example: '배포 직후 refused 면 앱이 아직 바인딩 전 또는 잘못된 포트, 타임아웃이면 SG/NACL.' },
  { diff: 'hard', type: 'short', q: '리눅스에서 `eth0` 의 MTU 를 1400 으로 즉시 변경하는 iproute2 명령어는?', answer: 'ip link set dev eth0 mtu 1400', accept: ['ip link set eth0 mtu 1400', 'sudo ip link set dev eth0 mtu 1400', 'sudo ip link set eth0 mtu 1400'],
    explain: '재부팅 시 초기화되므로 netplan/NetworkManager/systemd-networkd 설정에 영구 반영해야 합니다. 터널(VXLAN 50바이트, IPsec ~60바이트) 오버헤드만큼 낮춥니다.',
    example: 'VPN/오버레이 네트워크에서 큰 응답이 멈출 때 MTU 를 낮춰 즉시 검증.' },
  { diff: 'hard', type: 'mcq', q: 'HSTS `preload` 를 설정한 도메인의 위험은?', options: ['성능 저하', '브라우저 내장 목록에 등록되어 HTTP 로 되돌리기가 사실상 불가능해지며, 서브도메인까지 HTTPS 를 강제', 'SEO 불이익', 'CDN 비용 증가'], answer: 1,
    explain: '`includeSubDomains; preload` 는 인증서가 없는 내부 서브도메인(http 만 쓰는 사내 툴)도 접속 불가하게 만듭니다. 목록 제거는 수개월이 걸립니다.',
    example: 'preload 전에 모든 서브도메인의 HTTPS 를 확인하고 `max-age` 를 짧게 두어 단계적으로 늘립니다.' },
  { diff: 'hard', type: 'short', q: 'nftables 로 현재 적용된 모든 방화벽 규칙을 출력하는 명령어는?', answer: 'nft list ruleset', accept: ['sudo nft list ruleset', 'nft -a list ruleset'],
    explain: 'nftables 는 iptables 의 후속입니다. 최신 배포판은 `iptables` 명령도 내부적으로 nft 를 씁니다(iptables-nft). `-a` 는 규칙 핸들 표시(삭제 시 필요).',
    example: 'Docker/Kubernetes 가 추가한 NAT 규칙과 수동 규칙이 섞여 있을 때 전체를 한 번에 확인.' },
  { diff: 'hard', type: 'mcq', q: '클라이언트가 TLS 핸드셰이크 중 "handshake_failure" 를 받는다. 가장 흔한 원인은?', options: ['DNS 오류', '클라이언트와 서버 간 공통 cipher suite/TLS 버전이 없음 (예: 서버가 TLS 1.2+ 만 허용, 구형 클라이언트) 또는 SNI 누락', '인증서 만료', '포트가 닫힘'], answer: 1,
    explain: '`openssl s_client -connect host:443 -tls1_2` / `-tls1_3` / `-cipher` 로 어떤 조합이 되는지 좁힙니다. Java 7, Python 2, 구형 안드로이드가 TLS 1.0/1.1 만 쓰는 경우가 대표적.',
    example: 'ALB 보안 정책을 `TLS13-1-2-2021-06` 으로 올린 뒤 오래된 IoT 장비가 접속 실패.' },
  { diff: 'hard', type: 'short', q: '`openssl` 로 서버 `example.com:443` 이 TLS 1.3 을 지원하는지 테스트하는 명령어는?', answer: 'openssl s_client -connect example.com:443 -tls1_3', accept: ['openssl s_client -connect example.com:443 -tls1_3 </dev/null', 'echo | openssl s_client -connect example.com:443 -tls1_3', 'openssl s_client -tls1_3 -connect example.com:443', 'openssl s_client -connect example.com:443 -servername example.com -tls1_3', 'echo | openssl s_client -connect example.com:443 -servername example.com -tls1_3'],
    explain: '출력의 `Protocol : TLSv1.3` 과 `Cipher` 를 확인. 실패하면 "no protocols available" 또는 handshake failure. `nmap --script ssl-enum-ciphers -p 443 host` 로 전체 목록도 가능.',
    example: '보안 감사에서 TLS 1.0/1.1 비활성화 확인: `-tls1` 이 실패해야 정상.' },
  { diff: 'hard', type: 'ox', q: 'TCP window scaling 이 비활성화되면 수신 윈도우가 최대 64KB 로 제한되어, 지연이 큰(고 RTT) 링크에서 처리량이 크게 떨어진다.', answer: true,
    explain: '대역폭 지연 곱(BDP) = 대역폭 × RTT 만큼 윈도우가 필요합니다. 100ms RTT 에서 64KB 윈도우면 최대 ~5Mbps. `net.ipv4.tcp_window_scaling=1` 과 `tcp_rmem/wmem` 최대값 조정.',
    example: '대륙 간 S3 전송이 느릴 때 윈도우 크기와 BBR 혼잡 제어(`net.ipv4.tcp_congestion_control=bbr`)를 검토.' },
  { diff: 'hard', type: 'short', q: '리눅스에서 현재 TCP 혼잡 제어 알고리즘을 확인하는 명령어는?', answer: 'sysctl net.ipv4.tcp_congestion_control', accept: ['cat /proc/sys/net/ipv4/tcp_congestion_control', 'sysctl -n net.ipv4.tcp_congestion_control', 'sysctl net.ipv4.tcp_available_congestion_control'],
    explain: '기본 `cubic`. `bbr` 은 손실이 아닌 대역폭·RTT 추정 기반이라 손실 있는 장거리 링크에서 처리량이 좋습니다. `modprobe tcp_bbr` 후 sysctl 로 변경.',
    example: '글로벌 사용자 대상 다운로드 서버, 위성/모바일 링크에서 BBR 적용으로 처리량 개선.' },
  { diff: 'hard', type: 'mcq', q: 'DNS 응답이 `SERVFAIL` 인데 다른 리졸버(8.8.8.8)에서는 정상이다. 의심할 것은?', options: ['도메인 만료', '사용 중인 리졸버의 DNSSEC 검증 실패 또는 해당 리졸버에서 권위 서버 도달 불가', '레코드 TTL 이 0', '브라우저 캐시'], answer: 1,
    explain: 'SERVFAIL 은 리졸버가 답을 구하지 못한 상태. `dig +cd`(checking disabled) 로 응답이 오면 DNSSEC 문제. `dig +trace` 로 위임 경로를 따라가며 어디서 끊기는지 확인.',
    example: 'DS 레코드를 등록기관에 남긴 채 DNS 호스팅을 옮겨 DNSSEC 체인이 깨지는 사고.' },
  { diff: 'hard', type: 'short', q: '루트 서버부터 위임을 따라가며 `example.com` 의 A 레코드 해석 과정을 추적하는 dig 명령어는?', answer: 'dig +trace example.com', accept: ['dig example.com +trace', 'dig +trace example.com A', 'dig +trace A example.com'],
    explain: '루트 → TLD(.com) → 권위 NS 순으로 각 단계의 응답을 보여줍니다. 캐시를 거치지 않으므로 전파 확인과 위임 오류 진단에 적합.',
    example: 'NS 변경 후 TLD 가 새 네임서버를 가리키는지 `+trace` 로 확인.' },
  { diff: 'hard', type: 'mcq', q: '로드밸런서 뒤에서 WebSocket 이나 긴 폴링 연결이 정확히 60초마다 끊긴다. 원인은?', options: ['클라이언트 버그', 'LB 의 idle timeout(ALB 기본 60s) — 그 안에 데이터가 오가지 않으면 연결 종료', 'DNS TTL', 'TLS 세션 만료'], answer: 1,
    explain: '주기적 ping/pong 프레임(예: 30초)으로 연결을 살리거나 LB idle timeout 을 늘립니다. nginx 는 `proxy_read_timeout` 이 같은 역할.',
    example: '채팅 앱이 정확히 1분 유휴 후 재연결하는 현상 → 하트비트 추가로 해결.' },
  { diff: 'hard', type: 'short', q: '`ip` 명령으로 `eth0` 에 보조 IP `10.0.1.20/24` 를 추가하는 명령어는?', answer: 'ip addr add 10.0.1.20/24 dev eth0', accept: ['sudo ip addr add 10.0.1.20/24 dev eth0', 'ip address add 10.0.1.20/24 dev eth0', 'ip a add 10.0.1.20/24 dev eth0'],
    explain: '`ip addr add/del`. 재부팅 시 사라지므로 네트워크 설정에 반영. 클라우드에서는 ENI 에도 보조 프라이빗 IP 를 할당해야 트래픽이 도달합니다.',
    example: 'VIP 를 이용한 keepalived 장애 조치, 한 NIC 에 여러 서비스 IP 운영.' },
  { diff: 'hard', type: 'ox', q: 'SNI(Server Name Indication)는 TLS ClientHello 에 접속하려는 호스트명을 평문으로 담아 하나의 IP 에서 여러 도메인의 인증서를 서비스할 수 있게 하며, ECH(Encrypted Client Hello)가 이를 암호화하는 확장이다.', answer: true,
    explain: 'SNI 덕분에 ALB/nginx 가 도메인별 인증서를 고릅니다. `curl` 은 URL 호스트명을 SNI 로 보내고, `openssl s_client` 는 `-servername` 이 필요합니다. SNI 가 없으면 기본 인증서가 반환되어 검증 실패.',
    example: 'IP 로 직접 `https://1.2.3.4` 접속 시 인증서 불일치가 나는 이유, 그리고 SNI 기반 방화벽 필터링의 원리.' },
  { diff: 'hard', type: 'mcq', q: '`traceroute` 결과 중간 홉들이 `* * *` 로 표시되지만 최종 목적지에는 도달한다. 해석은?', options: ['네트워크 장애', '해당 라우터들이 ICMP Time Exceeded 응답을 보내지 않도록 설정됨 — 경로 자체는 정상', '패킷 손실 100%', 'MTU 문제'], answer: 1,
    explain: '많은 라우터·클라우드 백본이 traceroute 응답을 억제합니다. 최종 홉이 응답하면 연결성은 정상. `mtr` 에서 특정 홉만 손실이고 이후 홉이 정상이면 그 홉의 ICMP 제한일 뿐입니다.',
    example: 'AWS 내부 홉이 `*` 로 나오는 것은 정상이며, 장애 판단은 마지막 홉의 손실률로 합니다.' },
  { diff: 'hard', type: 'short', q: '리눅스에서 `eth0` 의 링크 속도·듀플렉스·링크 상태를 확인하는 명령어는?', answer: 'ethtool eth0', accept: ['sudo ethtool eth0', 'ethtool eth0 | grep -i speed', 'cat /sys/class/net/eth0/speed'],
    explain: '`Speed: 1000Mb/s`, `Duplex: Full`, `Link detected: yes`. 물리 서버에서 자동 협상 실패로 100Mb/Half 가 되면 성능이 급락합니다. `ethtool -S eth0` 로 NIC 통계(드롭, 오류).',
    example: '온프레미스 서버 처리량이 이상하게 낮을 때 듀플렉스 불일치를 먼저 확인.' },

  /* ---------------- EXTREME ---------------- */
  { diff: 'extreme', type: 'mcq', q: '`ss -tlnp` 의 특정 리스닝 소켓에서 `Recv-Q` 가 `Send-Q`(backlog 한도)에 근접해 있다. 의미와 조치는?', options: ['수신 버퍼 부족 → tcp_rmem 증가', 'accept 큐가 거의 찬 상태 — 앱이 accept() 를 충분히 빨리 호출하지 못함. 워커 수/이벤트 루프 점검, listen backlog 와 somaxconn 상향', '송신 지연', '정상 상태'], answer: 1,
    explain: '리스닝 소켓의 Recv-Q 는 accept 대기 연결 수, Send-Q 는 backlog 한도입니다. 꽉 차면 SYN 이 드롭되어 클라이언트가 타임아웃/재시도합니다. 앱이 블로킹되거나 GC 정지 중일 때 급증.',
    example: 'Node 단일 스레드가 CPU 작업에 묶여 accept 를 못 해 backlog 가 차는 현상 → 워커 프로세스 분리(cluster).' },
  { diff: 'extreme', type: 'short', q: 'iptables/nftables 규칙 없이, 리눅스 커널이 특정 패킷을 어디서 드롭하는지 추적하기 위해 사용하는 eBPF/커널 도구는? (BCC 도구 이름)', answer: 'dropwatch', accept: ['tcpdrop', 'perf trace --event skb:kfree_skb', 'nstat', 'tcpdrop / dropwatch', 'bpftrace'],
    explain: '`dropwatch` 나 BCC 의 `tcpdrop` 은 커널의 `kfree_skb` 트레이스포인트에서 드롭 위치(함수·스택)를 보여줍니다. `nstat -az` 의 `TcpExtListenDrops`, `TcpExtTCPBacklogDrop` 카운터도 원인 분류에 유용.',
    example: '"패킷이 서버까지 오는데(tcpdump 에 보임) 앱이 못 받는다" 유형의 문제에서 conntrack/backlog/rp_filter 중 어느 단계에서 드롭되는지 특정.' },
  { diff: 'extreme', type: 'mcq', q: '비대칭 라우팅(요청은 eth0 로 들어오고 응답은 eth1 로 나감) 환경에서 리눅스 서버가 패킷을 조용히 버린다. 관련 커널 설정은?', options: ['net.ipv4.ip_forward', 'net.ipv4.conf.all.rp_filter (reverse path filtering) — strict 모드(1)에서 비대칭 경로 패킷 드롭. loose(2) 또는 정책 라우팅으로 해결', 'net.ipv4.tcp_syncookies', 'net.core.netdev_max_backlog'], answer: 1,
    explain: 'rp_filter=1 은 "이 소스로 응답할 때 같은 인터페이스로 나가는가" 를 검사합니다. 다중 NIC·VPN·클라우드 다중 ENI 에서 흔한 함정. `ip rule` + 인터페이스별 라우팅 테이블로 대칭 경로를 만드는 것이 정석.',
    example: 'EC2 에 보조 ENI 를 붙였는데 그 IP 로 오는 연결이 응답을 못 받는 문제 → rp_filter 와 정책 라우팅.' },
  { diff: 'extreme', type: 'short', q: '보조 인터페이스 `eth1` (IP 10.0.2.10, 게이트웨이 10.0.2.1) 로 들어온 트래픽이 같은 인터페이스로 응답하도록, 라우팅 테이블 100 을 만들고 소스 기반 규칙을 추가하는 두 명령어는? (테이블 기본 경로 + ip rule)', answer: 'ip route add default via 10.0.2.1 dev eth1 table 100 && ip rule add from 10.0.2.10 table 100', accept: ['ip route add default via 10.0.2.1 dev eth1 table 100; ip rule add from 10.0.2.10 table 100', 'ip route add default via 10.0.2.1 dev eth1 table 100 ; ip rule add from 10.0.2.10 table 100', 'ip rule add from 10.0.2.10 table 100 && ip route add default via 10.0.2.1 dev eth1 table 100', 'ip route add default via 10.0.2.1 table 100 && ip rule add from 10.0.2.10 table 100', 'ip route add default via 10.0.2.1 dev eth1 table 100 && ip rule add from 10.0.2.10/32 table 100', 'ip route add default via 10.0.2.1 dev eth1 table 100 && ip rule add from 10.0.2.10 lookup 100'],
    explain: '정책 라우팅: `ip rule` 이 소스 IP 에 따라 별도 테이블을 참조하게 합니다. `ip route show table 100`, `ip rule show` 로 확인. 영구화는 `/etc/iproute2/rt_tables` 와 네트워크 설정에 기록.',
    example: '한 서버에 두 ENI 로 각각 다른 서브넷의 서비스를 받는 구성, 또는 NLB 다중 타겟 IP 구성.' },
  { diff: 'extreme', type: 'ox', q: 'TLS 1.3 에서는 ClientHello 이후 1-RTT 만에 핸드셰이크가 완료되며, 0-RTT 재개(early data)는 재전송 공격(replay)에 취약하므로 멱등하지 않은 요청(POST)에는 쓰면 안 된다.', answer: true,
    explain: 'TLS 1.2 는 2-RTT. 1.3 의 0-RTT 는 이전 세션 키로 첫 요청을 즉시 보내지만 공격자가 그대로 재전송할 수 있어 서버는 `Early-Data: 1` 헤더를 보고 비멱등 요청을 425 Too Early 로 거부해야 합니다.',
    example: 'CDN 에서 0-RTT 를 켤 때 오리진 앱이 425 를 처리하거나 GET 에만 허용하도록 설정.' },
  { diff: 'extreme', type: 'mcq', q: 'Kubernetes/도커 노드에서 컨테이너 간 통신이 간헐적으로 실패하고 `dmesg` 에 "nf_conntrack: table full, dropping packet" 이 있다. 근본적 대응으로 가장 적절한 것은?', options: ['컨테이너 재시작', 'conntrack 테이블 상한(`nf_conntrack_max`) 상향과 established 타임아웃 단축, 그리고 대량 단기 연결의 원인(커넥션 풀 미사용, DNS 폭주) 제거', '노드 재부팅', 'MTU 조정'], answer: 1,
    explain: 'NAT/브리지 네트워킹은 모든 연결을 conntrack 으로 추적합니다. 기본 상한(65536 또는 RAM 기반)은 노드당 수만 연결에서 소진됩니다. kube-proxy 는 `--conntrack-max-per-core` 로 자동 조정하지만 사용자 정의 노드에서는 직접 설정.',
    example: 'DNS 조회를 매 요청마다 하는 앱이 UDP conntrack 항목을 폭발시켜 노드 전체 네트워킹이 흔들린 사례 → NodeLocal DNSCache + 앱 DNS 캐시.' }
);
