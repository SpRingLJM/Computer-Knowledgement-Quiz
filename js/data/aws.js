window.QUIZ_BANK = window.QUIZ_BANK || {};
window.QUIZ_BANK.aws = [
  /* ---------------- EASY ---------------- */
  { diff: 'easy', type: 'short', q: '현재 AWS CLI 자격 증명이 어떤 계정/사용자(ARN)인지 확인하는 명령어는?', answer: 'aws sts get-caller-identity',
    explain: 'Account ID, UserId, ARN 을 반환합니다. 어떤 프로파일/역할로 명령이 실행되는지 확인하는 첫 단계입니다.',
    example: '`AccessDenied` 가 나면 먼저 `aws sts get-caller-identity` 로 의도한 역할인지 확인합니다. `--profile prod` 로 프로파일별 확인.' },
  { diff: 'easy', type: 'short', q: '내 계정의 모든 S3 버킷을 나열하는 AWS CLI 명령어는?', answer: 'aws s3 ls', accept: ['aws s3api list-buckets'],
    explain: '`aws s3` 는 고수준 명령(ls/cp/sync/rm), `aws s3api` 는 API 1:1 저수준 명령입니다. `aws s3 ls s3://bucket/prefix/` 로 객체도 나열.',
    example: '`aws s3 ls s3://my-logs/2024/ --recursive --human-readable --summarize` 로 총 용량 확인.' },
  { diff: 'easy', type: 'short', q: '로컬 파일 `app.zip` 을 S3 버킷 `my-bucket` 의 `releases/` 경로에 업로드하는 명령어는?', answer: 'aws s3 cp app.zip s3://my-bucket/releases/', accept: ['aws s3 cp app.zip s3://my-bucket/releases/app.zip', 'aws s3 cp ./app.zip s3://my-bucket/releases/'],
    explain: '`aws s3 cp 원본 대상`. 대상이 `/` 로 끝나면 파일명이 유지됩니다. 디렉터리는 `--recursive`, 동기화는 `aws s3 sync`.',
    example: '정적 사이트 배포: `aws s3 sync ./dist s3://my-site --delete` 후 CloudFront 무효화.' },
  { diff: 'easy', type: 'mcq', q: 'AWS 에서 사용자·그룹·역할과 권한 정책을 관리하는 서비스는?', options: ['Cognito', 'IAM', 'KMS', 'Organizations'], answer: 1,
    explain: 'IAM (Identity and Access Management). Cognito 는 앱 최종 사용자 인증, KMS 는 암호화 키 관리, Organizations 는 다중 계정 관리.',
    example: 'CI 서버에는 IAM User 의 액세스 키 대신 IAM Role(OIDC 연동)을 써서 장기 자격증명 없이 배포합니다.' },
  { diff: 'easy', type: 'ox', q: 'S3 버킷 이름은 전 세계 모든 AWS 계정에 걸쳐 고유해야 한다.', answer: true,
    explain: 'S3 버킷 네임스페이스는 글로벌입니다. 다른 계정이 이미 사용 중인 이름은 만들 수 없습니다. 3~63자, 소문자·숫자·하이픈만 가능.',
    example: '`my-app-logs` 같은 이름은 이미 있을 확률이 높아 `my-app-logs-123456789012-ap-northeast-2` 처럼 계정 ID/리전을 붙입니다.' },
  { diff: 'easy', type: 'short', q: '실행 중인 EC2 인스턴스 목록을 조회하는 AWS CLI 명령어는?', answer: 'aws ec2 describe-instances', accept: ['aws ec2 describe-instances --filters Name=instance-state-name,Values=running'],
    explain: '`describe-*` 는 조회 명령의 공통 접두사입니다. `--filters`, `--query` (JMESPath), `--output table` 로 출력을 가공합니다.',
    example: '`aws ec2 describe-instances --query "Reservations[].Instances[].[InstanceId,State.Name,PrivateIpAddress]" --output table`.' },
  { diff: 'easy', type: 'mcq', q: 'EC2 인스턴스에 대한 가상 방화벽 역할을 하며 인바운드/아웃바운드 규칙을 정의하는 것은?', options: ['Route Table', 'Security Group', 'Internet Gateway', 'VPC Peering'], answer: 1,
    explain: 'Security Group 은 인스턴스 레벨의 stateful 방화벽입니다(응답 트래픽 자동 허용). 서브넷 레벨의 stateless 방화벽은 Network ACL.',
    example: '웹 서버 SG: 인바운드 443 from 0.0.0.0/0, 22 from 사무실 IP. DB SG: 5432 from 웹서버 SG(SG 참조).' },
  { diff: 'easy', type: 'ox', q: 'Security Group 은 기본적으로 모든 인바운드 트래픽을 거부하고, 모든 아웃바운드 트래픽을 허용한다.', answer: true,
    explain: '새 SG 는 인바운드 규칙 없음(모두 거부), 아웃바운드는 all allow 입니다. SG 는 허용 규칙만 있으며 명시적 거부는 NACL 로 합니다.',
    example: '아웃바운드를 제한하는 보안 강화 정책에서는 아웃바운드 기본 규칙을 지우고 필요한 443 등만 허용합니다.' },
  { diff: 'easy', type: 'short', q: 'AWS CLI 의 기본 자격 증명과 리전을 대화형으로 설정하는 명령어는?', answer: 'aws configure',
    explain: 'Access Key, Secret Key, 기본 리전, 출력 형식을 `~/.aws/credentials` 와 `~/.aws/config` 에 저장합니다. `--profile 이름` 으로 여러 계정 관리.',
    example: 'SSO 환경에서는 `aws configure sso` 로 설정하고 `aws sso login --profile prod` 로 로그인합니다.' },
  { diff: 'easy', type: 'mcq', q: 'AWS 의 관리형 관계형 데이터베이스 서비스는?', options: ['DynamoDB', 'RDS', 'ElastiCache', 'Redshift'], answer: 1,
    explain: 'RDS 는 MySQL/PostgreSQL/MariaDB/Oracle/SQL Server 및 Aurora 를 관리형으로 제공합니다. DynamoDB 는 NoSQL, ElastiCache 는 Redis/Memcached, Redshift 는 데이터 웨어하우스.',
    example: '운영 DB 는 RDS Multi-AZ 로 자동 장애 조치, 읽기 부하는 Read Replica 로 분산.' },
  { diff: 'easy', type: 'short', q: 'S3 버킷 `my-bucket` 의 객체 `logs/app.log` 를 로컬 현재 디렉터리로 다운로드하는 명령어는?', answer: 'aws s3 cp s3://my-bucket/logs/app.log .', accept: ['aws s3 cp s3://my-bucket/logs/app.log ./', 'aws s3 cp s3://my-bucket/logs/app.log app.log', 'aws s3 cp s3://my-bucket/logs/app.log ./app.log'],
    explain: 'cp 는 양방향입니다. `s3://` 접두사가 원본에 있으면 다운로드, 대상에 있으면 업로드, 양쪽이면 버킷 간 복사.',
    example: '장애 분석용으로 `aws s3 cp s3://alb-logs/2024/06/01/ ./logs --recursive` 로 ALB 액세스 로그를 내려받습니다.' },
  { diff: 'easy', type: 'ox', q: 'IAM 정책에서 명시적 `Deny` 는 어떤 `Allow` 보다 항상 우선한다.', answer: true,
    explain: 'IAM 평가 로직: 기본 거부 → 명시적 Allow 가 있으면 허용 → 명시적 Deny 가 하나라도 있으면 무조건 거부. SCP, 권한 경계, 리소스 정책 모두 이 규칙을 따릅니다.',
    example: 'Organizations SCP 로 `s3:DeleteBucket` 을 Deny 하면 계정 관리자(AdministratorAccess)도 버킷을 못 지웁니다.' },
  { diff: 'easy', type: 'mcq', q: 'AWS 에서 서버를 관리하지 않고 코드를 이벤트 기반으로 실행하는 서비스는?', options: ['EC2', 'ECS', 'Lambda', 'Batch'], answer: 2,
    explain: 'Lambda 는 요청·이벤트당 실행되며 실행 시간(ms)과 메모리에 따라 과금됩니다. 최대 15분 실행, 콜드 스타트 존재.',
    example: 'S3 에 이미지가 업로드되면 Lambda 가 트리거되어 썸네일을 생성해 다른 버킷에 저장하는 패턴.' },
  { diff: 'easy', type: 'short', q: 'CloudWatch 로그 그룹 `/aws/lambda/my-fn` 의 로그를 실시간으로 tail 하는 AWS CLI 명령어는?', answer: 'aws logs tail /aws/lambda/my-fn --follow', accept: ['aws logs tail /aws/lambda/my-fn -f', 'aws logs tail --follow /aws/lambda/my-fn'],
    explain: '`aws logs tail` 은 CLI v2 에서 추가된 고수준 명령입니다. `--since 1h`, `--filter-pattern ERROR` 로 필터링 가능.',
    example: 'Lambda 배포 후 `aws logs tail /aws/lambda/my-fn --follow --filter-pattern "ERROR"` 로 즉시 에러 감시.' },
  { diff: 'easy', type: 'mcq', q: 'VPC 안의 서브넷이 "퍼블릭 서브넷" 이 되기 위한 조건은?', options: ['퍼블릭 IP 를 가진 인스턴스가 하나 이상 있음', '라우트 테이블에 Internet Gateway 로 가는 경로(0.0.0.0/0 → igw)가 있음', 'NAT Gateway 가 연결됨', '서브넷 이름에 public 이 포함됨'], answer: 1,
    explain: '퍼블릭/프라이빗은 라우트 테이블로 결정됩니다. 프라이빗 서브넷은 IGW 경로가 없고, 아웃바운드 인터넷은 NAT Gateway 를 경유합니다.',
    example: '표준 구성: 퍼블릭 서브넷에 ALB/NAT GW, 프라이빗 서브넷에 앱 서버·DB. 앱 서버는 NAT 을 통해 패키지만 다운로드.' },
  { diff: 'easy', type: 'ox', q: 'EC2 인스턴스를 중지(stop)하면 EBS 루트 볼륨의 데이터는 유지되지만, 인스턴스 스토어(임시 스토리지) 데이터는 사라진다.', answer: true,
    explain: 'EBS 는 네트워크 연결 영구 스토리지, 인스턴스 스토어는 호스트 물리 디스크입니다. stop/terminate/하드웨어 장애 시 인스턴스 스토어는 소실됩니다.',
    example: '인스턴스 스토어는 캐시·임시 파일용으로만 쓰고, DB 데이터는 EBS(gp3/io2)에 둡니다.' },
  { diff: 'easy', type: 'short', q: 'AWS CLI 출력에서 JMESPath 로 필요한 필드만 추출하는 옵션은?', answer: '--query',
    explain: '`--query "Reservations[].Instances[].InstanceId"` 처럼 JSON 을 필터링합니다. `--output text|table|json` 과 조합. jq 없이도 셸 스크립트에서 값 추출이 가능합니다.',
    example: '`INSTANCE_ID=$(aws ec2 describe-instances --filters Name=tag:Name,Values=web --query "Reservations[0].Instances[0].InstanceId" --output text)`.' },
  { diff: 'easy', type: 'mcq', q: 'CloudFront 의 주요 역할은?', options: ['DNS 서비스', '콘텐츠를 엣지 로케이션에 캐싱하는 CDN', '로드 밸런서', '오브젝트 스토리지'], answer: 1,
    explain: 'CloudFront 는 전 세계 엣지에서 정적/동적 콘텐츠를 캐싱·전달합니다. S3, ALB, 커스텀 오리진과 연결하고 WAF, HTTPS, 서명 URL 을 지원합니다.',
    example: 'S3 정적 사이트 + CloudFront + ACM 인증서로 HTTPS 글로벌 배포. 배포 후 `aws cloudfront create-invalidation --distribution-id X --paths "/*"`.' },

  /* ---------------- NORMAL ---------------- */
  { diff: 'normal', type: 'short', q: '로컬 디렉터리 `./dist` 를 S3 버킷 `my-site` 에 동기화하되, 로컬에 없는 원격 파일은 삭제하는 명령어는?', answer: 'aws s3 sync ./dist s3://my-site --delete', accept: ['aws s3 sync dist s3://my-site --delete', 'aws s3 sync ./dist/ s3://my-site/ --delete', 'aws s3 sync ./dist s3://my-site/ --delete', 'aws s3 sync --delete ./dist s3://my-site'],
    explain: '`sync` 는 변경된 파일만 전송합니다. `--delete` 는 대상에만 있는 객체 제거. `--exclude/--include` 로 패턴 필터, `--cache-control` 로 헤더 지정.',
    example: 'SPA 배포: `aws s3 sync ./dist s3://my-site --delete --cache-control "max-age=31536000" --exclude index.html` 후 index.html 은 no-cache 로 따로 업로드.' },
  { diff: 'normal', type: 'mcq', q: 'EC2 인스턴스에서 실행되는 앱이 S3 에 접근해야 한다. 가장 권장되는 자격 증명 방식은?', options: ['액세스 키를 코드에 하드코딩', '액세스 키를 환경변수에 설정', 'IAM Role 을 인스턴스 프로파일로 연결', 'root 계정 키 사용'], answer: 2,
    explain: '인스턴스 프로파일로 연결된 IAM Role 은 임시 자격 증명을 메타데이터 서비스(IMDS)를 통해 자동 갱신 제공합니다. 키 유출·로테이션 걱정이 없습니다.',
    example: 'ECS 는 Task Role, Lambda 는 Execution Role, EKS 는 IRSA(IAM Roles for Service Accounts) 가 같은 원리입니다.' },
  { diff: 'normal', type: 'short', q: 'IAM 사용자 `deploy-bot` 에 연결된 관리형 정책 목록을 보는 AWS CLI 명령어는?', answer: 'aws iam list-attached-user-policies --user-name deploy-bot',
    explain: '관리형 정책은 `list-attached-user-policies`, 인라인 정책은 `list-user-policies`. 그룹 경유 권한은 `list-groups-for-user` 로 따로 추적해야 합니다.',
    example: '권한 감사 시 `aws iam get-account-authorization-details` 로 전체를 덤프해 분석하기도 합니다.' },
  { diff: 'normal', type: 'mcq', q: 'S3 객체에 대해 일정 시간만 유효한 다운로드 링크를 만들려면?', options: ['버킷을 퍼블릭으로 설정', 'Presigned URL 생성', 'CloudFront 연결', 'ACL 을 public-read 로 설정'], answer: 1,
    explain: 'Presigned URL 은 생성자의 자격 증명으로 서명되어 만료 시간까지만 유효합니다. 업로드용(PUT)도 가능합니다.',
    example: '`aws s3 presign s3://my-bucket/report.pdf --expires-in 3600` 으로 1시간 유효 링크 생성. 앱에서는 SDK 로 사용자별 다운로드 링크 발급.' },
  { diff: 'normal', type: 'short', q: 'S3 객체 `s3://my-bucket/report.pdf` 에 대해 1시간 동안 유효한 presigned URL 을 만드는 CLI 명령어는?', answer: 'aws s3 presign s3://my-bucket/report.pdf --expires-in 3600', accept: ['aws s3 presign --expires-in 3600 s3://my-bucket/report.pdf'],
    explain: '`--expires-in` 은 초 단위, 기본 3600. 최대 7일(IAM 사용자 자격 증명 기준). 임시 자격 증명(역할)으로 만들면 그 세션 만료와 함께 무효화됩니다.',
    example: '고객에게 인보이스 PDF 를 이메일로 보낼 때 24시간 유효 링크로 발급.' },
  { diff: 'normal', type: 'ox', q: 'NAT Gateway 는 프라이빗 서브넷의 인스턴스가 인터넷으로 나갈 수 있게 해주지만, 인터넷에서 그 인스턴스로 들어오는 연결은 허용하지 않는다.', answer: true,
    explain: 'NAT 은 아웃바운드 전용(소스 NAT). NAT Gateway 는 퍼블릭 서브넷에 위치하며 시간당 + 처리 GB 당 과금이 있어 비용에 주의합니다.',
    example: 'S3/DynamoDB 접근은 NAT 대신 VPC Gateway Endpoint 를 쓰면 무료이며 NAT 비용을 크게 줄일 수 있습니다.' },
  { diff: 'normal', type: 'short', q: 'EC2 인스턴스 `i-0abc123` 을 중지하는 AWS CLI 명령어는?', answer: 'aws ec2 stop-instances --instance-ids i-0abc123',
    explain: '`stop-instances` 중지, `start-instances` 시작, `terminate-instances` 삭제(복구 불가), `reboot-instances` 재부팅. `--instance-ids` 는 복수 지정 가능.',
    example: '야간 비용 절감 스크립트: 태그 `Schedule=office-hours` 인 인스턴스를 조회해 18시에 stop, 9시에 start.' },
  { diff: 'normal', type: 'mcq', q: 'RDS Multi-AZ 배포의 주 목적은?', options: ['읽기 성능 향상', '고가용성 — 장애 시 대기 인스턴스로 자동 장애 조치', '스토리지 비용 절감', '여러 리전에 데이터 복제'], answer: 1,
    explain: 'Multi-AZ 는 다른 가용 영역에 동기 복제 standby 를 두고 장애 시 DNS 를 자동 전환합니다. 읽기 분산은 Read Replica(비동기), 리전 간 복제는 Cross-Region Replica.',
    example: 'Multi-AZ 장애 조치는 1~2분 걸리며 앱은 DNS TTL 이 짧아야 하고 커넥션 재시도 로직이 필요합니다.' },
  { diff: 'normal', type: 'short', q: 'Systems Manager Session Manager 로 EC2 인스턴스 `i-0abc123` 에 셸 접속하는 명령어는?', answer: 'aws ssm start-session --target i-0abc123',
    explain: 'SSH 포트 개방과 키 관리 없이 IAM 권한으로 접속합니다. 인스턴스에 SSM Agent 와 `AmazonSSMManagedInstanceCore` 역할이 필요하며 세션 로그를 CloudWatch/S3 에 남길 수 있습니다.',
    example: '보안 정책상 22 번 포트를 완전히 닫고 Session Manager 만 허용하는 구성이 표준이 되고 있습니다. 로컬엔 session-manager-plugin 설치 필요.' },
  { diff: 'normal', type: 'mcq', q: 'S3 의 스토리지 클래스 중, 거의 접근하지 않지만 필요 시 수 시간 내 복원해도 되는 장기 보관 데이터에 가장 저렴한 것은?', options: ['S3 Standard', 'S3 Standard-IA', 'S3 Glacier Flexible Retrieval / Deep Archive', 'S3 One Zone-IA'], answer: 2,
    explain: 'Glacier 계열은 GB 당 비용이 가장 낮지만 복원에 분~시간이 걸리고 조기 삭제 수수료가 있습니다. 접근 패턴을 모르면 Intelligent-Tiering.',
    example: '규정상 7년 보관해야 하는 감사 로그: Lifecycle 규칙으로 30일 후 IA, 90일 후 Glacier, 7년 후 삭제.' },
  { diff: 'normal', type: 'short', q: 'Lambda 함수 `my-fn` 을 CLI 에서 동기 호출하고 응답을 `out.json` 에 저장하는 명령어는? (페이로드 없음)', answer: 'aws lambda invoke --function-name my-fn out.json', accept: ['aws lambda invoke --function-name my-fn --payload "{}" out.json', "aws lambda invoke --function-name my-fn --payload '{}' out.json"],
    explain: '마지막 인수는 출력 파일. `--payload` 로 JSON 입력(v2 는 `--cli-binary-format raw-in-base64-out` 필요할 수 있음), `--invocation-type Event` 로 비동기 호출.',
    example: '배포 검증: `aws lambda invoke --function-name my-fn --payload file://event.json out.json && cat out.json`.' },
  { diff: 'normal', type: 'ox', q: 'Auto Scaling Group 에 연결된 인스턴스가 헬스 체크에 실패하면 ASG 는 해당 인스턴스를 종료하고 새 인스턴스로 교체한다.', answer: true,
    explain: 'ASG 는 desired capacity 를 유지합니다. 기본은 EC2 상태 검사이며, ELB 헬스 체크를 활성화하면 앱 레벨 장애도 교체 대상이 됩니다.',
    example: 'ALB 타겟 그룹 헬스 체크 `/health` + ASG `HealthCheckType: ELB` 로 앱이 죽은 인스턴스를 자동 교체합니다. 교체가 반복되면 `HealthCheckGracePeriod` 부족을 의심.' },
  { diff: 'normal', type: 'short', q: 'Secrets Manager 에서 시크릿 `prod/db` 의 값을 조회하는 CLI 명령어는?', answer: 'aws secretsmanager get-secret-value --secret-id prod/db', accept: ['aws secretsmanager get-secret-value --secret-id prod/db --query SecretString --output text'],
    explain: '`SecretString` 필드에 JSON 문자열로 저장된 경우가 많습니다. `--query SecretString --output text | jq -r .password` 로 개별 키 추출.',
    example: '배포 스크립트에서 DB 비밀번호를 파일에 쓰지 않고 `export DB_PASS=$(aws secretsmanager get-secret-value --secret-id prod/db --query SecretString --output text | jq -r .password)`.' },
  { diff: 'normal', type: 'mcq', q: 'EC2 인스턴스 메타데이터(IAM 역할 자격증명, 인스턴스 ID 등)에 접근하는 링크로컬 주소는?', options: ['127.0.0.1', '169.254.169.254', '10.0.0.1', '172.17.0.1'], answer: 1,
    explain: 'IMDS(Instance Metadata Service). IMDSv2 는 토큰 기반(PUT 으로 토큰 발급 후 헤더에 포함)으로 SSRF 공격을 완화합니다. 새 인스턴스는 IMDSv2 필수 설정을 권장.',
    example: '`TOKEN=$(curl -X PUT "http://169.254.169.254/latest/api/token" -H "X-aws-ec2-metadata-token-ttl-seconds: 60")` 후 `curl -H "X-aws-ec2-metadata-token: $TOKEN" http://169.254.169.254/latest/meta-data/instance-id`.' },
  { diff: 'normal', type: 'short', q: 'ECS 클러스터 `prod` 의 서비스 `api` 를 강제로 새 배포(현재 태스크 정의로 재배포)하는 명령어는?', answer: 'aws ecs update-service --cluster prod --service api --force-new-deployment',
    explain: '`--force-new-deployment` 는 태스크 정의 변경 없이 태스크를 롤링 교체합니다. 같은 태그(`latest`)로 이미지를 덮어썼을 때 새 이미지를 받게 하는 데 씁니다.',
    example: '태스크 정의 변경 배포: `aws ecs update-service --cluster prod --service api --task-definition api:42`.' },
  { diff: 'normal', type: 'ox', q: 'VPC 의 CIDR 블록은 생성 후 변경할 수 없지만, 보조 CIDR 블록을 추가할 수는 있다.', answer: true,
    explain: '기본 CIDR 은 불변이므로 처음부터 충분히 크게(/16) 잡습니다. IP 부족 시 `associate-vpc-cidr-block` 으로 보조 CIDR 추가. 피어링 대상과 겹치지 않도록 IP 계획이 중요합니다.',
    example: 'EKS 는 Pod 마다 VPC IP 를 소비하므로 IP 고갈이 흔합니다 → 보조 CIDR(100.64.0.0/16) 추가 후 커스텀 네트워킹.' },
  { diff: 'normal', type: 'mcq', q: 'ALB(Application Load Balancer)와 NLB(Network Load Balancer)의 차이로 옳은 것은?', options: ['ALB 는 L4, NLB 는 L7', 'ALB 는 L7(HTTP 경로/호스트 라우팅), NLB 는 L4(TCP/UDP, 고정 IP, 초저지연)', '둘 다 L7', 'NLB 만 HTTPS 지원'], answer: 1,
    explain: 'ALB 는 HTTP 헤더 기반 라우팅, WebSocket, 인증 통합. NLB 는 수백만 rps, 고정 EIP, 클라이언트 IP 보존, TLS 패스스루.',
    example: 'HTTP API 는 ALB, 게임 서버·MQTT·DB 프록시처럼 TCP 프로토콜이거나 고정 IP 화이트리스트가 필요하면 NLB.' },
  { diff: 'normal', type: 'short', q: 'CloudFormation 스택 `my-stack` 을 템플릿 `template.yaml` 로 생성 또는 업데이트(있으면 갱신)하는 CLI 명령어는?', answer: 'aws cloudformation deploy --stack-name my-stack --template-file template.yaml', accept: ['aws cloudformation deploy --template-file template.yaml --stack-name my-stack', 'aws cloudformation deploy --stack-name my-stack --template-file template.yaml --capabilities CAPABILITY_IAM', 'aws cloudformation deploy --stack-name my-stack --template-file template.yaml --capabilities CAPABILITY_NAMED_IAM'],
    explain: '`deploy` 는 change set 생성 + 실행을 합친 멱등 명령. IAM 리소스가 있으면 `--capabilities CAPABILITY_IAM`, 파라미터는 `--parameter-overrides Key=Value`.',
    example: '`create-stack`/`update-stack` 은 없음/있음에 따라 실패하므로 CI 에서는 `deploy` 를 씁니다. `--no-execute-changeset` 으로 검토만 가능.' },

  /* ---------------- HARD ---------------- */
  { diff: 'hard', type: 'short', q: 'IAM 역할 `arn:aws:iam::123456789012:role/Admin` 을 assume 하여 임시 자격 증명을 얻는 AWS CLI 명령어는? (세션 이름: cli)', answer: 'aws sts assume-role --role-arn arn:aws:iam::123456789012:role/Admin --role-session-name cli', accept: ['aws sts assume-role --role-session-name cli --role-arn arn:aws:iam::123456789012:role/Admin'],
    explain: '반환된 AccessKeyId/SecretAccessKey/SessionToken 을 환경변수로 export 해서 사용합니다. `~/.aws/config` 에 `role_arn` + `source_profile` 을 설정하면 CLI 가 자동으로 assume 합니다.',
    example: '크로스 계정 배포: CI 계정의 역할이 prod 계정의 `DeployRole` 을 assume (신뢰 정책에 CI 역할 ARN 명시).' },
  { diff: 'hard', type: 'mcq', q: 'S3 버킷에 `s3:GetObject` 를 허용하는 IAM 정책이 있는데도 EC2 앱에서 AccessDenied 가 난다. 다음 중 원인이 **아닌** 것은?', options: ['버킷 정책에 명시적 Deny 가 있음', 'SCP 가 S3 접근을 제한함', '객체가 KMS 로 암호화되어 있고 `kms:Decrypt` 권한이 없음', 'Security Group 아웃바운드가 열려 있음'], answer: 3,
    explain: 'S3 접근은 IAM 정책 ∩ 버킷 정책 ∩ SCP ∩ 권한 경계 ∩ (SSE-KMS 면) KMS 키 정책 모두 통과해야 합니다. SG 아웃바운드 허용은 문제가 아닙니다(막혀 있으면 타임아웃).',
    example: '다른 계정이 업로드한 객체는 소유권 문제로 AccessDenied 가 나기도 합니다 → Bucket owner enforced 설정.' },
  { diff: 'hard', type: 'short', q: '태그 `Env=prod` 인 EC2 인스턴스의 InstanceId 만 텍스트로 출력하는 CLI 명령어는?', answer: 'aws ec2 describe-instances --filters Name=tag:Env,Values=prod --query "Reservations[].Instances[].InstanceId" --output text', accept: ["aws ec2 describe-instances --filters Name=tag:Env,Values=prod --query 'Reservations[].Instances[].InstanceId' --output text", 'aws ec2 describe-instances --filters "Name=tag:Env,Values=prod" --query "Reservations[].Instances[].InstanceId" --output text', 'aws ec2 describe-instances --filters Name=tag:Env,Values=prod --query Reservations[].Instances[].InstanceId --output text', 'aws ec2 describe-instances --filters Name=tag:Env,Values=prod --query "Reservations[*].Instances[*].InstanceId" --output text', "aws ec2 describe-instances --filters Name=tag:Env,Values=prod --query 'Reservations[*].Instances[*].InstanceId' --output text"],
    explain: '`--filters Name=tag:KEY,Values=VAL` 로 태그 필터. `[]` 는 평탄화. `--output text` 는 탭/공백 구분 문자열이라 셸 루프에 바로 쓸 수 있습니다.',
    example: '`for id in $(aws ec2 describe-instances ... --output text); do aws ec2 create-tags --resources $id --tags Key=Backup,Value=daily; done`.' },
  { diff: 'hard', type: 'mcq', q: 'Lambda 함수가 VPC 안의 RDS 에 접근하도록 VPC 를 연결했더니, 이전에 되던 외부 API 호출이 타임아웃 난다. 원인은?', options: ['Lambda 는 VPC 에서 인터넷을 절대 못 씀', 'VPC Lambda 는 퍼블릭 IP 가 없어 프라이빗 서브넷 + NAT Gateway 가 필요함', 'Security Group 인바운드가 막힘', 'Lambda 메모리 부족'], answer: 1,
    explain: 'VPC 연결 Lambda 의 ENI 는 퍼블릭 IP 를 받지 않습니다. 퍼블릭 서브넷에 넣어도 인터넷이 안 되고, 프라이빗 서브넷 + NAT GW(또는 VPC Endpoint) 가 필요합니다.',
    example: 'AWS API(S3, Secrets Manager) 호출은 NAT 없이 VPC Endpoint 로 해결해 비용을 절감합니다.' },
  { diff: 'hard', type: 'short', q: 'S3 버킷 `my-bucket` 의 버전 관리를 활성화하는 CLI 명령어는?', answer: 'aws s3api put-bucket-versioning --bucket my-bucket --versioning-configuration Status=Enabled',
    explain: '버전 관리는 실수 삭제·덮어쓰기 복구를 가능하게 하며, 한 번 켜면 끌 수 없고 Suspended 만 가능합니다. Lifecycle 로 이전 버전 만료를 설정해 비용을 관리합니다.',
    example: 'Terraform state 버킷은 버전 관리 + MFA Delete 가 표준. 삭제 마커를 지우면 객체가 복구됩니다.' },
  { diff: 'hard', type: 'ox', q: 'S3 버킷 정책에서 `"Principal": "*"` 와 `"Effect": "Allow"` 를 쓰면 Block Public Access 설정과 무관하게 버킷이 공개된다.', answer: false,
    explain: 'Block Public Access(계정/버킷 레벨)가 켜져 있으면 퍼블릭 정책 자체가 거부되거나 무시됩니다. 2023년부터 새 버킷은 기본 활성화입니다.',
    example: '정적 웹사이트 호스팅도 요즘은 버킷을 공개하지 않고 CloudFront OAC 로만 접근하게 하는 것이 권장됩니다.' },
  { diff: 'hard', type: 'mcq', q: 'EKS 에서 Pod 가 특정 IAM 권한만 갖게 하는 권장 방식은?', options: ['노드 IAM Role 에 모든 권한 부여', 'Pod 에 액세스 키를 Secret 으로 주입', 'IRSA (IAM Roles for Service Accounts) 또는 EKS Pod Identity', 'kube2iam 만 사용'], answer: 2,
    explain: 'IRSA 는 OIDC 공급자를 통해 ServiceAccount 토큰을 IAM 역할로 교환합니다. 노드 역할 공유는 최소 권한 원칙 위반. Pod Identity 는 2023년 등장한 더 간단한 대안.',
    example: 'ServiceAccount 에 `eks.amazonaws.com/role-arn` 어노테이션을 달면 SDK 가 자동으로 해당 역할 자격증명을 사용합니다.' },
  { diff: 'hard', type: 'short', q: 'CloudWatch Logs Insights 에서 로그 그룹의 `ERROR` 포함 메시지를 최근순으로 20개 가져오는 쿼리는?', answer: 'fields @timestamp, @message | filter @message like /ERROR/ | sort @timestamp desc | limit 20', accept: ['fields @timestamp, @message | filter @message like "ERROR" | sort @timestamp desc | limit 20', 'fields @timestamp,@message | filter @message like /ERROR/ | sort @timestamp desc | limit 20', 'filter @message like /ERROR/ | sort @timestamp desc | limit 20'],
    explain: 'Insights 쿼리 언어: `fields`, `filter`, `stats`, `sort`, `limit`, `parse`. 정규식은 `/.../`. CLI 로는 `start-query` 후 `get-query-results`.',
    example: '에러율 집계: `filter @message like /ERROR/ | stats count() by bin(5m)` 로 5분 단위 에러 추이.' },
  { diff: 'hard', type: 'mcq', q: 'DynamoDB 테이블에서 특정 파티션 키의 항목만 효율적으로 조회하는 연산은?', options: ['Scan', 'Query', 'GetItem 을 반복', 'BatchGetItem'], answer: 1,
    explain: '`Query` 는 파티션 키(+ 정렬 키 조건)로 인덱스를 타므로 효율적. `Scan` 은 전체 테이블을 읽어 비용·시간이 큽니다. 다른 속성으로 조회하려면 GSI 를 만듭니다.',
    example: '`aws dynamodb query --table-name Orders --key-condition-expression "userId = :u" --expression-attribute-values \'{":u":{"S":"u123"}}\'`.' },
  { diff: 'hard', type: 'short', q: 'ECR 레지스트리에 Docker 로그인하는 명령어는? (리전: ap-northeast-2, 계정: 123456789012)', answer: 'aws ecr get-login-password --region ap-northeast-2 | docker login --username AWS --password-stdin 123456789012.dkr.ecr.ap-northeast-2.amazonaws.com',
    explain: '`get-login-password` 가 12시간 유효 토큰을 출력하고 이를 `docker login` 에 파이프합니다. 사용자명은 항상 `AWS`.',
    example: 'CI 파이프라인 첫 단계로 이 명령을 실행한 뒤 `docker build/tag/push` 를 수행합니다. ECR 은 `ImagePullBackOff` 원인이 토큰 만료인 경우가 많습니다.' },
  { diff: 'hard', type: 'ox', q: 'Route 53 의 Alias 레코드는 CNAME 과 달리 Zone Apex(예: example.com)에 사용할 수 있고, ALB/CloudFront/S3 등을 가리키는 조회는 무료다.', answer: true,
    explain: 'DNS 규격상 apex 에는 CNAME 을 둘 수 없습니다. Alias 는 AWS 리소스의 IP 를 Route 53 이 내부적으로 해석해 A/AAAA 로 응답합니다.',
    example: '`example.com` → ALB: Alias A 레코드. `www.example.com` 은 CNAME 이든 Alias 든 가능하지만 Alias 가 무료라 유리.' },
  { diff: 'hard', type: 'mcq', q: 'EBS gp3 볼륨이 gp2 보다 유리한 점은?', options: ['항상 더 빠름', '용량과 무관하게 3000 IOPS/125MB/s 기본 제공, IOPS/처리량을 독립적으로 추가 구매 가능하며 GB 당 약 20% 저렴', 'gp2 만 스냅샷 지원', 'gp3 는 부팅 볼륨으로 불가'], answer: 1,
    explain: 'gp2 는 용량에 IOPS 가 묶여(3 IOPS/GB) 성능을 위해 용량을 늘려야 했습니다. gp3 는 분리되어 작은 볼륨도 고성능 가능.',
    example: '`aws ec2 modify-volume --volume-id vol-xxx --volume-type gp3` 로 무중단 변환. 대부분의 gp2 워크로드는 즉시 비용 절감.' },
  { diff: 'hard', type: 'short', q: 'CloudFormation 스택 `my-stack` 이 UPDATE_ROLLBACK_FAILED 상태에서 벗어나도록 롤백을 이어서 진행하는 CLI 명령어는?', answer: 'aws cloudformation continue-update-rollback --stack-name my-stack', accept: ['aws cloudformation continue-update-rollback --stack-name my-stack --resources-to-skip'],
    explain: '롤백조차 실패하면 스택이 잠깁니다. 수동으로 리소스 문제를 고친 뒤 `continue-update-rollback`, 문제 리소스를 건너뛰려면 `--resources-to-skip LogicalId`.',
    example: '수동으로 삭제한 리소스 때문에 롤백이 실패한 경우 해당 리소스를 skip 하고 이후 템플릿에서 정리합니다.' },
  { diff: 'hard', type: 'mcq', q: 'SQS 표준 큐와 FIFO 큐의 차이로 옳은 것은?', options: ['표준 큐는 정확히 한 번 전달을 보장', 'FIFO 는 순서 보장 + 정확히 한 번 처리, 표준은 최소 한 번 전달 + 최선의 순서, 대신 처리량 무제한', 'FIFO 가 더 저렴', '표준 큐만 DLQ 지원'], answer: 1,
    explain: '표준 큐는 중복 가능하므로 소비자가 멱등해야 합니다. FIFO 는 MessageGroupId 로 순서 보장, 초당 300(배치 3000) 건 제한(고처리량 모드로 확장 가능).',
    example: '주문 상태 변경 이벤트는 순서가 중요하므로 FIFO + MessageGroupId=orderId. 로그 수집은 표준 큐.' },

  /* ---------------- EXTREME ---------------- */
  { diff: 'extreme', type: 'mcq', q: '크로스 계정 S3 접근을 위해 계정 B 의 버킷 정책에서 계정 A 의 역할을 허용했는데, 계정 A 의 역할이 여전히 AccessDenied 를 받는다. 역할의 IAM 정책도 허용 상태다. 가장 가능성 높은 남은 원인은?', options: ['버킷 정책은 크로스 계정에 적용되지 않음', '객체가 SSE-KMS 로 암호화되어 있고 KMS 키 정책이 계정 A 의 역할을 허용하지 않음', '역할 세션 이름이 다름', 'S3 는 리전 간 접근 불가'], answer: 1,
    explain: '크로스 계정 KMS 는 키 정책에서 외부 계정(또는 역할)에 `kms:Decrypt` 를 명시적으로 허용해야 하고, 사용하는 쪽 IAM 정책에도 해당 키 ARN 에 대한 권한이 있어야 합니다. 두 조건 모두 필요합니다.',
    example: 'CloudTrail 에서 `kms:Decrypt` 의 `AccessDenied` 이벤트를 찾으면 S3 가 아닌 KMS 가 원인임을 확정할 수 있습니다.' },
  { diff: 'extreme', type: 'short', q: 'CloudTrail 이벤트 히스토리에서 사용자 `deploy-bot` 의 최근 이벤트를 조회하는 AWS CLI 명령어는?', answer: 'aws cloudtrail lookup-events --lookup-attributes AttributeKey=Username,AttributeValue=deploy-bot', accept: ['aws cloudtrail lookup-events --lookup-attributes AttributeKey=Username,AttributeValue=deploy-bot --max-results 50', 'aws cloudtrail lookup-events --lookup-attributes "AttributeKey=Username,AttributeValue=deploy-bot"'],
    explain: 'lookup-events 는 최근 90일 관리 이벤트를 조회합니다. `AttributeKey` 로 EventName, ResourceName, EventSource 등도 가능. 장기 분석은 S3 에 저장된 트레일을 Athena 로 쿼리합니다.',
    example: '"누가 이 Security Group 규칙을 열었나": `--lookup-attributes AttributeKey=ResourceName,AttributeValue=sg-0abc123`.' },
  { diff: 'extreme', type: 'mcq', q: 'ALB 뒤의 서비스에서 간헐적으로 502 Bad Gateway 가 발생한다. 타겟 헬스 체크는 정상이다. 가장 흔한 원인은?', options: ['ALB 용량 부족', '타겟(앱 서버)의 keep-alive idle timeout 이 ALB 의 idle timeout(기본 60s)보다 짧아 ALB 가 재사용하려는 연결을 앱이 먼저 끊음', '인증서 만료', 'Security Group 이 헬스 체크 포트를 막음'], answer: 1,
    explain: '앱의 keep-alive timeout 은 ALB idle timeout 보다 길어야 합니다(예: nginx `keepalive_timeout 65s` 이상, Node 는 `server.keepAliveTimeout = 65000`). 또한 응답 헤더가 비정상이거나 타겟이 요청 중 종료될 때도 502 가 납니다.',
    example: 'Node.js 기본 keepAliveTimeout 5s 때문에 ALB 뒤에서 간헐 502 가 나는 것은 매우 유명한 사례입니다.' },
  { diff: 'extreme', type: 'short', q: 'IAM Access Analyzer 를 사용하지 않고, 특정 역할 `arn:aws:iam::123456789012:role/App` 이 `s3:GetObject` 를 `arn:aws:s3:::my-bucket/*` 에 수행할 수 있는지 정책 시뮬레이션으로 검사하는 CLI 명령어는?', answer: 'aws iam simulate-principal-policy --policy-source-arn arn:aws:iam::123456789012:role/App --action-names s3:GetObject --resource-arns arn:aws:s3:::my-bucket/*', accept: ['aws iam simulate-principal-policy --policy-source-arn arn:aws:iam::123456789012:role/App --action-names s3:GetObject --resource-arns "arn:aws:s3:::my-bucket/*"'],
    explain: '`simulate-principal-policy` 는 주체에 연결된 IAM 정책(+권한 경계)을 평가해 `EvalDecision` 을 반환합니다. 리소스 정책(버킷 정책)은 `--resource-policy` 로 추가 전달해야 반영됩니다.',
    example: '권한 축소 작업 전 회귀 검사 스크립트로 여러 액션을 한 번에 시뮬레이션해 "allowed/implicitDeny/explicitDeny" 를 확인합니다.' },
  { diff: 'extreme', type: 'ox', q: 'VPC Peering 은 전이적(transitive)이지 않다. 즉 A-B, B-C 가 피어링되어 있어도 A 에서 C 로는 통신할 수 없다.', answer: true,
    explain: '피어링은 1:1 이며 라우팅이 전이되지 않습니다. 다수 VPC 를 연결하려면 Transit Gateway 를 허브로 사용합니다. 또 CIDR 이 겹치면 피어링 자체가 불가합니다.',
    example: '공유 서비스 VPC(모니터링, CI)를 여러 계정 VPC 와 연결할 때 피어링 n² 문제를 Transit Gateway 로 해결합니다.' },
  { diff: 'extreme', type: 'mcq', q: 'Lambda 콜드 스타트를 줄이기 위한 방법으로 효과가 **없는** 것은?', options: ['Provisioned Concurrency 설정', '배포 패키지 크기와 초기화 코드(핸들러 밖) 최소화', 'Lambda SnapStart(Java) 사용', '함수 타임아웃을 늘림'], answer: 3,
    explain: '타임아웃은 실행 최대 시간일 뿐 초기화와 무관합니다. 콜드 스타트는 런타임 부트 + 패키지 로드 + 핸들러 밖 초기화 시간이며, VPC 연결(ENI)은 현재는 크게 개선되었습니다.',
    example: 'DB 커넥션·SDK 클라이언트는 핸들러 밖에서 한 번만 생성해 웜 컨테이너에서 재사용하고, 트래픽 패턴이 예측되면 Provisioned Concurrency 를 스케줄링합니다.' },
];
