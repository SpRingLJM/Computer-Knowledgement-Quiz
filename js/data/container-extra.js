window.QUIZ_BANK.container.push(
  /* ---------------- EASY ---------------- */
  { diff: 'easy', type: 'short', q: '실행 중인 컨테이너 `web` 을 정상 종료(SIGTERM 후 대기)하는 명령어는?', answer: 'docker stop web', accept: ['docker container stop web'],
    explain: '`stop` 은 SIGTERM 을 보내고 기본 10초 후 SIGKILL. `-t 30` 으로 대기 시간 조정. `docker kill` 은 즉시 SIGKILL.',
    example: '배포 스크립트: `docker stop web && docker rm web && docker run ... --name web`.' },
  { diff: 'easy', type: 'short', q: 'Docker Hub 에서 `redis:7` 이미지를 내려받는 명령어는?', answer: 'docker pull redis:7', accept: ['docker image pull redis:7'],
    explain: '태그를 생략하면 `latest`. `docker run` 은 로컬에 없으면 자동으로 pull 하지만, 미리 받아두면 배포 시간이 예측 가능합니다.',
    example: 'CI 캐시 워밍이나 오프라인 배포 전 `docker pull` 로 이미지를 준비합니다.' },
  { diff: 'easy', type: 'short', q: '로컬 이미지 `myapp:1.0` 에 `registry.example.com/team/myapp:1.0` 이라는 새 이름(태그)을 붙이는 명령어는?', answer: 'docker tag myapp:1.0 registry.example.com/team/myapp:1.0', accept: ['docker image tag myapp:1.0 registry.example.com/team/myapp:1.0'],
    explain: '태그는 같은 이미지 ID 를 가리키는 별칭입니다. 프라이빗 레지스트리에 push 하려면 레지스트리 주소가 포함된 이름이 필요합니다.',
    example: '`docker tag myapp:1.0 123456789012.dkr.ecr.ap-northeast-2.amazonaws.com/myapp:1.0 && docker push ...`.' },
  { diff: 'easy', type: 'short', q: '이미지 `registry.example.com/team/myapp:1.0` 을 레지스트리에 업로드하는 명령어는?', answer: 'docker push registry.example.com/team/myapp:1.0', accept: ['docker image push registry.example.com/team/myapp:1.0'],
    explain: '`docker login` 이 먼저 필요합니다. 레이어 단위로 전송되므로 베이스 레이어가 이미 있으면 빠릅니다.',
    example: 'CI: build → tag(git sha) → push → `kubectl set image` 순서.' },
  { diff: 'easy', type: 'ox', q: 'Dockerfile 의 각 명령(RUN, COPY 등)은 새 레이어를 만들고, 변경되지 않은 레이어는 다음 빌드에서 캐시로 재사용된다.', answer: true,
    explain: '레이어 캐시는 위에서부터 순서대로 검사되며, 한 레이어가 바뀌면 그 아래는 전부 다시 빌드됩니다. 자주 바뀌는 소스 COPY 를 뒤로 보내야 합니다.',
    example: '`COPY package.json .` → `RUN npm ci` → `COPY . .` 순서로 두면 코드만 바꿨을 때 npm 설치가 캐시됩니다.' },
  { diff: 'easy', type: 'short', q: 'Kubernetes 클러스터의 노드 목록과 상태를 보는 명령어는?', answer: 'kubectl get nodes', accept: ['kubectl get node', 'kubectl get no', 'kubectl get nodes -o wide'],
    explain: 'STATUS 가 `Ready` 인지, `-o wide` 로 IP·OS·컨테이너 런타임 버전을 봅니다. `kubectl describe node NAME` 으로 리소스 할당량과 조건 확인.',
    example: '노드 장애 의심 시 `kubectl get nodes` 에서 `NotReady` 노드를 찾고 `describe` 로 원인(MemoryPressure, DiskPressure)을 봅니다.' },
  { diff: 'easy', type: 'short', q: '현재 네임스페이스의 Service 목록을 보는 kubectl 명령어는?', answer: 'kubectl get svc', accept: ['kubectl get services', 'kubectl get service'],
    explain: 'TYPE(ClusterIP/NodePort/LoadBalancer), CLUSTER-IP, EXTERNAL-IP, PORT(S) 를 보여줍니다. `EXTERNAL-IP` 가 `<pending>` 이면 클라우드 LB 생성 대기 중.',
    example: 'Ingress 가 502 를 낼 때 `kubectl get svc` 로 대상 Service 가 존재하고 포트가 맞는지 확인합니다.' },
  { diff: 'easy', type: 'short', q: 'Pod `api-7d9f` 를 삭제하는 kubectl 명령어는?', answer: 'kubectl delete pod api-7d9f', accept: ['kubectl delete po api-7d9f', 'kubectl delete pods api-7d9f', 'kubectl delete pod/api-7d9f'],
    explain: 'Deployment 가 관리하는 Pod 는 삭제되면 새로 생성됩니다(사실상 재시작). `--grace-period=0 --force` 는 강제 삭제.',
    example: '설정 변경 후 Pod 하나만 재시작해 보고 싶을 때, 또는 이상 상태 Pod 를 교체할 때.' },
  { diff: 'easy', type: 'mcq', q: 'Dockerfile 에서 베이스 이미지를 지정하는 명령어는?', options: ['BASE', 'FROM', 'IMAGE', 'USE'], answer: 1,
    explain: '`FROM image:tag` 는 Dockerfile 의 첫 명령(ARG 제외)이어야 합니다. `FROM scratch` 는 빈 이미지, `AS name` 으로 멀티스테이지 이름을 붙입니다.',
    example: '`FROM node:20-alpine AS build` … `FROM nginx:1.25-alpine` … `COPY --from=build /app/dist /usr/share/nginx/html`.' },
  { diff: 'easy', type: 'mcq', q: '`docker run -it ubuntu bash` 에서 `-it` 의 의미는?', options: ['이미지 태그 지정', '대화형(stdin 유지) + TTY 할당으로 셸을 직접 사용', '즉시 종료', '이미지 pull 강제'], answer: 1,
    explain: '`-i` interactive, `-t` tty. 둘 다 없으면 bash 가 입력을 못 받아 즉시 종료됩니다. `--rm` 을 붙이면 나갈 때 컨테이너 자동 삭제.',
    example: '이미지 안을 둘러보기: `docker run --rm -it myapp:1.0 sh`.' },
  { diff: 'easy', type: 'short', q: 'Docker 볼륨 목록을 보는 명령어는?', answer: 'docker volume ls', accept: ['docker volume list'],
    explain: 'named volume 은 `/var/lib/docker/volumes/` 에 저장됩니다. `docker volume inspect NAME` 으로 경로, `docker volume rm` / `prune` 으로 삭제.',
    example: '컨테이너를 지워도 DB 데이터가 남아 있는 이유는 volume 이 별도로 존재하기 때문입니다. 완전 초기화는 `docker compose down -v`.' },
  { diff: 'easy', type: 'ox', q: '`kubectl get deploy` 는 `kubectl get deployments` 의 축약이며, `po`, `svc`, `ns`, `cm` 같은 짧은 이름도 사용할 수 있다.', answer: true,
    explain: '`kubectl api-resources` 로 전체 리소스와 SHORTNAMES 확인. `alias k=kubectl` 과 결합하면 `k get po -A` 처럼 짧아집니다.',
    example: '`k get deploy,svc,ing -n prod` 로 여러 리소스를 한 번에 조회.' },
  { diff: 'easy', type: 'short', q: '`dev` 네임스페이스를 생성하는 kubectl 명령어는?', answer: 'kubectl create namespace dev', accept: ['kubectl create ns dev'],
    explain: '네임스페이스는 리소스를 논리적으로 격리합니다(이름 충돌 방지, RBAC/쿼터 단위). 삭제 시 안의 모든 리소스가 함께 삭제됩니다.',
    example: '환경별 `dev`/`staging`/`prod`, 팀별 `team-a` 네임스페이스에 ResourceQuota 와 NetworkPolicy 를 적용.' },
  { diff: 'easy', type: 'mcq', q: '각 노드에서 실행되며 Pod 의 컨테이너를 실제로 시작·감시하는 Kubernetes 컴포넌트는?', options: ['kube-apiserver', 'kubelet', 'kube-scheduler', 'etcd'], answer: 1,
    explain: 'kubelet 은 API 서버에서 할당된 PodSpec 을 받아 컨테이너 런타임(containerd)에 지시하고 상태·probe 를 보고합니다. kube-proxy 는 Service 네트워킹 담당.',
    example: '노드가 NotReady 면 `systemctl status kubelet` 과 `journalctl -u kubelet` 을 노드에서 확인합니다.' },

  /* ---------------- NORMAL ---------------- */
  { diff: 'normal', type: 'short', q: '실행 중인 모든 컨테이너의 CPU·메모리·네트워크 사용량을 실시간으로 보는 명령어는?', answer: 'docker stats', accept: ['docker container stats', 'docker stats --no-stream'],
    explain: 'MEM USAGE / LIMIT 로 메모리 제한 대비 사용량, `--no-stream` 으로 한 번만 출력. 컨테이너 안의 `top`/`free` 는 호스트 값을 보여주므로 여기서 봐야 정확합니다.',
    example: '어떤 컨테이너가 호스트 메모리를 잠식하는지 `docker stats --format "{{.Name}} {{.MemUsage}}"` 로 확인.' },
  { diff: 'normal', type: 'short', q: '컨테이너 `web` 안의 `/etc/nginx/nginx.conf` 를 호스트 현재 디렉터리로 복사하는 명령어는?', answer: 'docker cp web:/etc/nginx/nginx.conf .', accept: ['docker cp web:/etc/nginx/nginx.conf ./', 'docker cp web:/etc/nginx/nginx.conf ./nginx.conf', 'docker cp web:/etc/nginx/nginx.conf nginx.conf'],
    explain: '`docker cp 컨테이너:경로 호스트경로` 또는 반대. 중지된 컨테이너에서도 동작합니다. Kubernetes 는 `kubectl cp pod:/path ./local`.',
    example: '이미지 안의 기본 설정 파일을 꺼내 수정한 뒤 볼륨으로 마운트하는 흐름.' },
  { diff: 'normal', type: 'short', q: 'Docker 네트워크 목록을 보는 명령어는?', answer: 'docker network ls', accept: ['docker network list'],
    explain: '기본으로 `bridge`, `host`, `none` 이 있습니다. `docker network inspect NAME` 으로 연결된 컨테이너와 서브넷 확인. Compose 는 프로젝트별 네트워크를 자동 생성.',
    example: '두 Compose 프로젝트의 컨테이너가 서로 통신하려면 외부 네트워크를 만들어 양쪽에 `external: true` 로 연결합니다.' },
  { diff: 'normal', type: 'mcq', q: 'Dockerfile 의 `WORKDIR /app` 의 역할은?', options: ['호스트 디렉터리를 마운트', '이후 RUN/COPY/CMD 의 작업 디렉터리를 설정 (없으면 생성)', '이미지 저장 위치 지정', '볼륨 선언'], answer: 1,
    explain: '`RUN cd /app && ...` 는 다음 명령에 영향이 없으므로 WORKDIR 을 씁니다. 상대 경로 COPY 의 기준도 WORKDIR 입니다.',
    example: '`WORKDIR /app` → `COPY . .` → `CMD ["node", "server.js"]` 가 표준 패턴.' },
  { diff: 'normal', type: 'mcq', q: '`.dockerignore` 파일의 목적은?', options: ['컨테이너에서 무시할 시그널 지정', '빌드 컨텍스트에서 제외할 파일 지정 → 전송량·이미지 크기 감소, 비밀 파일 유출 방지', '이미지 태그 무시', 'Compose 서비스 제외'], answer: 1,
    explain: '`docker build .` 는 컨텍스트 전체를 데몬에 전송합니다. `.git`, `node_modules`, `.env`, `*.log` 를 제외하면 빌드가 빨라지고 `COPY . .` 로 비밀이 들어가는 사고를 막습니다.',
    example: '`.dockerignore` 없이 `COPY . .` 를 하면 `.env` 와 `.git` 이 이미지에 포함되어 레지스트리로 유출되는 사례가 많습니다.' },
  { diff: 'normal', type: 'short', q: 'kubectl 이 사용할 컨텍스트를 `prod-cluster` 로 전환하는 명령어는?', answer: 'kubectl config use-context prod-cluster', accept: ['kubectl config use prod-cluster'],
    explain: '컨텍스트 = 클러스터 + 사용자 + 기본 네임스페이스 조합. `kubectl config get-contexts` 로 목록. `kubectx` 도구가 편리합니다.',
    example: '실수로 prod 에 명령을 날리는 것을 막기 위해 prod 컨텍스트는 읽기 전용 사용자로 두고, 배포는 CI 만 하도록 합니다.' },
  { diff: 'normal', type: 'short', q: 'Pod 목록을 노드 이름과 Pod IP 까지 포함해 보는 kubectl 명령어는?', answer: 'kubectl get pods -o wide', accept: ['kubectl get po -o wide', 'kubectl get pod -o wide', 'kubectl get pods -owide'],
    explain: '`-o wide` 는 NODE, IP, NOMINATED NODE, READINESS GATES 컬럼을 추가합니다. `-o yaml/json` 은 전체 스펙, `-o jsonpath` 는 특정 필드.',
    example: '특정 노드에 Pod 가 몰려 있는지, 재시작된 Pod 가 어느 노드에 있는지 확인.' },
  { diff: 'normal', type: 'short', q: 'Deployment 리소스의 `spec.strategy` 필드 문서를 터미널에서 보는 kubectl 명령어는?', answer: 'kubectl explain deployment.spec.strategy', accept: ['kubectl explain deploy.spec.strategy', 'kubectl explain deployment.spec.strategy --recursive'],
    explain: '`kubectl explain 리소스.경로` 는 API 스키마 문서를 보여줍니다. `--recursive` 로 하위 필드 전체. YAML 작성 시 필드명·타입을 확인하는 가장 빠른 방법.',
    example: '`kubectl explain pod.spec.containers.resources` 로 requests/limits 형식 확인.' },
  { diff: 'normal', type: 'short', q: '현재 네임스페이스의 이벤트를 시간순으로 정렬해 보는 kubectl 명령어는?', answer: 'kubectl get events --sort-by=.metadata.creationTimestamp', accept: ["kubectl get events --sort-by='.metadata.creationTimestamp'", 'kubectl get events --sort-by=.lastTimestamp', "kubectl get events --sort-by='.lastTimestamp'", 'kubectl get events --sort-by .metadata.creationTimestamp', 'kubectl events'],
    explain: '이벤트는 기본 정렬이 없어 `--sort-by` 가 필요합니다. `-A` 로 전체, `--field-selector type=Warning` 으로 경고만. kubectl 1.26+ 는 `kubectl events` 가 정렬된 출력을 제공합니다.',
    example: 'Pod 가 스케줄되지 않거나 이미지 pull 이 실패할 때 클러스터 수준 원인을 이벤트에서 찾습니다. 이벤트는 기본 1시간만 보관됩니다.' },
  { diff: 'normal', type: 'short', q: 'Docker Compose 의 `api` 서비스 로그를 실시간으로 보는 명령어는?', answer: 'docker compose logs -f api', accept: ['docker-compose logs -f api', 'docker compose logs --follow api', 'docker compose logs -f --tail 100 api'],
    explain: '서비스명을 생략하면 모든 서비스 로그가 섞여 나옵니다. `--tail N`, `--since 10m`, `-t`(타임스탬프) 옵션.',
    example: '`docker compose up -d && docker compose logs -f api db` 로 두 서비스만 추적.' },
  { diff: 'normal', type: 'short', q: 'Deployment `api` 의 Pod 를 스펙 변경 없이 롤링 방식으로 모두 재시작하는 kubectl 명령어는?', answer: 'kubectl rollout restart deployment/api', accept: ['kubectl rollout restart deploy/api', 'kubectl rollout restart deployment api', 'kubectl rollout restart deploy api'],
    explain: 'Pod 템플릿에 재시작 시각 어노테이션을 추가해 새 ReplicaSet 을 만듭니다. ConfigMap/Secret 을 바꾼 뒤 반영할 때 표준 방법입니다.',
    example: 'ConfigMap 수정 후 `kubectl rollout restart deploy/api && kubectl rollout status deploy/api`.' },
  { diff: 'normal', type: 'mcq', q: 'Kubernetes NodePort 서비스가 기본적으로 사용하는 포트 범위는?', options: ['1-1024', '8000-9000', '30000-32767', '49152-65535'], answer: 2,
    explain: '`--service-node-port-range` 로 변경 가능. 모든 노드의 해당 포트로 접근하면 Service 로 전달됩니다. 운영에서는 NodePort 직접 노출보다 LoadBalancer/Ingress 를 씁니다.',
    example: '온프레미스에서 외부 L4 장비가 노드들의 NodePort 로 트래픽을 보내는 구성.' },
  { diff: 'normal', type: 'short', q: '프라이빗 레지스트리 `registry.example.com` 에 로그인하는 docker 명령어는?', answer: 'docker login registry.example.com', accept: ['docker login registry.example.com -u user', 'docker login -u user registry.example.com'],
    explain: '자격 증명은 `~/.docker/config.json` 에 저장됩니다(기본 base64, credential helper 권장). Kubernetes 는 `imagePullSecrets` 로 같은 정보를 전달합니다.',
    example: 'CI 에서 `echo $TOKEN | docker login ghcr.io -u $USER --password-stdin` 으로 비밀번호를 인수에 노출하지 않고 로그인.' },
  { diff: 'normal', type: 'mcq', q: '모든(또는 선택된) 노드에 정확히 하나씩 Pod 를 실행해야 할 때 사용하는 리소스는?', options: ['Deployment', 'StatefulSet', 'DaemonSet', 'Job'], answer: 2,
    explain: 'DaemonSet 은 노드가 추가되면 자동으로 Pod 를 배치합니다. 로그 수집기, 모니터링 에이전트, CNI, 스토리지 드라이버에 사용. `drain` 시 `--ignore-daemonsets` 가 필요한 이유.',
    example: 'Fluent Bit, node-exporter, kube-proxy 가 DaemonSet 으로 배포됩니다.' },
  { diff: 'normal', type: 'short', q: '매니페스트 파일 `app.yaml` 에 정의된 리소스들을 클러스터에서 삭제하는 kubectl 명령어는?', answer: 'kubectl delete -f app.yaml',
    explain: '`apply -f` 의 반대. 파일에 있는 모든 리소스를 삭제합니다. 디렉터리도 가능. `--dry-run=client` 로 미리 확인.',
    example: '테스트 환경 정리: `kubectl delete -f k8s/ -n test`.' },
  { diff: 'normal', type: 'ox', q: 'Kubernetes 에서 Pod 의 IP 는 재생성될 때 바뀔 수 있으므로, 다른 Pod 는 Pod IP 가 아닌 Service 이름(DNS)으로 접근해야 한다.', answer: true,
    explain: 'Service 는 레이블 셀렉터로 Pod 집합을 추적하며 안정적인 ClusterIP 와 DNS 이름(`svc.ns.svc.cluster.local`)을 제공합니다. Endpoints/EndpointSlice 가 실제 Pod IP 목록.',
    example: '앱 설정에 `DB_HOST=postgres` (Service 이름) 로 지정하면 Pod 가 교체되어도 연결이 유지됩니다.' },
  { diff: 'normal', type: 'short', q: '실행 중인 컨테이너 `web` 을 그대로 새 이미지 `web-debug:1` 로 저장하는 명령어는?', answer: 'docker commit web web-debug:1',
    explain: '컨테이너의 쓰기 레이어를 이미지로 굳힙니다. 디버깅 스냅샷에는 유용하지만 재현성이 없으므로 운영 이미지는 항상 Dockerfile 로 빌드해야 합니다.',
    example: '장애 상태의 컨테이너를 `commit` 해 두고 나중에 분석하는 용도.' },

  /* ---------------- HARD ---------------- */
  { diff: 'hard', type: 'short', q: '컨테이너가 호스트의 네트워크 스택을 그대로 사용하도록(포트 매핑 없이) `nginx` 를 실행하는 명령어는?', answer: 'docker run -d --network host nginx', accept: ['docker run --network host -d nginx', 'docker run -d --net host nginx', 'docker run -d --network=host nginx', 'docker run -d --net=host nginx'],
    explain: 'host 네트워크는 NAT 오버헤드가 없고 호스트 포트를 직접 점유합니다. 격리가 약해지고 포트 충돌이 가능하며, Docker Desktop(Mac/Windows)에서는 동작이 다릅니다.',
    example: '고성능 프록시나 호스트 네트워크 인터페이스를 감시해야 하는 모니터링 에이전트에 사용.' },
  { diff: 'hard', type: 'short', q: '비-root 사용자로 실행 중인 컨테이너 `app` 에 root 권한으로 셸을 여는 명령어는?', answer: 'docker exec -it -u root app sh', accept: ['docker exec -it -u 0 app sh', 'docker exec -u root -it app sh', 'docker exec -it --user root app sh', 'docker exec -it -u root app bash', 'docker exec -it -u root app /bin/sh', 'docker exec -u 0 -it app sh'],
    explain: '`-u`/`--user` 로 exec 시 사용자 지정. 이미지에 `USER app` 이 있어도 디버깅용으로 root 진입이 가능합니다(런타임이 허용하는 한).',
    example: '패키지 설치가 필요한 임시 디버깅: `docker exec -it -u root app apk add curl`.' },
  { diff: 'hard', type: 'short', q: 'Pod `api-7d9f` 의 전체 스펙과 현재 상태를 YAML 로 출력하는 kubectl 명령어는?', answer: 'kubectl get pod api-7d9f -o yaml', accept: ['kubectl get po api-7d9f -o yaml', 'kubectl get pods api-7d9f -o yaml', 'kubectl get pod/api-7d9f -o yaml', 'kubectl get pod api-7d9f -oyaml'],
    explain: '`status` 섹션에 컨테이너 상태, 재시작 횟수, lastState(종료 사유·Exit Code), conditions 가 있습니다. `describe` 보다 기계 처리에 적합.',
    example: '`kubectl get pod X -o yaml | grep -A5 lastState` 로 이전 종료 원인 확인.' },
  { diff: 'hard', type: 'short', q: 'Pod `api-7d9f` 에 레이블 `tier=backend` 를 추가하는 kubectl 명령어는?', answer: 'kubectl label pod api-7d9f tier=backend', accept: ['kubectl label po api-7d9f tier=backend', 'kubectl label pods api-7d9f tier=backend'],
    explain: '기존 레이블 덮어쓰기는 `--overwrite`, 제거는 `tier-`. 레이블은 셀렉터(Service, NetworkPolicy, affinity)의 기준이므로 운영 중 변경은 트래픽에 영향을 줄 수 있습니다.',
    example: '문제 Pod 를 Service 에서 빼서 격리 분석: `kubectl label pod X app-` (Service 셀렉터에서 제외되어 트래픽 중단, Pod 는 유지).' },
  { diff: 'hard', type: 'short', q: '노드 `worker-2` 에 `dedicated=gpu:NoSchedule` taint 를 추가하는 kubectl 명령어는?', answer: 'kubectl taint nodes worker-2 dedicated=gpu:NoSchedule', accept: ['kubectl taint node worker-2 dedicated=gpu:NoSchedule'],
    explain: 'taint 는 노드가 Pod 를 밀어내고, toleration 을 가진 Pod 만 스케줄됩니다. 효과: `NoSchedule`, `PreferNoSchedule`, `NoExecute`(기존 Pod 도 퇴거). 제거는 끝에 `-`.',
    example: 'GPU 노드에 일반 워크로드가 올라가지 않도록 taint 하고, ML 작업에만 toleration + nodeSelector 를 줍니다.' },
  { diff: 'hard', type: 'short', q: 'Deployment `api` 에 CPU 70% 기준으로 최소 2, 최대 10 replica 의 HPA 를 생성하는 kubectl 명령어는?', answer: 'kubectl autoscale deployment api --min=2 --max=10 --cpu-percent=70', accept: ['kubectl autoscale deploy api --min=2 --max=10 --cpu-percent=70', 'kubectl autoscale deployment/api --min=2 --max=10 --cpu-percent=70', 'kubectl autoscale deployment api --cpu-percent=70 --min=2 --max=10', 'kubectl autoscale deploy api --cpu-percent=70 --min=2 --max=10'],
    explain: 'HPA 는 metrics-server 의 사용량을 requests 대비 비율로 계산합니다. CPU requests 가 없으면 동작하지 않습니다. `kubectl get hpa` 로 TARGETS 확인. 운영에서는 YAML(autoscaling/v2)로 관리.',
    example: 'HPA 가 `<unknown>` 을 보이면 metrics-server 미설치 또는 requests 누락.' },
  { diff: 'hard', type: 'mcq', q: 'Pod 의 `initContainers` 의 특징으로 옳은 것은?', options: ['메인 컨테이너와 동시에 실행', '메인 컨테이너 시작 전에 순서대로 실행되며 모두 성공해야 메인이 시작됨', '실패해도 무시됨', '항상 root 로 실행'], answer: 1,
    explain: 'DB 마이그레이션, 의존 서비스 대기, 설정 파일 생성 등에 씁니다. 실패 시 Pod 의 restartPolicy 에 따라 재시도. `Init:0/1` 상태가 오래가면 init 컨테이너 로그를 봅니다(`kubectl logs POD -c init-name`).',
    example: '`initContainers: [{name: wait-db, image: busybox, command: ["sh","-c","until nc -z db 5432; do sleep 1; done"]}]`.' },
  { diff: 'hard', type: 'mcq', q: 'PodDisruptionBudget(PDB)의 역할은?', options: ['Pod 의 리소스 한도 설정', '노드 drain 등 자발적 중단 시 동시에 내려갈 수 있는 Pod 수를 제한해 최소 가용성 보장', 'Pod 의 최대 재시작 횟수 제한', '네트워크 대역폭 제한'], answer: 1,
    explain: '`minAvailable` 또는 `maxUnavailable` 로 정의. 노드 업그레이드 중 서비스 전체가 내려가는 것을 막습니다. 단, 노드 장애 같은 비자발적 중단에는 적용되지 않습니다.',
    example: 'replica 3 인 API 에 `minAvailable: 2` PDB → 클러스터 오토스케일러나 drain 이 한 번에 하나씩만 퇴거.' },
  { diff: 'hard', type: 'short', q: '키 `password=s3cret` 를 가진 Secret `db-cred` 를 명령줄에서 생성하는 kubectl 명령어는?', answer: 'kubectl create secret generic db-cred --from-literal=password=s3cret', accept: ['kubectl create secret generic db-cred --from-literal password=s3cret'],
    explain: '`generic` 타입. `--from-file=key=path` 로 파일에서, `--from-env-file` 로 .env 에서 생성 가능. 명령줄 이력에 남으므로 운영에서는 파일이나 시크릿 매니저 연동을 권장.',
    example: '매니페스트 생성만: `kubectl create secret generic db-cred --from-literal=password=s3cret --dry-run=client -o yaml > secret.yaml` (Sealed Secrets/SOPS 로 암호화 후 커밋).' },
  { diff: 'hard', type: 'short', q: '모든 네임스페이스에서 `Failed` 상태인 Pod 만 나열하는 kubectl 명령어는?', answer: 'kubectl get pods -A --field-selector status.phase=Failed', accept: ['kubectl get pods --all-namespaces --field-selector status.phase=Failed', 'kubectl get po -A --field-selector status.phase=Failed', 'kubectl get pods -A --field-selector=status.phase=Failed', 'kubectl get pod -A --field-selector status.phase=Failed'],
    explain: '`--field-selector` 는 레이블이 아닌 리소스 필드로 필터링합니다. `status.phase!=Running` 도 가능. `Evicted` Pod 는 phase 가 Failed 입니다.',
    example: 'Evicted Pod 일괄 정리: `kubectl delete pods -A --field-selector status.phase=Failed`.' },
  { diff: 'hard', type: 'mcq', q: '`imagePullPolicy` 에 대한 설명으로 옳은 것은?', options: ['항상 Always 가 기본', '태그가 `latest` 이거나 생략되면 기본 `Always`, 그 외 태그는 `IfNotPresent` 가 기본', 'Never 는 존재하지 않음', '이미지 다이제스트를 쓰면 매번 pull'], answer: 1,
    explain: '`latest` 를 덮어쓰며 배포하면 `IfNotPresent` 노드는 옛 이미지를 계속 씁니다. 불변 태그(git sha) 또는 `@sha256:` 다이제스트 사용이 정석입니다.',
    example: '"이미지를 push 했는데 변경이 반영 안 됨" 의 대부분은 같은 태그 재사용 + 캐시된 이미지 문제.' },
  { diff: 'hard', type: 'short', q: 'amd64 와 arm64 두 아키텍처용 이미지를 한 번에 빌드해 `myapp:1.0` 으로 push 하는 buildx 명령어는?', answer: 'docker buildx build --platform linux/amd64,linux/arm64 -t myapp:1.0 --push .', accept: ['docker buildx build --platform linux/amd64,linux/arm64 -t myapp:1.0 . --push', 'docker buildx build -t myapp:1.0 --platform linux/amd64,linux/arm64 --push .', 'docker buildx build --push --platform linux/amd64,linux/arm64 -t myapp:1.0 .'],
    explain: 'buildx 는 QEMU 에뮬레이션 또는 원격 빌더로 멀티 플랫폼 매니페스트 리스트를 만듭니다. `--load` 는 단일 플랫폼만 로컬에 로드 가능하므로 멀티는 `--push` 가 필요합니다.',
    example: 'Apple Silicon 개발자와 amd64 서버, Graviton(arm64) 노드를 모두 지원하는 이미지 빌드 파이프라인.' },
  { diff: 'hard', type: 'mcq', q: 'Kubernetes `Job` 과 `CronJob` 에 대한 설명으로 옳은 것은?', options: ['Job 은 항상 실행 상태를 유지', 'Job 은 완료될 때까지 Pod 를 실행(실패 시 backoffLimit 만큼 재시도), CronJob 은 스케줄에 따라 Job 을 생성', 'CronJob 은 Deployment 의 일종', 'Job 은 삭제되지 않음'], answer: 1,
    explain: '`completions`, `parallelism` 으로 병렬 배치. CronJob 은 `concurrencyPolicy`(Allow/Forbid/Replace) 와 `successfulJobsHistoryLimit` 설정이 중요. `startingDeadlineSeconds` 로 놓친 실행 처리.',
    example: '매일 리포트: `schedule: "0 3 * * *"`, `concurrencyPolicy: Forbid` 로 이전 실행이 안 끝났으면 건너뜀.' },
  { diff: 'hard', type: 'short', q: '레이블 `app=api` 인 모든 Pod 의 모든 컨테이너 로그를 한 번에 보는 kubectl 명령어는?', answer: 'kubectl logs -l app=api --all-containers', accept: ['kubectl logs -l app=api --all-containers=true', 'kubectl logs --selector app=api --all-containers', 'kubectl logs -l app=api --all-containers --prefix', 'kubectl logs -l app=api --all-containers -f'],
    explain: '`-l` 로 여러 Pod 를 선택하면 기본 최대 5개(`--max-log-requests`). `--prefix` 로 Pod/컨테이너 이름을 앞에 붙입니다. 여러 Pod 를 색상으로 구분해 보려면 `stern` 도구가 편리.',
    example: '롤링 배포 중 여러 replica 의 로그를 동시에 보며 에러가 특정 Pod 에만 나는지 확인.' },
  { diff: 'hard', type: 'short', q: '로컬 이미지 `myapp:1.0` 을 tar 파일로 저장해 레지스트리 없이 다른 서버로 옮기기 위한 명령어는?', answer: 'docker save -o myapp.tar myapp:1.0', accept: ['docker save myapp:1.0 -o myapp.tar', 'docker save myapp:1.0 > myapp.tar', 'docker save myapp:1.0 | gzip > myapp.tar.gz', 'docker image save -o myapp.tar myapp:1.0'],
    explain: '`docker save` 는 이미지(레이어+메타데이터), `docker export` 는 컨테이너 파일시스템만(히스토리 없음). 반대는 `docker load -i myapp.tar`. 폐쇄망 배포에 사용.',
    example: '`docker save myapp:1.0 | ssh server "docker load"` 로 한 줄 전송.' },
  { diff: 'hard', type: 'mcq', q: 'Pod 의 `securityContext` 에서 `readOnlyRootFilesystem: true` 를 설정했을 때 주의할 점은?', options: ['컨테이너가 시작되지 않음', '앱이 쓰는 임시 경로(/tmp, 캐시 등)에 emptyDir 볼륨을 별도로 마운트해야 함', '로그를 남길 수 없음', '이미지가 두 배로 커짐'], answer: 1,
    explain: '루트 파일시스템을 읽기 전용으로 만들어 런타임 변조를 막는 보안 강화 설정입니다. 앱이 파일을 쓰는 곳을 파악해 `emptyDir` 로 마운트합니다. `runAsNonRoot`, `allowPrivilegeEscalation: false` 와 함께 씁니다.',
    example: 'nginx 는 `/var/cache/nginx`, `/var/run` 에 씁니다 → 두 경로에 emptyDir 마운트.' },
  { diff: 'hard', type: 'ox', q: '`docker system df` 는 이미지·컨테이너·볼륨·빌드 캐시가 각각 차지하는 디스크 용량과 회수 가능한 양을 보여준다.', answer: true,
    explain: '`RECLAIMABLE` 컬럼이 prune 으로 되찾을 수 있는 양입니다. `-v` 로 항목별 상세. 빌드 캐시는 `docker builder prune` 으로 따로 정리.',
    example: 'CI 러너의 디스크가 찰 때 `docker system df` 로 빌드 캐시가 원인인지 이미지가 원인인지 먼저 파악.' },

  /* ---------------- EXTREME ---------------- */
  { diff: 'extreme', type: 'mcq', q: '노드에서 Pod 들이 `Evicted` 상태로 대량 종료되었고 노드 조건에 `DiskPressure=True` 가 있다. 가장 흔한 원인은?', options: ['PVC 용량 부족', '노드 루트 디스크(이미지 레이어, 컨테이너 로그, emptyDir)가 kubelet 의 eviction 임계값(기본 nodefs 10% 미만)에 도달', 'etcd 디스크 부족', 'Pod 의 메모리 limit 초과'], answer: 1,
    explain: 'kubelet 은 `imagefs.available`/`nodefs.available` 임계값에 도달하면 이미지 GC 후 Pod 를 퇴거합니다. 원인은 로테이션 안 된 컨테이너 로그, 큰 emptyDir, 이미지 누적. `kubectl describe node` 의 Conditions 와 Events 를 봅니다.',
    example: '노드에서 `du -sh /var/lib/containerd /var/log/pods` 로 범인을 찾고, `containerLogMaxSize` 설정과 이미지 GC 임계값을 조정합니다.' },
  { diff: 'extreme', type: 'short', q: 'Kubernetes 노드에 직접 접속해 containerd 런타임 수준에서 실행 중인 컨테이너 목록을 보는 명령어는?', answer: 'crictl ps', accept: ['sudo crictl ps', 'crictl ps -a', 'ctr -n k8s.io containers ls', 'ctr -n k8s.io c ls', 'sudo crictl ps -a', 'nerdctl -n k8s.io ps'],
    explain: '`crictl` 은 CRI 호환 런타임(containerd, CRI-O)용 kubectl 격 도구. `crictl pods`, `crictl logs`, `crictl inspect`, `crictl images`. `ctr` 은 containerd 저수준 CLI(네임스페이스 `k8s.io` 필수).',
    example: 'kubelet 이 죽어 `kubectl` 로 볼 수 없는 노드에서 컨테이너가 실제로 도는지, 이미지가 있는지 확인.' },
  { diff: 'extreme', type: 'mcq', q: 'etcd 의 DB 크기가 quota(기본 2GB)에 도달해 `mvcc: database space exceeded` 알람이 발생했다. 복구 절차는?', options: ['etcd 를 재시작', 'compaction 후 `etcdctl defrag`, 그리고 `etcdctl alarm disarm`', 'etcd 데이터 디렉터리 삭제', 'API 서버 재시작'], answer: 1,
    explain: '이 상태에서 etcd 는 읽기 전용이 되어 클러스터에 아무것도 쓸 수 없습니다. 이력 리비전을 compaction 으로 정리해도 파일 크기는 defrag 해야 줄며, 마지막에 알람을 해제합니다. 원인은 대량 이벤트, 큰 ConfigMap, 컨트롤러의 잦은 업데이트 등.',
    example: '`--auto-compaction-retention` 설정과 etcd DB 크기 메트릭(`etcd_mvcc_db_total_size_in_bytes`) 알람이 예방책입니다.' },
  { diff: 'extreme', type: 'ox', q: 'kube-proxy 의 iptables 모드는 Service 수가 수천 개로 늘어나면 규칙 갱신이 O(n) 이라 느려지며, ipvs 모드나 eBPF 기반 CNI(Cilium)로 대체하면 개선된다.', answer: true,
    explain: 'iptables 모드는 Service/Endpoint 변경마다 전체 규칙 체인을 재작성합니다. IPVS 는 해시 테이블 기반으로 대규모에 적합하고, Cilium 은 kube-proxy 자체를 대체합니다.',
    example: '수천 Service 클러스터에서 Endpoint 변경 반영이 수 초~수십 초 지연되어 롤링 배포 중 502 가 나는 문제.' },
  { diff: 'extreme', type: 'mcq', q: '프라이빗 레지스트리 이미지가 `ImagePullBackOff` 이고 `describe` 에 "unauthorized" 가 보인다. 해결로 옳은 것은?', options: ['노드에서 docker login', '`kubectl create secret docker-registry` 로 Secret 생성 후 Pod 의 `imagePullSecrets` 또는 ServiceAccount 에 연결', 'imagePullPolicy 를 Never 로', '이미지를 public 으로 변경'], answer: 1,
    explain: '노드에서 로그인해도 kubelet 은 그 자격 증명을 쓰지 않습니다(일부 클라우드 노드 IAM 통합 제외). Secret 을 네임스페이스마다 두거나 `default` ServiceAccount 에 `imagePullSecrets` 를 패치하면 모든 Pod 가 사용합니다.',
    example: '`kubectl create secret docker-registry regcred --docker-server=ghcr.io --docker-username=U --docker-password=T` → `kubectl patch sa default -p \'{"imagePullSecrets":[{"name":"regcred"}]}\'`.' },
  { diff: 'extreme', type: 'short', q: '네임스페이스 `prod` 의 Pod 별이 아닌 **컨테이너별** CPU/메모리 사용량을 보는 kubectl 명령어는?', answer: 'kubectl top pods -n prod --containers', accept: ['kubectl top pod -n prod --containers', 'kubectl -n prod top pods --containers', 'kubectl top po -n prod --containers', 'kubectl top pods --containers -n prod'],
    explain: '사이드카(istio-proxy, 로그 수집기)가 리소스를 얼마나 쓰는지 분리해 봐야 앱 컨테이너의 limit 을 정확히 정할 수 있습니다. `--sort-by=memory` 로 정렬.',
    example: 'OOMKilled 가 앱인지 사이드카인지 구분하거나, 사이드카 오버헤드를 측정해 requests 를 조정.' },
  { diff: 'extreme', type: 'mcq', q: '노드가 주기적으로 `NotReady` 가 되고 kubelet 로그에 "PLEG is not healthy: pleg was last seen active Xm ago" 가 찍힌다. 의미는?', options: ['네트워크 플러그인 오류', 'kubelet 의 Pod Lifecycle Event Generator 가 컨테이너 런타임 응답 지연(너무 많은 컨테이너, 런타임 hang, 디스크 I/O 병목)으로 relist 를 제때 못 함', '인증서 만료', 'API 서버 과부하'], answer: 1,
    explain: 'PLEG 는 런타임에 주기적으로 컨테이너 상태를 묻습니다(relist, 기본 3분 임계). 노드당 Pod 수가 많거나 containerd 가 느리면 발생. `crictl ps` 응답 시간, 디스크 I/O, 좀비 컨테이너를 확인합니다.',
    example: '`maxPods` 를 낮추거나, 노드 디스크를 SSD 로 바꾸거나, 런타임 업그레이드로 해결한 사례들이 있습니다.' },
  { diff: 'extreme', type: 'short', q: '모든 네임스페이스의 Pod 를 "네임스페이스, 이름, 노드" 세 컬럼만 커스텀 출력하는 kubectl 명령어는?', answer: 'kubectl get pods -A -o custom-columns=NS:.metadata.namespace,NAME:.metadata.name,NODE:.spec.nodeName', accept: ['kubectl get pods --all-namespaces -o custom-columns=NS:.metadata.namespace,NAME:.metadata.name,NODE:.spec.nodeName', 'kubectl get po -A -o custom-columns=NS:.metadata.namespace,NAME:.metadata.name,NODE:.spec.nodeName', 'kubectl get pods -A -o custom-columns=NAMESPACE:.metadata.namespace,NAME:.metadata.name,NODE:.spec.nodeName', 'kubectl get pods -A -o=custom-columns=NS:.metadata.namespace,NAME:.metadata.name,NODE:.spec.nodeName'],
    explain: '`-o custom-columns=헤더:jsonpath,...` 는 jq 없이 표를 만듭니다. `custom-columns-file` 로 파일 정의도 가능. 컬럼 헤더 이름은 자유입니다.',
    example: '노드 drain 전 그 노드의 Pod 목록: `... | grep worker-2`, 또는 `--field-selector spec.nodeName=worker-2`.' }
);
