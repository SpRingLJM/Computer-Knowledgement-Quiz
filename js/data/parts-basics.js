/* basics — 정답 명령어 풀이 (문제 ID → 조각별 설명, 순서 규칙)
 * 결과 화면의 "명령어 풀이" 칸에 표시된다. 조각은 정답 문자열의 부분 문자열이며 tools/validate.js 가 검증한다. */
window.QUIZ_PARTS = window.QUIZ_PARTS || {};
Object.assign(window.QUIZ_PARTS, {
 "basics-006": {
  "parts": [
   [
    "ctrl",
    "`Ctrl`(Control, 컨트롤) 키: 키보드 왼쪽 아래 구석에 있는 보조 키로, 혼자서는 아무 일도 하지 않고 다른 키와 함께 눌러 단축 기능을 만듭니다. 맥에서는 이 자리에 `Command`(⌘) 키를 씁니다."
   ],
   [
    "+",
    "`+` 기호: 실제로 누르는 키가 아니라 두 키를 함께 누른다는 뜻의 표기입니다."
   ],
   [
    "v",
    "`V` 키: 붙여넣기(paste)를 뜻합니다. 복사(C)·잘라내기(X) 바로 옆에 있어 손가락 하나로 오가기 쉽게 배치되었습니다. 방금 복사하거나 잘라 둔 내용을 커서 위치(또는 선택한 폴더)에 넣습니다."
   ]
  ],
  "order": "`Ctrl` 을 먼저 누른 채로 유지하고, 그 상태에서 `V` 를 짧게 누른 뒤 손을 뗍니다. `V` 를 먼저 누르면 그냥 글자(또는 키 본래 기능)가 입력되므로 순서가 중요합니다."
 },
 "basics-009": {
  "parts": [
   [
    "ctrl",
    "`Ctrl`(Control, 컨트롤) 키: 키보드 왼쪽 아래 구석에 있는 보조 키로, 혼자서는 아무 일도 하지 않고 다른 키와 함께 눌러 단축 기능을 만듭니다."
   ],
   [
    "+",
    "`+` 기호: 실제로 누르는 키가 아니라 두 키를 함께 누른다는 뜻의 표기입니다."
   ],
   [
    "shift",
    "`Shift`(시프트) 키: 대문자나 기호를 입력할 때 쓰는 보조 키로, 여기서는 `Ctrl` 과 함께 눌러 다른 단축키와 구분하는 역할을 합니다."
   ],
   [
    "+",
    "`+` 기호: 실제로 누르는 키가 아니라 두 키를 함께 누른다는 뜻의 표기입니다."
   ],
   [
    "esc",
    "`Esc`(Escape, 이스케이프) 키: 키보드 맨 왼쪽 위의 키입니다. `Ctrl`·`Shift` 를 누른 상태에서 이 키를 누르면 작업 관리자가 바로 열립니다. (맥에서는 이 단축키 대신 `Command+Option+Esc` 로 강제 종료 창을 엽니다.)"
   ]
  ],
  "order": "`Ctrl` 과 `Shift` 는 어느 것을 먼저 눌러도 되지만, 두 키를 모두 누른 채로 유지한 상태에서 마지막에 `Esc` 를 눌러야 합니다. `Esc` 를 먼저 누르면 동작하지 않습니다."
 },
 "basics-014": {
  "parts": [
   [
    "f2",
    "`F2` 키: 키보드 맨 윗줄의 기능 키(Function key) 중 두 번째 키입니다. 파일 탐색기에서 파일을 선택하고 누르면 이름을 고칠 수 있는 상태가 됩니다. 노트북에서는 `Fn` 키를 함께 눌러야 할 수도 있고, 맥 Finder 에서는 `Return`(엔터) 키가 이름 바꾸기입니다."
   ]
  ]
 },
 "basics-017": {
  "parts": [
   [
    "UEFI",
    "Unified Extensible Firmware Interface(통합 확장 펌웨어 인터페이스)의 약자입니다. 전원을 켜면 운영체제보다 먼저 실행되어 부품을 점검하고 윈도우 같은 운영체제를 불러오는 기본 프로그램으로, 옛 BIOS 를 대신합니다."
   ]
  ]
 }
});
