window.QUIZ_BANK.aws.push(
  /* ---------------- EASY ---------------- */
  { diff: 'easy', type: 'short', q: 'AWS CLI 에서 프로파일 `prod` 로 S3 버킷 목록을 조회하는 명령어는?', answer: 'aws s3 ls --profile prod', accept: ['aws --profile prod s3 ls', 'AWS_PROFILE=prod aws s3 ls'],
    explain: '`--profile` 은 `~/.aws/credentials` 의 섹션을 선택합니다. 환경변수 `AWS_PROFILE` 로 기본값을 바꿀 수 있습니다.',
    example: '여러 계정을 다룰 때 `export AWS_PROFILE=prod` 후 작업하고 프롬프트에 프로파일을 표시해 사고를 막습니다.' },
  { diff: 'easy', type: 'short', q: 'S3 버킷 `my-bucket` 의 객체 `tmp/old.log` 를 삭제하는 명령어는?', answer: 'aws s3 rm s3://my-bucket/tmp/old.log',
    explain: '`aws s3 rm`. 접두사 아래 전체 삭제는 `--recursive`. 버전 관리가 켜진 버킷에서는 삭제 마커만 생기고 이전 버전은 남습니다.',
    example: '오래된 빌드 산출물 정리: `aws s3 rm s3://artifacts/builds/2023/ --recursive`. Lifecycle 규칙이 더 안전한 자동화입니다.' },
  { diff: 'easy', type: 'mcq', q: 'AWS 에서 DNS 서비스는?', options: ['CloudFront', 'Route 53', 'API Gateway', 'Direct Connect'], answer: 1,
    explain: 'Route 53 은 도메인 등록, 호스팅 존, 헬스 체크 기반 장애 조치, 가중치/지연/지리 라우팅을 제공합니다. 이름은 DNS 포트 53에서 유래.',
    example: '`api.example.com` 을 ALB 에 Alias 로 연결하고, 헬스 체크 실패 시 다른 리전으로 failover 라우팅.' },
  { diff: 'easy', type: 'ox', q: 'IAM 사용자의 액세스 키(Access Key ID / Secret Access Key)는 생성 시점에만 Secret 을 확인할 수 있으며, 이후에는 다시 조회할 수 없다.', answer: true,
    explain: '분실하면 새 키를 만들고 이전 키를 비활성화/삭제합니다. 사용자당 최대 2개(로테이션용). `aws iam list-access-keys` 로 목록과 생성일 확인.',
    example: '키 로테이션: 새 키 생성 → 앱 교체 → 이전 키 Inactive → 문제 없으면 삭제.' },
  { diff: 'easy', type: 'short', q: 'EC2 인스턴스 `i-0abc123` 을 시작하는 AWS CLI 명령어는?', answer: 'aws ec2 start-instances --instance-ids i-0abc123',
    explain: '중지된 인스턴스를 시작합니다. 퍼블릭 IP 는 EIP 가 아니면 바뀝니다. `aws ec2 wait instance-running --instance-ids ...` 로 대기 가능.',
    example: '스케줄 스크립트에서 출근 시간에 개발 서버 자동 시작.' },
  { diff: 'easy', type: 'mcq', q: 'EC2 인스턴스를 중지했다가 시작하면 바뀌는 것은? (EIP 미사용)', options: ['프라이빗 IP', '퍼블릭 IP', '인스턴스 ID', 'EBS 볼륨 데이터'], answer: 1,
    explain: '퍼블릭 IPv4 는 동적 할당이라 stop/start 시 바뀝니다. 고정하려면 Elastic IP 를 연결. 프라이빗 IP 와 인스턴스 ID 는 유지, 재부팅(reboot)은 퍼블릭 IP 도 유지.',
    example: 'DNS 에 퍼블릭 IP 를 직접 넣었다가 중지 후 접속이 안 되는 사고 → EIP 또는 ALB 사용.' },
  { diff: 'easy', type: 'short', q: '현재 리전의 VPC 목록을 조회하는 AWS CLI 명령어는?', answer: 'aws ec2 describe-vpcs',
    explain: 'VpcId, CidrBlock, IsDefault 등을 반환. `--query "Vpcs[].[VpcId,CidrBlock]" --output table` 로 정리.',
    example: '새 리전에 인프라를 만들기 전 기본 VPC 존재 여부와 CIDR 충돌을 확인.' },
  { diff: 'easy', type: 'ox', q: 'AWS 리전(Region)은 여러 개의 가용 영역(Availability Zone)으로 구성되며, 각 AZ 는 물리적으로 분리된 데이터센터다.', answer: true,
    explain: 'AZ 간은 저지연 전용망으로 연결됩니다. 고가용성은 최소 2개 AZ 에 분산 배치(서브넷을 AZ 별로 생성, ALB/ASG/RDS Multi-AZ).',
    example: '`ap-northeast-2a`, `2b`, `2c` 에 각각 서브넷을 두고 ASG 가 인스턴스를 고르게 분배.' },
  { diff: 'easy', type: 'mcq', q: 'AWS 에서 컨테이너를 서버 관리 없이 실행하는 서버리스 컴퓨팅 엔진은?', options: ['EC2', 'Fargate', 'Lightsail', 'Elastic Beanstalk'], answer: 1,
    explain: 'Fargate 는 ECS/EKS 의 실행 유형으로, 노드(EC2) 없이 태스크/Pod 단위로 vCPU·메모리를 지정해 과금됩니다.',
    example: '트래픽이 불규칙한 API 를 ECS Fargate 로 띄우고 서비스 오토스케일링을 적용.' },
  { diff: 'easy', type: 'short', q: 'CloudWatch 로그 그룹 목록을 조회하는 AWS CLI 명령어는?', answer: 'aws logs describe-log-groups', accept: ['aws logs describe-log-groups --log-group-name-prefix /aws/lambda'],
    explain: '로그 그룹은 보존 기간(기본 무제한 → 비용)을 설정해야 합니다. `aws logs put-retention-policy --log-group-name X --retention-in-days 30`.',
    example: '비용 절감: 보존 기간이 없는 로그 그룹을 찾아 일괄 30일로 설정하는 스크립트.' },
  { diff: 'easy', type: 'mcq', q: 'S3 객체에 대한 "최종 일관성" 관련 설명으로 현재(2020년 12월 이후) 옳은 것은?', options: ['새 객체 PUT 후 즉시 읽으면 없을 수 있다', 'PUT/DELETE 후 즉시 강력한 읽기 일관성(strong read-after-write)이 보장된다', 'LIST 는 여전히 최종 일관성', '버전 관리 버킷만 강력 일관성'], answer: 1,
    explain: '2020년 12월부터 S3 는 모든 작업에 강력한 일관성을 제공합니다. 이전에는 덮어쓰기/삭제가 최종 일관성이라 데이터 파이프라인에 재시도 로직이 필요했습니다.',
    example: 'ETL 에서 파일을 쓴 직후 LIST 로 확인하는 패턴이 이제 안전합니다.' },
  { diff: 'easy', type: 'short', q: 'RDS 인스턴스 목록과 상태를 조회하는 AWS CLI 명령어는?', answer: 'aws rds describe-db-instances',
    explain: 'DBInstanceStatus(available/modifying/backing-up), Endpoint.Address, MultiAZ, 엔진 버전 확인. `--db-instance-identifier` 로 특정 인스턴스.',
    example: '`--query "DBInstances[].[DBInstanceIdentifier,DBInstanceStatus,Endpoint.Address]" --output table`.' },
  { diff: 'easy', type: 'ox', q: 'AWS 프리 티어와 무관하게, 중지(stop)된 EC2 인스턴스에 연결된 EBS 볼륨은 계속 과금된다.', answer: true,
    explain: '인스턴스 요금은 실행 중일 때만이지만 EBS 스토리지, 스냅샷, EIP(미사용 시) 는 계속 과금됩니다. 완전히 비용을 없애려면 terminate + 볼륨 삭제.',
    example: '"중지해 뒀는데 요금이 나온다" 의 원인은 대개 EBS 와 미연결 EIP.' },
  { diff: 'easy', type: 'mcq', q: 'ECS 에서 컨테이너 이미지, CPU/메모리, 환경변수, 포트 등을 정의하는 리소스는?', options: ['Cluster', 'Service', 'Task Definition', 'Capacity Provider'], answer: 2,
    explain: 'Task Definition 은 불변 버전(리비전)으로 관리되며, Service 가 이를 참조해 원하는 개수의 Task 를 유지합니다. Kubernetes 의 Pod 스펙 + Deployment 에 해당.',
    example: '이미지 태그 변경 → 새 리비전 등록(`register-task-definition`) → `update-service --task-definition api:43`.' },

  /* ---------------- NORMAL ---------------- */
  { diff: 'normal', type: 'short', q: 'EC2 인스턴스 `i-0abc123` 에 태그 `Env=prod` 를 추가하는 AWS CLI 명령어는?', answer: 'aws ec2 create-tags --resources i-0abc123 --tags Key=Env,Value=prod', accept: ['aws ec2 create-tags --tags Key=Env,Value=prod --resources i-0abc123'],
    explain: '태그는 비용 배분, 자동화(백업, 스케줄), IAM 조건(`aws:ResourceTag`)의 기준입니다. 여러 리소스·태그를 한 번에 지정 가능.',
    example: '태그 정책(Organizations)으로 `Env`, `Owner`, `CostCenter` 태그를 강제하는 거버넌스.' },
  { diff: 'normal', type: 'short', q: 'Security Group `sg-0abc123` 에 사무실 IP `203.0.113.5/32` 에서의 TCP 22 인바운드를 허용하는 명령어는?', answer: 'aws ec2 authorize-security-group-ingress --group-id sg-0abc123 --protocol tcp --port 22 --cidr 203.0.113.5/32', accept: ['aws ec2 authorize-security-group-ingress --group-id sg-0abc123 --cidr 203.0.113.5/32 --protocol tcp --port 22'],
    explain: '`authorize-security-group-ingress` 추가, `revoke-security-group-ingress` 제거. 규칙에 `--description` 을 붙여 출처를 남기는 것이 좋습니다.',
    example: '0.0.0.0/0 으로 22 를 여는 것은 Security Hub/Config 에서 즉시 경고됩니다. 대신 Session Manager 를 쓰세요.' },
  { diff: 'normal', type: 'mcq', q: 'IAM 정책 문서에서 `"Resource": "*"` 와 `"Action": "s3:*"` 를 함께 쓰는 것이 위험한 이유는?', options: ['문법 오류', '계정 내 모든 S3 버킷·객체에 대해 모든 작업(삭제 포함)을 허용해 최소 권한 원칙에 위배', 'S3 만 접근 가능해서', '비용이 증가'], answer: 1,
    explain: '리소스는 ARN 으로 특정 버킷(`arn:aws:s3:::my-bucket`, `.../*`)으로, 액션은 필요한 것(`s3:GetObject`, `s3:PutObject`)으로 좁힙니다. IAM Access Analyzer 가 사용 이력 기반으로 정책 축소를 제안합니다.',
    example: '읽기 전용 앱 역할: `s3:GetObject` on `arn:aws:s3:::app-assets/*` + `s3:ListBucket` on 버킷 ARN.' },
  { diff: 'normal', type: 'short', q: 'ECS 클러스터 `prod` 의 서비스 `api` 의 원하는 태스크 수를 4 로 변경하는 명령어는?', answer: 'aws ecs update-service --cluster prod --service api --desired-count 4', accept: ['aws ecs update-service --service api --cluster prod --desired-count 4'],
    explain: '`--desired-count` 로 수동 스케일. Application Auto Scaling 이 붙어 있으면 그 범위 내에서 조정됩니다. 배포 없이 개수만 바꿉니다.',
    example: '이벤트 전 사전 증설: `--desired-count 10` 후 종료 시 원복.' },
  { diff: 'normal', type: 'mcq', q: 'S3 버킷에 실수로 삭제한 객체를 복구할 수 있으려면 미리 켜 둬야 하는 것은?', options: ['버킷 암호화', '버전 관리(Versioning)', '전송 가속', '정적 웹 호스팅'], answer: 1,
    explain: '버전 관리가 켜져 있으면 삭제는 "삭제 마커" 추가일 뿐이며, 마커를 지우면 복구됩니다. MFA Delete 로 마커 삭제까지 보호할 수 있습니다.',
    example: '`aws s3api list-object-versions --bucket B --prefix key` 로 마커 VersionId 를 찾아 `delete-object --version-id` 로 마커 제거.' },
  { diff: 'normal', type: 'short', q: 'Lambda 함수 `my-fn` 의 코드를 로컬 `fn.zip` 으로 업데이트하는 AWS CLI 명령어는?', answer: 'aws lambda update-function-code --function-name my-fn --zip-file fileb://fn.zip', accept: ['aws lambda update-function-code --zip-file fileb://fn.zip --function-name my-fn'],
    explain: '바이너리 파일은 `fileb://` 접두사. 큰 패키지는 S3 에 올리고 `--s3-bucket --s3-key`. 설정(메모리, 타임아웃, 환경변수)은 `update-function-configuration`.',
    example: 'CI: `zip -r fn.zip . && aws lambda update-function-code ... && aws lambda wait function-updated --function-name my-fn`.' },
  { diff: 'normal', type: 'ox', q: 'AWS Systems Manager Parameter Store 의 `SecureString` 파라미터는 KMS 로 암호화되며, 표준 파라미터는 무료다.', answer: true,
    explain: '설정값·시크릿을 계층 경로(`/prod/db/password`)로 관리합니다. Secrets Manager 는 자동 로테이션과 크로스 리전 복제를 제공하지만 시크릿당 과금이 있습니다.',
    example: '`aws ssm get-parameter --name /prod/db/password --with-decryption --query Parameter.Value --output text`.' },
  { diff: 'normal', type: 'short', q: 'SSM Parameter Store 의 `/prod/db/password` (SecureString) 값을 복호화해 텍스트로 출력하는 명령어는?', answer: 'aws ssm get-parameter --name /prod/db/password --with-decryption --query Parameter.Value --output text', accept: ['aws ssm get-parameter --with-decryption --name /prod/db/password --query Parameter.Value --output text', 'aws ssm get-parameter --name /prod/db/password --with-decryption --query "Parameter.Value" --output text', "aws ssm get-parameter --name /prod/db/password --with-decryption --query 'Parameter.Value' --output text", 'aws ssm get-parameter --name /prod/db/password --with-decryption --output text --query Parameter.Value'],
    explain: '`--with-decryption` 이 없으면 암호문이 반환됩니다. 호출자에게 `ssm:GetParameter` 와 해당 KMS 키의 `kms:Decrypt` 가 모두 필요합니다.',
    example: '컨테이너 시작 스크립트에서 환경변수로 주입. ECS 는 태스크 정의의 `secrets` 필드로 자동 주입 가능.' },
  { diff: 'normal', type: 'mcq', q: 'CloudWatch 알람이 `INSUFFICIENT_DATA` 상태로 계속 남아 있다. 가장 흔한 원인은?', options: ['알람 임계값이 너무 높음', '메트릭 이름·네임스페이스·디멘션이 실제 데이터와 불일치하거나 데이터가 발행되지 않음', 'SNS 토픽 없음', '알람 기간이 너무 김'], answer: 1,
    explain: '디멘션(InstanceId, FunctionName 등)이 하나라도 다르면 데이터가 없습니다. 콘솔에서 메트릭이 그래프로 보이는지 먼저 확인. 데이터가 간헐적이면 `TreatMissingData` 설정 조정.',
    example: 'ASG 인스턴스가 교체되어 InstanceId 디멘션 알람이 영원히 INSUFFICIENT_DATA 가 되는 사례 → AutoScalingGroupName 디멘션 사용.' },
  { diff: 'normal', type: 'short', q: 'CloudFront 배포 `E1ABC` 의 전체 경로를 캐시 무효화하는 AWS CLI 명령어는?', answer: 'aws cloudfront create-invalidation --distribution-id E1ABC --paths "/*"', accept: ["aws cloudfront create-invalidation --distribution-id E1ABC --paths '/*'", 'aws cloudfront create-invalidation --paths "/*" --distribution-id E1ABC'],
    explain: '월 1,000 경로까지 무료, 이후 경로당 과금. `/*` 는 하나의 경로로 계산됩니다. 해시가 붙은 파일명(캐시 버스팅)을 쓰면 무효화가 거의 필요 없습니다.',
    example: 'SPA 배포: S3 sync 후 `index.html` 만 무효화하거나 전체 `/*` 무효화.' },
  { diff: 'normal', type: 'mcq', q: 'VPC 안의 EC2 에서 S3 로 대량 데이터를 전송할 때 NAT Gateway 비용을 없애는 방법은?', options: ['인스턴스에 퍼블릭 IP 부여', 'S3 용 VPC Gateway Endpoint 생성 (무료)', 'CloudFront 사용', 'Direct Connect'], answer: 1,
    explain: 'Gateway Endpoint(S3, DynamoDB)는 라우트 테이블에 접두사 목록 경로를 추가해 AWS 백본으로 직접 연결합니다. 무료이며 트래픽이 인터넷을 거치지 않습니다. 다른 서비스는 Interface Endpoint(PrivateLink, 유료).',
    example: 'NAT 비용 청구서에서 큰 부분이 S3 트래픽이었던 것을 Gateway Endpoint 로 제거하는 것은 대표적 비용 최적화.' },
  { diff: 'normal', type: 'short', q: 'DynamoDB 테이블 `Users` 에서 파티션 키 `userId`=`u123` 인 항목을 조회하는 AWS CLI 명령어는?', answer: 'aws dynamodb get-item --table-name Users --key \'{"userId":{"S":"u123"}}\'', accept: ['aws dynamodb get-item --table-name Users --key "{\\"userId\\":{\\"S\\":\\"u123\\"}}"', "aws dynamodb get-item --table-name Users --key '{\"userId\": {\"S\": \"u123\"}}'", 'aws dynamodb get-item --key \'{"userId":{"S":"u123"}}\' --table-name Users'],
    explain: 'DynamoDB JSON 은 타입 표기(`S` 문자열, `N` 숫자, `M` 맵)를 요구합니다. `--consistent-read` 로 강력 일관성. 여러 항목은 `batch-get-item`.',
    example: '운영 중 특정 사용자 레코드를 빠르게 확인. 스크립트에서는 SDK 의 Document Client 가 타입 표기를 대신 처리합니다.' },
  { diff: 'normal', type: 'ox', q: 'AWS Organizations 의 SCP(Service Control Policy)는 권한을 **부여**하지 않고, 멤버 계정에서 가능한 최대 권한의 **경계**만 정한다.', answer: true,
    explain: 'SCP 로 허용해도 계정 내 IAM 정책이 없으면 아무것도 못 합니다. 관리 계정(management account)에는 SCP 가 적용되지 않습니다. 리전 제한, 루트 사용자 차단, 특정 서비스 금지에 사용.',
    example: '`aws:RequestedRegion` 조건으로 허용 리전 외 모든 API 를 Deny 하는 SCP.' },
  { diff: 'normal', type: 'short', q: '계정의 IAM 사용자·역할별 마지막 활동과 자격 증명 상태를 CSV 로 받는 자격 증명 보고서 생성 명령어는?', answer: 'aws iam generate-credential-report', accept: [],
    explain: '`generate-credential-report` 로 생성 후 `get-credential-report` 로 다운로드(base64). MFA 여부, 액세스 키 마지막 사용일, 비밀번호 나이를 감사합니다.',
    example: '90일 이상 미사용 액세스 키를 찾아 비활성화하는 월간 보안 점검.' },
  { diff: 'normal', type: 'mcq', q: 'EKS 클러스터에 kubectl 을 연결하기 위해 kubeconfig 를 갱신하는 명령어는?', options: ['aws eks get-token', 'aws eks update-kubeconfig --name my-cluster --region ap-northeast-2', 'kubectl config eks my-cluster', 'aws eks describe-cluster'], answer: 1,
    explain: 'kubeconfig 에 클러스터 엔드포인트·CA 와 `aws eks get-token` 을 호출하는 exec 인증을 기록합니다. IAM 주체가 aws-auth ConfigMap 또는 Access Entry 에 매핑되어야 합니다.',
    example: '"error: You must be logged in to the server (Unauthorized)" 는 IAM 주체가 클러스터에 매핑되지 않은 경우.' },
  { diff: 'normal', type: 'short', q: 'EC2 인스턴스 `i-0abc123` 의 시스템 콘솔 출력(부팅 로그)을 확인하는 AWS CLI 명령어는?', answer: 'aws ec2 get-console-output --instance-id i-0abc123', accept: ['aws ec2 get-console-output --instance-id i-0abc123 --output text', 'aws ec2 get-console-output --instance-id i-0abc123 --latest'],
    explain: 'SSH 가 안 될 때 커널 패닉, 파일시스템 오류, cloud-init 실패를 볼 수 있는 유일한 창구 중 하나입니다. `--latest` 로 최신 출력. 스크린샷은 `get-console-screenshot`.',
    example: 'fstab 오타로 부팅이 멈춘 인스턴스 진단 → 볼륨을 다른 인스턴스에 붙여 수정.' },
  { diff: 'normal', type: 'mcq', q: 'ALB 타겟 그룹의 헬스 체크가 계속 `unhealthy` 인데 인스턴스에서 `curl localhost:8080/health` 는 200 이다. 확인할 것은?', options: ['인스턴스 타입', '인스턴스 Security Group 이 ALB 의 Security Group 에서 오는 8080 인바운드를 허용하는지, 헬스 체크 경로·포트·성공 코드 설정', 'ALB 의 리스너 인증서', 'Route 53 레코드'], answer: 1,
    explain: 'ALB → 타겟 트래픽은 ALB 의 SG 를 소스로 허용해야 합니다. 헬스 체크 포트가 `traffic-port` 인지, 경로가 리다이렉트(301)를 반환하지 않는지, 성공 코드 범위(기본 200)를 확인합니다.',
    example: '헬스 체크 경로가 `/` 인데 앱이 `/` 를 `/login` 으로 302 리다이렉트해 unhealthy 가 되는 흔한 사례.' },

  /* ---------------- HARD ---------------- */
  { diff: 'hard', type: 'short', q: 'S3 버킷 `my-bucket` 의 30일 지난 `logs/` 객체를 Glacier 로 옮기는 Lifecycle 규칙을 JSON 파일 `lc.json` 으로 적용하는 명령어는?', answer: 'aws s3api put-bucket-lifecycle-configuration --bucket my-bucket --lifecycle-configuration file://lc.json', accept: ['aws s3api put-bucket-lifecycle-configuration --lifecycle-configuration file://lc.json --bucket my-bucket'],
    explain: '`put-bucket-lifecycle-configuration` 은 기존 규칙 전체를 **교체**합니다. 먼저 `get-bucket-lifecycle-configuration` 으로 현재 규칙을 받아 병합해야 합니다.',
    example: 'Rules: `{Filter:{Prefix:"logs/"}, Transitions:[{Days:30,StorageClass:"GLACIER"}], Expiration:{Days:365}, Status:"Enabled"}`.' },
  { diff: 'hard', type: 'mcq', q: 'EC2 인스턴스에서 `curl http://169.254.169.254/latest/meta-data/` 가 401 을 반환한다. 원인은?', options: ['IAM 역할 없음', 'IMDSv2 필수 설정이라 세션 토큰 없이 접근 불가', '인스턴스가 프라이빗 서브넷', 'Security Group 차단'], answer: 1,
    explain: 'IMDSv2 는 `PUT /latest/api/token` 으로 토큰을 받고 `X-aws-ec2-metadata-token` 헤더를 붙여야 합니다. 오래된 SDK/에이전트는 v1 만 지원하므로 업그레이드가 필요할 수 있습니다.',
    example: 'SSRF 취약점으로 IMDS 자격 증명이 탈취된 Capital One 사고 이후 IMDSv2 강제(`--metadata-options HttpTokens=required`)가 표준이 되었습니다.' },
  { diff: 'hard', type: 'short', q: 'EC2 인스턴스 `i-0abc123` 을 IMDSv2 필수로 변경하는 AWS CLI 명령어는?', answer: 'aws ec2 modify-instance-metadata-options --instance-id i-0abc123 --http-tokens required', accept: ['aws ec2 modify-instance-metadata-options --instance-id i-0abc123 --http-tokens required --http-endpoint enabled', 'aws ec2 modify-instance-metadata-options --http-tokens required --instance-id i-0abc123'],
    explain: '`--http-tokens required` 가 v2 강제. `--http-put-response-hop-limit 1` 은 컨테이너에서의 IMDS 접근을 막습니다(EKS 노드는 IRSA 를 써야 하므로 1 권장). 실행 중 적용 가능.',
    example: 'Config 규칙 `ec2-imdsv2-check` 로 미준수 인스턴스를 찾아 일괄 변경.' },
  { diff: 'hard', type: 'mcq', q: 'RDS 에서 커넥션 수가 한계에 도달해 Lambda 가 "too many connections" 를 받는다. 권장 해결책은?', options: ['RDS 인스턴스 크기 무한 증설', 'RDS Proxy 를 두어 커넥션 풀링/재사용', 'Lambda 타임아웃 증가', 'Lambda 메모리 감소'], answer: 1,
    explain: 'Lambda 는 동시 실행마다 새 커넥션을 열어 DB 를 압도합니다. RDS Proxy 는 커넥션을 풀링하고 IAM 인증·장애 조치 시간 단축도 제공합니다. Lambda 의 예약 동시성으로 상한을 두는 것도 병행.',
    example: 'Serverless API + Aurora 조합에서 RDS Proxy 는 거의 필수 구성입니다.' },
  { diff: 'hard', type: 'short', q: 'ECS 서비스 `api` (클러스터 prod) 의 최근 배포 이벤트와 태스크 상태를 확인하는 AWS CLI 명령어는?', answer: 'aws ecs describe-services --cluster prod --services api', accept: ['aws ecs describe-services --cluster prod --services api --query "services[0].events[:10]"', 'aws ecs describe-services --cluster prod --services api --query services[0].events', 'aws ecs describe-services --services api --cluster prod'],
    explain: '`events` 에 "has started 2 tasks", "unable to place a task" 같은 배포 메시지가, `deployments` 에 PRIMARY/ACTIVE 배포의 running/pending 수가 있습니다.',
    example: '배포가 멈추면 events 에서 "insufficient memory" (용량 부족) 나 "failed ELB health checks" 를 찾습니다. 태스크 종료 사유는 `describe-tasks` 의 `stoppedReason`.' },
  { diff: 'hard', type: 'ox', q: 'AWS KMS 고객 관리형 키(CMK)를 삭제 예약하면 최소 7일의 대기 기간이 있으며, 삭제 후에는 그 키로 암호화된 데이터를 영구히 복호화할 수 없다.', answer: true,
    explain: '대기 기간(7~30일) 동안 취소할 수 있습니다. 실수 방지를 위해 삭제 대신 비활성화(`disable-key`)를 먼저 하고 영향이 없는지 확인합니다. CloudTrail 로 키 사용 이력 확인.',
    example: 'S3/EBS/RDS 가 그 키로 암호화되어 있으면 삭제 시 데이터 접근 불가 → 삭제 전 `kms:Decrypt` 이벤트가 없는지 최소 30일 관찰.' },
  { diff: 'hard', type: 'short', q: 'Auto Scaling Group `web-asg` 의 원하는 용량을 6 으로 즉시 변경하는 AWS CLI 명령어는?', answer: 'aws autoscaling set-desired-capacity --auto-scaling-group-name web-asg --desired-capacity 6', accept: ['aws autoscaling update-auto-scaling-group --auto-scaling-group-name web-asg --desired-capacity 6', 'aws autoscaling set-desired-capacity --desired-capacity 6 --auto-scaling-group-name web-asg'],
    explain: '`set-desired-capacity` 는 min/max 범위 내에서만 가능. `update-auto-scaling-group` 은 min/max 도 함께 변경. `--honor-cooldown` 으로 쿨다운 존중.',
    example: '스케줄 액션(`put-scheduled-update-group-action`)으로 매일 아침 미리 증설하는 예약(스케줄) 스케일링.' },
  { diff: 'hard', type: 'mcq', q: 'S3 버킷을 CloudFront 로만 접근 가능하게 하고 직접 URL 접근은 막는 권장 방법은?', options: ['버킷을 퍼블릭으로 두고 Referer 헤더 검사', 'OAC(Origin Access Control)를 설정하고 버킷 정책에서 해당 CloudFront 배포의 서비스 주체만 허용', '버킷 ACL 을 private 으로만', 'S3 정적 웹 호스팅 엔드포인트 사용'], answer: 1,
    explain: 'OAC(구 OAI)는 CloudFront 가 SigV4 로 S3 에 서명 요청합니다. 버킷 정책 Principal 을 `cloudfront.amazonaws.com` + Condition `AWS:SourceArn` 배포 ARN 으로 제한하고 Block Public Access 는 켜 둡니다.',
    example: '정적 사이트·이미지 CDN 표준 구성. 서명 URL/쿠키와 결합해 유료 콘텐츠도 보호.' },
  { diff: 'hard', type: 'short', q: '계정에서 미사용(어떤 인스턴스에도 연결되지 않은) EBS 볼륨 ID 만 나열하는 AWS CLI 명령어는?', answer: 'aws ec2 describe-volumes --filters Name=status,Values=available --query "Volumes[].VolumeId" --output text', accept: ["aws ec2 describe-volumes --filters Name=status,Values=available --query 'Volumes[].VolumeId' --output text", 'aws ec2 describe-volumes --filters Name=status,Values=available --query Volumes[].VolumeId --output text', 'aws ec2 describe-volumes --filters Name=status,Values=available --query "Volumes[*].VolumeId" --output text', 'aws ec2 describe-volumes --filters Name=status,Values=available --output text --query "Volumes[].VolumeId"'],
    explain: '상태 `available` = 미연결. 인스턴스 종료 시 `DeleteOnTermination=false` 인 볼륨이 남아 비용을 유발합니다. 삭제 전 스냅샷을 만들어 두는 것이 안전.',
    example: '월간 비용 점검 스크립트: 미사용 볼륨, 미연결 EIP, 오래된 스냅샷을 리포트.' },
  { diff: 'hard', type: 'mcq', q: 'Lambda 함수의 동시 실행이 급증해 다른 함수들이 `TooManyRequestsException` 을 받는다. 원인과 대응은?', options: ['함수 코드 버그', '리전 계정 동시 실행 한도(기본 1,000)를 한 함수가 소진 → 해당 함수에 예약 동시성(reserved concurrency) 상한 설정, 필요 시 한도 상향 요청', '메모리 부족', 'VPC ENI 부족'], answer: 1,
    explain: '예약 동시성은 함수의 상한이자 다른 함수로부터 보호받는 하한입니다. SQS 트리거는 `maximum concurrency` 로 소비 속도를 제한할 수 있습니다.',
    example: '이벤트 폭주 시 배치 처리 함수가 API 함수의 동시성을 잠식하는 사고를 예약 동시성으로 격리.' },
  { diff: 'hard', type: 'short', q: 'Route 53 호스팅 존 `Z123` 의 레코드 목록을 조회하는 AWS CLI 명령어는?', answer: 'aws route53 list-resource-record-sets --hosted-zone-id Z123',
    explain: '레코드 변경은 `change-resource-record-sets --change-batch file://change.json` (UPSERT/DELETE). 변경은 전파에 최대 60초. `--query` 로 특정 이름 필터.',
    example: '`--query "ResourceRecordSets[?Type==\'A\']"` 로 A 레코드만, 마이그레이션 전 전체 존을 JSON 으로 백업.' },
  { diff: 'hard', type: 'ox', q: 'Aurora 의 Reader 엔드포인트는 읽기 복제본 간 연결을 라운드로빈으로 분산하지만, DNS 기반이므로 커넥션 풀이 오래 유지되면 특정 복제본에 쏠릴 수 있다.', answer: true,
    explain: 'Reader 엔드포인트는 DNS 응답을 바꿔가며 분산합니다. 커넥션 풀은 이미 맺은 연결을 재사용하므로 편중이 생깁니다. 드라이버의 max lifetime 을 짧게 하거나 RDS Proxy 의 읽기 엔드포인트를 씁니다.',
    example: '복제본 3대 중 1대만 CPU 90% 인 현상의 원인이 앱 커넥션 풀의 고정 연결이었던 사례.' },
  { diff: 'hard', type: 'short', q: 'CloudTrail 에서 최근 `DeleteBucket` 이벤트를 조회하는 AWS CLI 명령어는?', answer: 'aws cloudtrail lookup-events --lookup-attributes AttributeKey=EventName,AttributeValue=DeleteBucket', accept: ['aws cloudtrail lookup-events --lookup-attributes "AttributeKey=EventName,AttributeValue=DeleteBucket"', 'aws cloudtrail lookup-events --lookup-attributes AttributeKey=EventName,AttributeValue=DeleteBucket --max-results 20'],
    explain: '`EventName` 으로 특정 API 호출 추적. 결과의 `Username`, `sourceIPAddress`, `userAgent` 로 누가 어디서 했는지 확인. `--start-time`/`--end-time` 으로 기간 제한.',
    example: '"누가 이 리소스를 삭제했나" 사후 조사, 혹은 EventBridge 규칙으로 실시간 알림.' },
  { diff: 'hard', type: 'mcq', q: 'EKS 에서 Pod 가 `Pending` 이고 이벤트에 "0/3 nodes are available: 3 Too many pods" 가 보인다. 원인은?', options: ['CPU 부족', '노드당 최대 Pod 수(인스턴스 타입별 ENI·IP 한도)에 도달', '이미지 크기 초과', 'PVC 바인딩 실패'], answer: 1,
    explain: 'VPC CNI 는 Pod 마다 VPC IP 를 쓰므로 인스턴스 타입의 ENI×IP 수가 노드당 Pod 상한입니다(t3.medium 은 17개). 해결: 더 큰 타입, prefix delegation 활성화, 노드 추가.',
    example: 'CPU/메모리는 남는데 Pod 가 안 뜨는 EKS 특유의 현상. `kubectl describe node | grep -i pods` 로 Capacity 확인.' },
  { diff: 'hard', type: 'short', q: 'S3 버킷 `my-bucket` 의 현재 버킷 정책을 조회하는 AWS CLI 명령어는?', answer: 'aws s3api get-bucket-policy --bucket my-bucket', accept: ['aws s3api get-bucket-policy --bucket my-bucket --query Policy --output text', 'aws s3api get-bucket-policy --bucket my-bucket --output text --query Policy'],
    explain: '반환값의 `Policy` 는 JSON 문자열이라 `--query Policy --output text | jq` 로 정리해 봅니다. 정책이 없으면 `NoSuchBucketPolicy` 에러. 적용은 `put-bucket-policy --policy file://p.json`.',
    example: '크로스 계정 접근 문제 조사 시 IAM 정책과 버킷 정책을 나란히 놓고 Deny 문을 찾습니다.' },

  /* ---------------- EXTREME ---------------- */
  { diff: 'extreme', type: 'mcq', q: 'CloudFormation 스택 삭제가 `DELETE_FAILED` 로 끝났고 원인이 "The bucket you tried to delete is not empty" 다. 올바른 처리는?', options: ['스택을 강제 삭제', '버킷을 비운 뒤 재시도하거나, 버킷을 보존하려면 `--retain-resources` 로 해당 논리 ID 를 지정해 삭제', 'CloudFormation 지원 티켓', '버킷 이름 변경'], answer: 1,
    explain: 'S3 버킷은 비어 있어야 삭제됩니다. 템플릿에 `DeletionPolicy: Retain` 을 두면 스택 삭제 시 버킷을 남깁니다. 커스텀 리소스(Lambda)로 삭제 전 비우기를 자동화하기도 합니다.',
    example: '`aws cloudformation delete-stack --stack-name S --retain-resources LogsBucket` 후 버킷은 수동 관리.' },
  { diff: 'extreme', type: 'short', q: 'VPC Flow Logs 없이 특정 ENI `eni-0abc` 로 향하는 트래픽 경로가 SG/NACL/라우팅 중 어디서 막히는지 분석하는 Reachability Analyzer 경로를 생성하는 AWS CLI 명령어는? (소스: i-0src, 목적지: eni-0abc, TCP 5432)', answer: 'aws ec2 create-network-insights-path --source i-0src --destination eni-0abc --protocol tcp --destination-port 5432', accept: ['aws ec2 create-network-insights-path --source i-0src --destination eni-0abc --destination-port 5432 --protocol tcp', 'aws ec2 create-network-insights-path --source i-0src --destination eni-0abc --protocol TCP --destination-port 5432'],
    explain: '경로 생성 후 `start-network-insights-analysis --network-insights-path-id` 로 분석하면 차단 지점(SG 규칙, NACL, 라우트 테이블)을 구성 요소 단위로 알려줍니다. 실제 패킷 없이 구성만 분석.',
    example: '"DB 에 연결이 안 되는데 SG 는 열었다" 를 수 분 만에 NACL 아웃바운드 ephemeral 포트 누락으로 특정한 사례.' },
  { diff: 'extreme', type: 'mcq', q: 'Aurora PostgreSQL 에서 장애 조치(failover) 후 앱이 계속 "cannot execute INSERT in a read-only transaction" 을 받는다. 원인은?', options: ['새 Writer 가 아직 준비 안 됨', '앱 커넥션 풀이 DNS 캐시/기존 연결로 이전 Writer(이제 Reader)에 붙어 있음 → 풀 연결 재검증·짧은 DNS TTL·RDS Proxy 또는 AWS JDBC Driver 사용', 'Aurora 버그', '스토리지 장애'], answer: 1,
    explain: 'Aurora 는 클러스터 엔드포인트 DNS 를 바꿔 failover 하지만 JVM 등의 DNS 캐시와 유지된 TCP 연결은 옛 인스턴스를 가리킵니다. 풀에서 `SHOW transaction_read_only` 검증, JVM `networkaddress.cache.ttl` 단축, AWS Advanced JDBC Wrapper 사용.',
    example: 'Multi-AZ 장애 조치 훈련(`failover-db-cluster`)을 정기적으로 실행해 앱의 복구 시간을 측정합니다.' },
  { diff: 'extreme', type: 'short', q: 'S3 버킷 `my-bucket` 의 삭제 마커를 포함한 모든 객체 버전 목록을 조회하는 AWS CLI 명령어는?', answer: 'aws s3api list-object-versions --bucket my-bucket', accept: [],
    explain: '`Versions` 와 `DeleteMarkers` 두 배열을 반환합니다. 버전 관리 버킷을 완전히 비우려면 두 배열의 모든 항목을 `delete-objects` 로 VersionId 와 함께 삭제해야 합니다. `aws s3 rm --recursive` 는 마커만 추가합니다.',
    example: '"버킷을 비웠는데 삭제가 안 된다/비용이 나온다" 는 대부분 남아 있는 이전 버전 때문입니다.' },
  { diff: 'extreme', type: 'ox', q: 'IAM 역할의 신뢰 정책(trust policy)에서 `"Principal": {"AWS": "arn:aws:iam::123456789012:root"}` 는 해당 계정의 루트 사용자만 허용한다는 뜻이다.', answer: false,
    explain: '`:root` 는 "계정 전체" 를 의미하며, 그 계정 안에서 `sts:AssumeRole` 권한을 가진 모든 IAM 주체가 assume 할 수 있습니다. 특정 역할/사용자만 허용하려면 그 ARN 을 명시하거나 Condition 을 추가합니다.',
    example: '크로스 계정 신뢰에서 `:root` 를 쓰면 상대 계정의 권한 관리에 전적으로 의존하게 됩니다. `ExternalId` 조건은 confused deputy 방지용.' },
  { diff: 'extreme', type: 'mcq', q: 'EBS gp3 볼륨의 CloudWatch `VolumeQueueLength` 가 높고 앱 지연이 크지만 IOPS 는 프로비저닝 값에 못 미친다. 가장 가능성 높은 병목은?', options: ['볼륨 크기 부족', 'EC2 인스턴스 자체의 EBS 대역폭/IOPS 한도(인스턴스 타입별) 도달 — 볼륨이 아닌 인스턴스가 병목', 'EBS 스냅샷 진행 중', '파일시스템 단편화'], answer: 1,
    explain: '각 인스턴스 타입은 EBS 최적화 대역폭과 IOPS 상한이 있습니다(예: m5.large 4,750 Mbps / 18,750 IOPS 버스트). 여러 볼륨의 합이 이를 넘으면 큐가 쌓입니다. `EBSIOBalance%`/`EBSByteBalance%` 메트릭이 0 에 가까우면 확정.',
    example: 'DB 서버의 io2 볼륨을 올려도 개선이 없다가 인스턴스 타입을 올리자 해결된 전형적 사례.' }
);
