window.QUIZ_BANK = window.QUIZ_BANK || {};
window.QUIZ_BANK.linux = [
  /* ---------------- EASY ---------------- */
  { diff: 'easy', type: 'short', q: '현재 작업 중인 디렉터리의 절대 경로를 출력하는 명령어는?', answer: 'pwd',
    explain: '`pwd` (print working directory)는 현재 셸이 위치한 디렉터리를 절대 경로로 출력합니다.',
    example: '스크립트 안에서 `cd` 후 위치를 확인하거나, 로그에 `echo "run in $(pwd)"` 로 실행 위치를 남길 때 씁니다.' },
  { diff: 'easy', type: 'short', q: '숨김 파일을 포함하여 파일을 자세히(권한·소유자·크기) 나열하는 `ls` 옵션 조합은?', answer: 'ls -la', accept: ['ls -al', '-la', '-al', 'ls -l -a'],
    explain: '`-l` 은 long format, `-a` 는 `.`으로 시작하는 숨김 파일까지 표시합니다.',
    example: '홈 디렉터리에서 `ls -la` 로 `.bashrc`, `.ssh/` 같은 설정 파일이 존재하는지 확인합니다.' },
  { diff: 'easy', type: 'short', q: '파일의 마지막 10줄을 실시간으로 계속 출력(follow)하는 명령어는? (파일명: app.log)', answer: 'tail -f app.log', accept: ['tail -f', 'tail -F app.log', 'tail -f ./app.log'],
    explain: '`tail -f` 는 파일 끝에 추가되는 내용을 계속 출력합니다. `-F` 는 로그 로테이션으로 파일이 교체되어도 추적합니다.',
    example: '배포 직후 `tail -f /var/log/nginx/error.log` 로 에러가 올라오는지 지켜봅니다.' },
  { diff: 'easy', type: 'ox', q: '`rm -rf /tmp/build` 는 확인 없이 디렉터리와 그 안의 모든 내용을 삭제한다.', answer: true,
    explain: '`-r` 은 재귀 삭제, `-f` 는 강제(확인 프롬프트 없음)입니다. 되돌릴 수 없으므로 경로를 반드시 확인해야 합니다.',
    example: 'CI 스크립트에서 이전 빌드 산출물을 지울 때 `rm -rf ./dist` 를 사용합니다. 변수 사용 시 `rm -rf "$DIR/"` 처럼 빈 변수를 방지해야 `rm -rf /` 사고를 막습니다.' },
  { diff: 'easy', type: 'mcq', q: '파일 `a.txt` 를 `b.txt` 로 이름을 바꾸는(이동하는) 명령어는?', options: ['cp a.txt b.txt', 'mv a.txt b.txt', 'rename a.txt b.txt', 'ln a.txt b.txt'], answer: 1,
    explain: '`mv` 는 파일 이동과 이름 변경을 모두 담당합니다. 같은 파일시스템 내에서는 inode 이름만 바뀌므로 즉시 완료됩니다.',
    example: '설정 파일을 수정하기 전 `mv nginx.conf nginx.conf.bak` 으로 백업합니다.' },
  { diff: 'easy', type: 'short', q: '텍스트 파일에서 "ERROR" 문자열이 포함된 줄만 출력하는 명령어는? (파일명: app.log)', answer: 'grep ERROR app.log', accept: ['grep "ERROR" app.log', "grep 'ERROR' app.log", 'grep -n ERROR app.log', 'cat app.log | grep ERROR'],
    explain: '`grep PATTERN FILE` 은 패턴이 매칭되는 줄을 출력합니다. `-i` 대소문자 무시, `-n` 줄번호, `-r` 재귀 검색 옵션이 자주 쓰입니다.',
    example: '`grep -c ERROR app.log` 로 에러 건수를 세거나, `grep -r "TODO" src/` 로 소스 전체를 검색합니다.' },
  { diff: 'easy', type: 'short', q: '파일 권한을 소유자는 읽기/쓰기/실행, 그룹과 기타는 읽기/실행만 가능하도록 숫자로 설정하는 chmod 명령어는? (파일명: run.sh)', answer: 'chmod 755 run.sh', accept: ['chmod 0755 run.sh', 'chmod u=rwx,go=rx run.sh', '755'],
    explain: 'r=4, w=2, x=1 을 합산합니다. 7(rwx) 5(r-x) 5(r-x) → 755.',
    example: '배포 스크립트 실행 권한 부여: `chmod 755 deploy.sh`. 비밀키는 `chmod 600 id_rsa` 로 소유자만 읽고 쓰게 합니다.' },
  { diff: 'easy', type: 'mcq', q: '현재 실행 중인 프로세스를 실시간으로 CPU/메모리 사용량과 함께 보여주는 명령어는?', options: ['ps', 'top', 'jobs', 'lsof'], answer: 1,
    explain: '`top` 은 실시간 갱신되는 프로세스 모니터입니다. `htop` 은 더 보기 좋은 대안입니다. `ps` 는 스냅샷만 보여줍니다.',
    example: '서버가 느릴 때 `top` 을 열고 `P`(CPU 정렬) 또는 `M`(메모리 정렬)으로 원인 프로세스를 찾습니다.' },
  { diff: 'easy', type: 'short', q: 'PID 1234 프로세스를 정상 종료(SIGTERM) 요청하는 명령어는?', answer: 'kill 1234', accept: ['kill -15 1234', 'kill -TERM 1234', 'kill -SIGTERM 1234'],
    explain: '`kill` 은 기본적으로 SIGTERM(15)을 보냅니다. 프로세스가 정리 작업을 하고 종료할 기회를 줍니다. `-9`(SIGKILL)는 강제 종료라 최후 수단입니다.',
    example: '무응답 애플리케이션에 먼저 `kill PID` 를 보내고, 수 초 뒤에도 살아 있으면 `kill -9 PID` 를 사용합니다.' },
  { diff: 'easy', type: 'ox', q: '`cat` 명령어는 파일 내용을 표준 출력으로 출력한다.', answer: true,
    explain: '`cat` (concatenate)은 파일들을 이어붙여 표준 출력으로 내보냅니다. 여러 파일을 합칠 때도 씁니다.',
    example: '`cat /etc/os-release` 로 배포판 확인, `cat a.log b.log > all.log` 로 로그 병합.' },
  { diff: 'easy', type: 'short', q: '디렉터리 `logs/archive` 를 상위 디렉터리가 없어도 한 번에 생성하는 명령어는?', answer: 'mkdir -p logs/archive', accept: ['mkdir -p ./logs/archive', 'mkdir --parents logs/archive'],
    explain: '`-p` (parents) 옵션은 중간 경로가 없으면 함께 만들고, 이미 있어도 에러를 내지 않습니다.',
    example: 'Dockerfile이나 배포 스크립트에서 `mkdir -p /var/app/data` 처럼 멱등하게 디렉터리를 준비합니다.' },
  { diff: 'easy', type: 'mcq', q: '디스크 파티션별 사용량을 사람이 읽기 쉬운 단위로 보여주는 명령어는?', options: ['du -sh', 'df -h', 'free -h', 'lsblk'], answer: 1,
    explain: '`df` (disk free)는 파일시스템 단위 사용량, `du` (disk usage)는 디렉터리/파일 단위 사용량입니다. `-h` 는 human-readable.',
    example: '디스크 풀 알람이 오면 `df -h` 로 어느 마운트가 찼는지 보고, `du -sh /var/log/*` 로 범인을 찾습니다.' },
  { diff: 'easy', type: 'short', q: '메모리 사용량을 사람이 읽기 쉬운 단위로 출력하는 명령어는?', answer: 'free -h', accept: ['free -m', 'free -g'],
    explain: '`free` 는 총/사용/여유/버퍼·캐시 메모리를 보여줍니다. `available` 컬럼이 실제 사용 가능한 양입니다.',
    example: 'OOM 의심 시 `free -h` 에서 `available` 이 거의 0 인지 확인합니다. buff/cache 는 필요 시 회수되므로 "사용 중"으로 오해하면 안 됩니다.' },
  { diff: 'easy', type: 'ox', q: '`sudo` 는 현재 사용자가 root 권한으로 명령을 실행할 수 있게 한다.', answer: true,
    explain: '`sudo` 는 `/etc/sudoers` 에 허용된 사용자가 다른 사용자(기본 root)로 명령을 실행하게 합니다. 사용 이력이 로그에 남습니다.',
    example: '`sudo systemctl restart nginx`, `sudo apt update`. root 셸 자체를 얻으려면 `sudo -i`.' },
  { diff: 'easy', type: 'short', q: '명령어 `ls` 의 매뉴얼 페이지를 여는 명령어는?', answer: 'man ls',
    explain: '`man` 은 시스템 매뉴얼을 봅니다. 섹션 번호를 지정할 수도 있습니다 (예: `man 5 crontab` 은 파일 포맷 설명).',
    example: '옵션이 기억나지 않을 때 `man tar` 후 `/extract` 로 검색합니다. `ls --help` 는 짧은 요약입니다.' },
  { diff: 'easy', type: 'mcq', q: '명령어의 출력 결과를 파일에 **덮어쓰기** 저장하는 리다이렉션 기호는?', options: ['>', '>>', '<', '|'], answer: 0,
    explain: '`>` 는 파일을 새로 만들거나 덮어쓰고, `>>` 는 끝에 추가(append)합니다. `|` 는 다른 명령의 입력으로 넘깁니다.',
    example: '`ps aux > procs.txt` 로 스냅샷 저장, `echo "line" >> notes.txt` 로 추가.' },
  { diff: 'easy', type: 'short', q: '`.tar.gz` 압축 파일 `app.tar.gz` 를 현재 디렉터리에 풀어내는 명령어는?', answer: 'tar -xzf app.tar.gz', accept: ['tar xzf app.tar.gz', 'tar -xvzf app.tar.gz', 'tar xvzf app.tar.gz', 'tar -zxvf app.tar.gz', 'tar zxvf app.tar.gz', 'tar -zxf app.tar.gz', 'tar -xf app.tar.gz', 'tar xf app.tar.gz'],
    explain: '`x` 추출, `z` gzip, `f` 파일 지정, `v` 진행 출력. 생성은 `tar -czf out.tar.gz dir/`. 최신 tar 는 `-z` 없이도 압축 형식을 자동 감지합니다.',
    example: '다운로드한 바이너리 배포판을 `tar -xzf node-v20.tar.gz -C /opt` 로 특정 경로에 풀어 설치합니다.' },
  { diff: 'easy', type: 'ox', q: 'Linux에서 파일명은 대소문자를 구분한다 (`File.txt` 와 `file.txt` 는 다른 파일).', answer: true,
    explain: 'ext4 등 리눅스 파일시스템은 기본적으로 case-sensitive 입니다. Windows/macOS 기본은 case-insensitive라 이식 시 문제가 생깁니다.',
    example: 'macOS에서 개발한 코드가 `import "./Utils"` (실제 파일은 utils.js) 로 동작하다가 리눅스 CI에서 "module not found" 가 나는 흔한 사례.' },
  { diff: 'easy', type: 'short', q: '파일 `data.txt` 의 줄 수를 세는 명령어는?', answer: 'wc -l data.txt', accept: ['wc -l < data.txt', 'cat data.txt | wc -l'],
    explain: '`wc` (word count)의 `-l` 은 줄, `-w` 는 단어, `-c` 는 바이트 수입니다.',
    example: '`grep ERROR app.log | wc -l` 로 에러 발생 건수를 셉니다.' },
  { diff: 'easy', type: 'mcq', q: '환경 변수 `PATH` 의 값을 출력하는 명령어는?', options: ['print PATH', 'echo $PATH', 'env PATH', 'get PATH'], answer: 1,
    explain: '셸에서 변수는 `$이름` 으로 참조하며 `echo` 로 출력합니다. `env` 나 `printenv` 는 전체(또는 지정) 환경변수를 보여줍니다.',
    example: '"command not found" 가 나면 `echo $PATH` 로 실행 파일 경로가 포함되어 있는지 확인합니다.' },

  /* ---------------- NORMAL ---------------- */
  { diff: 'normal', type: 'short', q: '파일 `app.conf` 의 소유자를 `www-data`, 그룹을 `www-data` 로 변경하는 명령어는?', answer: 'chown www-data:www-data app.conf', accept: ['chown www-data.www-data app.conf', 'sudo chown www-data:www-data app.conf', 'chown www-data: app.conf'],
    explain: '`chown 사용자:그룹 파일`. `-R` 을 붙이면 디렉터리 하위까지 재귀 적용됩니다.',
    example: '웹 서버가 업로드 디렉터리에 쓸 수 있도록 `chown -R www-data:www-data /var/www/uploads`.' },
  { diff: 'normal', type: 'short', q: 'systemd 서비스 `nginx` 의 상태를 확인하는 명령어는?', answer: 'systemctl status nginx', accept: ['sudo systemctl status nginx', 'systemctl status nginx.service'],
    explain: '`systemctl status` 는 활성 여부, PID, 최근 로그 몇 줄을 보여줍니다. `start/stop/restart/reload/enable/disable` 이 짝을 이룹니다.',
    example: '`systemctl enable --now nginx` 는 부팅 시 자동 시작 등록과 즉시 시작을 한 번에 합니다.' },
  { diff: 'normal', type: 'short', q: '`nginx` 서비스의 journald 로그를 최근 것부터 100줄 보는 명령어는?', answer: 'journalctl -u nginx -n 100', accept: ['journalctl -u nginx --lines=100', 'journalctl -n 100 -u nginx', 'journalctl -u nginx -n100', 'journalctl -u nginx.service -n 100'],
    explain: '`journalctl -u UNIT` 으로 특정 유닛 로그를 필터링합니다. `-f` 실시간, `--since "1 hour ago"` 시간 필터, `-p err` 우선순위 필터.',
    example: '서비스가 시작 실패하면 `journalctl -u myapp -n 50 --no-pager` 로 스택트레이스를 확인합니다.' },
  { diff: 'normal', type: 'mcq', q: '어떤 프로세스가 TCP 8080 포트를 리스닝 중인지 확인하는 명령어로 적절한 것은?', options: ['ps -ef | grep 8080', 'ss -tlnp | grep 8080', 'ifconfig | grep 8080', 'route -n'], answer: 1,
    explain: '`ss -tlnp`: TCP(`t`), 리스닝(`l`), 숫자 표시(`n`), 프로세스 표시(`p`). 구형 `netstat -tlnp` 와 동일. `lsof -i :8080` 도 가능합니다.',
    example: '"address already in use" 에러가 나면 `ss -tlnp | grep :8080` 으로 점유 프로세스를 찾아 종료합니다.' },
  { diff: 'normal', type: 'short', q: '`grep` 으로 `src/` 디렉터리 하위 전체에서 대소문자 구분 없이 "todo" 를 검색하는 명령어는?', answer: 'grep -ri todo src/', accept: ['grep -ir todo src/', 'grep -ri todo src', 'grep -ir todo src', 'grep -rni todo src/', 'grep -inr todo src/', 'grep -ri "todo" src/'],
    explain: '`-r` 재귀, `-i` 대소문자 무시. `-n` 줄번호, `-l` 파일명만 출력, `--include="*.py"` 로 파일 필터가 가능합니다.',
    example: '코드 리뷰 전 남은 작업을 찾을 때 `grep -rn "TODO\\|FIXME" src/`.' },
  { diff: 'normal', type: 'mcq', q: '`find / -name "*.log" -size +100M` 명령어의 의미는?', options: ['100MB 이하의 .log 파일 찾기', '100MB 를 초과하는 .log 파일 찾기', '.log 파일 100개 찾기', '최근 100분 내 수정된 .log 찾기'], answer: 1,
    explain: '`-size +100M` 은 100MB 초과, `-100M` 은 미만. `-mtime -1` 은 최근 1일 내 수정, `-type f/d` 로 파일/디렉터리 필터.',
    example: '디스크 정리: `find /var/log -name "*.log" -size +500M -exec ls -lh {} \\;` 로 큰 로그를 찾은 뒤 로테이션 설정을 점검합니다.' },
  { diff: 'normal', type: 'short', q: '`find` 로 현재 디렉터리에서 7일 이상 수정되지 않은 `.tmp` 파일을 삭제하는 명령어는?', answer: 'find . -name "*.tmp" -mtime +7 -delete', accept: ["find . -name '*.tmp' -mtime +7 -delete", 'find . -name *.tmp -mtime +7 -delete', 'find . -mtime +7 -name "*.tmp" -delete', 'find . -type f -name "*.tmp" -mtime +7 -delete', 'find . -name "*.tmp" -mtime +7 -exec rm {} \\;', 'find . -name "*.tmp" -mtime +7 -exec rm -f {} \\;'],
    explain: '`-mtime +7` 은 수정 시각이 7일보다 오래된 파일. `-delete` 는 매칭된 파일 삭제 (조건 뒤에 와야 함).',
    example: 'cron 으로 매일 `find /tmp/uploads -type f -mtime +1 -delete` 를 돌려 임시 업로드를 정리합니다. 먼저 `-delete` 없이 실행해 목록을 확인하는 습관이 중요합니다.' },
  { diff: 'normal', type: 'ox', q: '`ps aux` 에서 STAT 컬럼이 `Z` 인 프로세스는 좀비 프로세스로, `kill -9` 로 직접 제거할 수 있다.', answer: false,
    explain: '좀비는 이미 종료되어 자원이 없고, 부모가 `wait()` 로 종료 상태를 회수하지 않아 프로세스 테이블에만 남은 상태입니다. 부모 프로세스를 종료/수정해야 사라집니다.',
    example: '컨테이너에서 PID 1 이 자식을 reap 하지 않아 좀비가 쌓이는 문제 → `tini` 나 `dumb-init` 을 entrypoint 로 사용합니다.' },
  { diff: 'normal', type: 'short', q: '심볼릭 링크 `/usr/local/bin/python` 이 `/usr/bin/python3.11` 을 가리키도록 만드는 명령어는?', answer: 'ln -s /usr/bin/python3.11 /usr/local/bin/python', accept: ['sudo ln -s /usr/bin/python3.11 /usr/local/bin/python', 'ln -sf /usr/bin/python3.11 /usr/local/bin/python'],
    explain: '`ln -s 원본 링크명` 순서입니다. `-f` 는 기존 링크를 덮어씁니다. 하드링크(`-s` 없음)와 달리 다른 파일시스템, 디렉터리도 가리킬 수 있습니다.',
    example: '배포 시 `ln -sfn /app/releases/v42 /app/current` 로 심볼릭 링크만 바꿔 무중단 롤백을 가능하게 합니다.' },
  { diff: 'normal', type: 'mcq', q: '`chmod +x script.sh` 후에도 `./script.sh` 실행 시 "bad interpreter" 오류가 난다. 가장 가능성 높은 원인은?', options: ['실행 권한이 부족하다', 'shebang 이 잘못되었거나 Windows 줄바꿈(CRLF)이 섞여 있다', '파일이 너무 크다', 'root 가 아니다'], answer: 1,
    explain: '`#!/bin/bash\\r` 처럼 CRLF 가 붙으면 커널이 `bash\\r` 라는 인터프리터를 찾다 실패합니다. `file script.sh` 로 확인하고 `sed -i "s/\\r$//"` 또는 `dos2unix` 로 고칩니다.',
    example: 'Windows 에서 작성한 스크립트를 리눅스 서버에 올려 실행할 때 매우 흔한 문제입니다. git 의 `core.autocrlf` 설정으로 예방합니다.' },
  { diff: 'normal', type: 'short', q: 'crontab 표현식으로 "매일 새벽 3시 30분" 을 나타내면? (분 시 일 월 요일)', answer: '30 3 * * *',
    explain: '필드 순서: 분(0-59) 시(0-23) 일(1-31) 월(1-12) 요일(0-7, 0과 7은 일요일). `*` 은 모든 값.',
    example: '`30 3 * * * /opt/scripts/backup.sh >> /var/log/backup.log 2>&1` 로 매일 백업 스케줄링. `crontab -e` 로 편집, `crontab -l` 로 확인.' },
  { diff: 'normal', type: 'short', q: '표준 출력과 표준 에러를 모두 `out.log` 파일로 보내는 리다이렉션은? (명령어: `./run.sh`)', answer: './run.sh > out.log 2>&1', accept: ['./run.sh &> out.log', './run.sh >out.log 2>&1', './run.sh &>out.log', './run.sh 2>&1 > out.log'],
    explain: '`2>&1` 은 fd 2(stderr)를 fd 1(stdout)이 가리키는 곳으로 복제합니다. 순서가 중요해서 `> out.log 2>&1` 이어야 합니다 (`2>&1 > out.log` 는 stderr 가 터미널로 감). bash 에선 `&>` 축약 가능.',
    example: 'cron 잡의 출력을 남길 때 `cmd >> /var/log/job.log 2>&1`. 정답으로 인정된 `2>&1 > out.log` 는 실제로는 stderr가 화면에 가므로 주의.' },
  { diff: 'normal', type: 'mcq', q: '`nohup ./server &` 의 목적은?', options: ['서버를 root 로 실행', '터미널을 닫아도(SIGHUP) 프로세스가 계속 실행되게 함', '서버를 우선순위 높게 실행', '서버 출력을 버림'], answer: 1,
    explain: '`nohup` 은 SIGHUP 을 무시하게 하고 출력을 `nohup.out` 으로 돌립니다. `&` 는 백그라운드 실행. 현대적으로는 `systemd` 서비스나 `tmux`/`screen` 을 권장합니다.',
    example: 'SSH 로 접속해 장시간 마이그레이션을 돌릴 때 `nohup ./migrate.sh > migrate.log 2>&1 &` 후 접속을 끊어도 안전합니다.' },
  { diff: 'normal', type: 'short', q: 'SSH 개인키 `~/.ssh/id_ed25519` 의 권한을 소유자만 읽고 쓸 수 있게 설정하는 명령어는?', answer: 'chmod 600 ~/.ssh/id_ed25519', accept: ['chmod 0600 ~/.ssh/id_ed25519', 'chmod 400 ~/.ssh/id_ed25519', 'chmod u=rw,go= ~/.ssh/id_ed25519'],
    explain: 'ssh 는 개인키 권한이 느슨하면 "UNPROTECTED PRIVATE KEY FILE" 경고와 함께 키를 거부합니다. 600 (rw-------) 또는 400.',
    example: 'AWS 에서 받은 `.pem` 키로 접속하려면 먼저 `chmod 400 key.pem` 이 필요합니다.' },
  { diff: 'normal', type: 'ox', q: '`export VAR=1` 로 설정한 변수는 셸을 종료해도 다음 로그인 때 유지된다.', answer: false,
    explain: '`export` 는 현재 셸과 자식 프로세스에만 적용됩니다. 영구 설정은 `~/.bashrc`, `~/.profile`, `/etc/environment` 등에 기록해야 합니다.',
    example: '`JAVA_HOME` 을 영구 설정하려면 `echo "export JAVA_HOME=/opt/jdk" >> ~/.bashrc && source ~/.bashrc`.' },
  { diff: 'normal', type: 'short', q: '`sed` 로 파일 `config.ini` 안의 모든 `localhost` 를 `db.internal` 로 **파일 자체를 수정**하며 치환하는 명령어는?', answer: 'sed -i "s/localhost/db.internal/g" config.ini', accept: ["sed -i 's/localhost/db.internal/g' config.ini", 'sed -i s/localhost/db.internal/g config.ini', "sed -i 's#localhost#db.internal#g' config.ini", "sed -i 's|localhost|db.internal|g' config.ini"],
    explain: '`-i` in-place 편집, `s/old/new/g` 에서 `g` 는 한 줄의 모든 매치. macOS(BSD sed) 는 `-i ""` 처럼 백업 확장자 인수가 필요합니다.',
    example: 'CI 에서 이미지 태그 교체: `sed -i "s/image: app:.*/image: app:${TAG}/" k8s/deploy.yaml`.' },
  { diff: 'normal', type: 'mcq', q: '`awk -F: \'{print $1}\' /etc/passwd` 의 출력은?', options: ['모든 사용자의 홈 디렉터리', '모든 사용자명', '모든 사용자의 UID', '모든 사용자의 셸'], answer: 1,
    explain: '`-F:` 로 구분자를 `:` 로 지정, `$1` 은 첫 필드 = 사용자명. `/etc/passwd` 형식: `name:x:uid:gid:comment:home:shell`.',
    example: '`awk \'{print $1, $3}\'` 로 로그에서 IP 와 상태코드만 뽑거나, `awk \'$9 >= 500\'` 처럼 조건 필터도 가능합니다.' },
  { diff: 'normal', type: 'short', q: '원격 서버 `web01` 의 `/var/log/app.log` 를 로컬 현재 디렉터리로 복사하는 scp 명령어는? (사용자: deploy)', answer: 'scp deploy@web01:/var/log/app.log .', accept: ['scp deploy@web01:/var/log/app.log ./', 'scp deploy@web01:/var/log/app.log ./app.log'],
    explain: '`scp [옵션] 원본 대상`, 원격은 `user@host:path`. `-r` 로 디렉터리, `-P` 로 포트 지정. 대용량/증분은 `rsync -avz` 가 낫습니다.',
    example: '로컬 → 서버 업로드는 반대로 `scp build.tar.gz deploy@web01:/tmp/`.' },
  { diff: 'normal', type: 'ox', q: '`/etc/fstab` 은 부팅 시 자동으로 마운트할 파일시스템 목록을 정의한다.', answer: true,
    explain: 'fstab 각 줄은 `장치 마운트지점 타입 옵션 dump pass` 형식입니다. 오타가 있으면 부팅이 멈출 수 있어 `mount -a` 로 먼저 검증합니다.',
    example: 'EBS 볼륨을 추가한 뒤 `UUID=xxxx /data ext4 defaults,nofail 0 2` 를 fstab 에 추가합니다. `nofail` 은 볼륨이 없어도 부팅되게 합니다.' },
  { diff: 'normal', type: 'short', q: '실행 중인 셸 세션에서 `~/.bashrc` 를 다시 읽어 적용하는 명령어는?', answer: 'source ~/.bashrc', accept: ['. ~/.bashrc'],
    explain: '`source` (또는 `.`)는 파일을 현재 셸에서 실행합니다. `bash ~/.bashrc` 는 자식 셸에서 실행되어 현재 셸에 영향이 없습니다.',
    example: 'alias 나 PATH 를 추가한 뒤 재로그인 없이 `source ~/.bashrc` 로 즉시 반영.' },

  /* ---------------- HARD ---------------- */
  { diff: 'hard', type: 'short', q: '파일 `secret.txt` 에 setuid 는 없이 소유자 rw, 그룹 r, 기타 없음 권한을 **심볼릭 모드** 로 설정하는 chmod 명령어는?', answer: 'chmod u=rw,g=r,o= secret.txt', accept: ['chmod u=rw,g=r,o-rwx secret.txt', 'chmod 640 secret.txt', 'chmod u=rw,g=r,o-r secret.txt'],
    explain: '심볼릭 모드: `u`(user) `g`(group) `o`(other) `a`(all), `=` 설정, `+` 추가, `-` 제거. `o=` 는 기타 권한 전부 제거. 숫자로는 640.',
    example: '기존 권한을 유지하며 일부만 바꿀 때 심볼릭이 편합니다: `chmod -R g+w shared/`, `chmod o-rwx ~/.ssh`.' },
  { diff: 'hard', type: 'mcq', q: '디렉터리에 sticky bit (`chmod +t`) 가 설정되었을 때의 효과는?', options: ['디렉터리가 삭제되지 않음', '파일 소유자(또는 root)만 자신의 파일을 삭제/이름변경 가능', '모든 파일이 실행 권한을 가짐', '디렉터리가 메모리에 고정됨'], answer: 1,
    explain: '`/tmp` 처럼 여러 사용자가 쓰는 공유 디렉터리에서 남의 파일을 지우지 못하게 합니다. `ls -ld /tmp` 에서 `drwxrwxrwt` 의 `t` 로 확인.',
    example: '공유 업로드 디렉터리: `chmod 1777 /shared/uploads` → 누구나 쓰지만 자기 파일만 삭제 가능.' },
  { diff: 'hard', type: 'short', q: '`lsof` 로 삭제되었지만 아직 프로세스가 열고 있어 디스크 공간을 차지하는 파일을 찾는 명령어는?', answer: 'lsof | grep deleted', accept: ['lsof +L1', 'lsof -nP | grep deleted', 'lsof -nP +L1', 'sudo lsof | grep deleted', 'lsof | grep "(deleted)"'],
    explain: '`df` 는 꽉 찼는데 `du` 합계가 작으면 이 경우입니다. `lsof +L1` 은 link count 가 1 미만인 (삭제된) 열린 파일을 표시합니다.',
    example: '로그 파일을 `rm` 했는데 공간이 안 돌아오는 경우 → 해당 프로세스를 재시작하거나 `> /proc/PID/fd/N` 으로 비웁니다. 로그는 `rm` 대신 `truncate -s 0` 이나 `: > file` 로 비우는 게 안전합니다.' },
  { diff: 'hard', type: 'mcq', q: '`ulimit -n` 이 출력하는 값은?', options: ['최대 프로세스 수', '프로세스당 열 수 있는 최대 파일 디스크립터 수', '최대 스택 크기', '최대 코어 덤프 크기'], answer: 1,
    explain: '소켓도 fd 이므로 고부하 서버는 "Too many open files" 오류를 겪습니다. `/etc/security/limits.conf` 나 systemd 유닛의 `LimitNOFILE=` 로 올립니다.',
    example: 'nginx/Node 서버 유닛 파일에 `[Service] LimitNOFILE=65535` 를 넣고 `systemctl daemon-reload && systemctl restart`.' },
  { diff: 'hard', type: 'short', q: 'systemd 유닛 파일을 수정한 뒤, 변경 사항을 systemd 에 다시 읽히는 명령어는?', answer: 'systemctl daemon-reload', accept: ['sudo systemctl daemon-reload'],
    explain: '유닛 파일(`/etc/systemd/system/*.service`)을 편집해도 daemon-reload 전까지 systemd 는 옛 정의를 사용합니다. 이후 `restart` 가 필요합니다.',
    example: '`Environment=` 나 `ExecStart=` 수정 → `systemctl daemon-reload && systemctl restart myapp`. 경고 메시지 "changed on disk. Run daemon-reload" 가 힌트입니다.' },
  { diff: 'hard', type: 'ox', q: '`kill -9` 로 종료한 프로세스는 SIGKILL 을 핸들링해 파일을 정리(cleanup)한 뒤 종료된다.', answer: false,
    explain: 'SIGKILL(9)과 SIGSTOP(19)은 프로세스가 가로챌 수 없습니다. 커널이 즉시 종료시키므로 임시 파일, 락 파일, 미기록 버퍼가 남습니다. 먼저 SIGTERM(15)을 보내야 합니다.',
    example: '데이터베이스를 `kill -9` 하면 WAL 복구가 필요해지거나 락 파일이 남아 재시작을 막습니다. Kubernetes 도 SIGTERM → `terminationGracePeriodSeconds` 후 SIGKILL 순서입니다.' },
  { diff: 'hard', type: 'short', q: '실행 중인 PID 4321 프로세스가 열고 있는 파일과 소켓 목록을 보는 명령어는?', answer: 'lsof -p 4321', accept: ['ls -l /proc/4321/fd', 'sudo lsof -p 4321', 'ls /proc/4321/fd'],
    explain: '`lsof -p PID` 또는 `/proc/PID/fd` 디렉터리를 직접 봅니다. `/proc/PID/` 아래에는 `cmdline`, `environ`, `status`, `cwd` 등 프로세스의 모든 정보가 있습니다.',
    example: '파일 디스크립터 누수 의심 시 `ls /proc/PID/fd | wc -l` 을 주기적으로 찍어 증가 여부를 봅니다.' },
  { diff: 'hard', type: 'mcq', q: '`rsync -avz --delete src/ dest/` 에서 `--delete` 의 의미는?', options: ['전송 후 src 를 삭제', 'dest 에만 있고 src 에 없는 파일을 삭제', '전송 전 dest 를 모두 비움', '심볼릭 링크를 삭제'], answer: 1,
    explain: '`--delete` 는 dest 를 src 와 동일하게 미러링합니다. `src/` 끝의 슬래시는 "디렉터리 내용" 을 의미 (없으면 `dest/src/` 로 복사됨). `-a` 아카이브(권한·시간 유지), `-v` 상세, `-z` 압축.',
    example: '정적 사이트 배포: `rsync -avz --delete ./dist/ deploy@web:/var/www/site/`. 먼저 `-n`(dry-run)으로 삭제 목록을 확인합니다.' },
  { diff: 'hard', type: 'short', q: '현재 시스템의 부하 평균(load average)과 가동 시간을 한 줄로 보여주는 명령어는?', answer: 'uptime', accept: ['w', 'cat /proc/loadavg'],
    explain: 'load average 는 1/5/15분 동안 실행 중 + 실행 대기(+ 비중단 I/O 대기) 프로세스의 평균 수입니다. CPU 코어 수(`nproc`)와 비교해서 해석합니다.',
    example: '4코어 서버에서 load 가 8 이면 과부하. I/O 대기가 원인이면 CPU 사용률은 낮은데 load 만 높게 나옵니다 (`iostat`, `vmstat 1` 로 확인).' },
  { diff: 'hard', type: 'mcq', q: '`vmstat 1` 출력에서 `wa` 컬럼이 지속적으로 높을 때 의미는?', options: ['CPU 가 사용자 코드 실행에 바쁨', 'CPU 가 디스크 I/O 완료를 기다리며 놀고 있음', '메모리 스왑이 발생 중', '인터럽트가 많음'], answer: 1,
    explain: '`wa` (iowait) 는 CPU 가 I/O 를 기다리는 시간 비율. 디스크 병목 신호입니다. `si/so` 는 스왑 in/out, `us/sy` 는 user/system CPU.',
    example: 'DB 서버에서 `wa` 가 30% 이상이면 `iostat -x 1` 로 `%util` 과 `await` 를 확인하고 디스크 업그레이드나 쿼리 최적화를 검토합니다.' },
  { diff: 'hard', type: 'short', q: '`/etc/sudoers` 를 문법 검사와 함께 안전하게 편집하는 명령어는?', answer: 'visudo', accept: ['sudo visudo'],
    explain: '`visudo` 는 파일을 잠그고 저장 시 문법을 검증합니다. 직접 편집해 문법 오류가 나면 sudo 자체가 불능이 될 수 있습니다. `/etc/sudoers.d/` 에 조각 파일로 두는 것이 권장됩니다.',
    example: '배포 계정에 재시작만 허용: `deploy ALL=(root) NOPASSWD: /bin/systemctl restart myapp` 를 `visudo -f /etc/sudoers.d/deploy` 로 작성.' },
  { diff: 'hard', type: 'ox', q: '하드 링크는 원본 파일이 삭제되어도 데이터에 계속 접근할 수 있지만, 심볼릭 링크는 끊어진(dangling) 링크가 된다.', answer: true,
    explain: '하드 링크는 같은 inode 를 가리키는 또 다른 이름입니다. 링크 수가 0 이 될 때만 데이터가 해제됩니다. 심볼릭 링크는 경로 문자열만 저장합니다.',
    example: '백업 툴(rsnapshot 등)이 `--link-dest` 로 변경 없는 파일을 하드링크해 공간을 절약합니다. `ls -li` 로 inode 번호와 링크 수를 확인할 수 있습니다.' },
  { diff: 'hard', type: 'short', q: '텍스트 파일 `ips.txt` 에서 중복을 제거하고 각 값의 출현 횟수를 많은 순으로 정렬해 출력하는 파이프라인은?', answer: 'sort ips.txt | uniq -c | sort -rn', accept: ['sort ips.txt | uniq -c | sort -nr', 'cat ips.txt | sort | uniq -c | sort -rn', 'cat ips.txt | sort | uniq -c | sort -nr', 'sort ips.txt | uniq -c | sort -k1,1nr'],
    explain: '`uniq` 는 **인접한** 중복만 제거하므로 먼저 `sort` 가 필요합니다. `-c` 는 횟수 출력, `sort -rn` 은 숫자 내림차순.',
    example: '접근 로그에서 요청이 많은 IP Top 10: `awk \'{print $1}\' access.log | sort | uniq -c | sort -rn | head`.' },
  { diff: 'hard', type: 'mcq', q: '`set -euo pipefail` 을 bash 스크립트 상단에 넣는 이유로 옳지 않은 것은?', options: ['명령 실패 시 즉시 종료 (-e)', '정의되지 않은 변수 사용 시 에러 (-u)', '파이프라인 중간 명령 실패도 실패로 처리 (pipefail)', '스크립트를 root 권한으로 실행 (-o)'], answer: 3,
    explain: '`-o pipefail` 은 파이프라인의 종료 코드를 마지막 명령이 아닌 실패한 명령 기준으로 만듭니다. `-o` 자체는 옵션 설정 구문일 뿐 권한과 무관합니다.',
    example: 'CI 스크립트에서 `curl ... | tar xz` 가 실패했는데도 tar 가 성공해 배포가 "성공" 으로 표시되는 사고를 pipefail 이 막아줍니다.' },
  { diff: 'hard', type: 'short', q: '커널 파라미터 `net.core.somaxconn` 의 현재 값을 확인하는 명령어는?', answer: 'sysctl net.core.somaxconn', accept: ['cat /proc/sys/net/core/somaxconn', 'sysctl -n net.core.somaxconn'],
    explain: '`sysctl` 은 `/proc/sys/` 아래 커널 파라미터를 읽고 씁니다. `sysctl -w key=value` 로 즉시 변경, 영구 적용은 `/etc/sysctl.d/*.conf` 에 기록 후 `sysctl --system`.',
    example: '고트래픽 웹 서버 튜닝: `net.core.somaxconn=65535`, `net.ipv4.ip_local_port_range="1024 65535"`, `vm.swappiness=10` 등.' },

  /* ---------------- EXTREME ---------------- */
  { diff: 'extreme', type: 'mcq', q: '`df -h` 는 90% 사용인데 `du -sh /` 합계는 40% 수준이다. `lsof +L1` 에도 삭제된 파일이 없다면 다음으로 의심할 것은?', options: ['inode 고갈', '마운트 포인트 아래에 가려진(hidden) 파일 — 다른 파일시스템이 그 위에 마운트됨', '파일시스템 저널 손상', 'du 의 버그'], answer: 1,
    explain: '예: `/data` 에 파일을 쓴 뒤 나중에 그 위에 볼륨을 마운트하면 원래 파일은 접근 불가하지만 루트 파일시스템 공간은 차지합니다. `mount --bind / /mnt/root` 로 원본 루트를 다른 곳에 바인드해 확인합니다. inode 고갈은 `df -i` 로 별도 확인.',
    example: '도커 데이터 디렉터리를 별도 볼륨으로 옮길 때 기존 `/var/lib/docker` 내용을 지우지 않고 그 위에 마운트해 수십 GB 가 "사라지는" 사고가 흔합니다.' },
  { diff: 'extreme', type: 'short', q: 'OOM killer 가 어떤 프로세스를 죽였는지 커널 로그에서 확인하는 명령어는? (dmesg 사용)', answer: 'dmesg | grep -i "out of memory"', accept: ['dmesg | grep -i oom', 'dmesg -T | grep -i oom', 'dmesg | grep -i killed', 'dmesg -T | grep -i "out of memory"', 'dmesg | grep -i "killed process"', 'journalctl -k | grep -i oom', 'sudo dmesg | grep -i oom'],
    explain: 'OOM killer 는 `oom_score` 가 높은 프로세스를 골라 SIGKILL 합니다. 로그에 "Out of memory: Killed process 1234 (java)" 형태로 남습니다. `dmesg -T` 는 사람이 읽을 수 있는 시각을 표시합니다.',
    example: '중요 프로세스를 보호하려면 `echo -1000 > /proc/PID/oom_score_adj`. 컨테이너에서는 메모리 limit 초과 시 cgroup OOM 으로 같은 일이 일어나며 `kubectl describe pod` 에 `OOMKilled` 로 표시됩니다.' },
  { diff: 'extreme', type: 'mcq', q: '`strace -f -e trace=network -p PID` 명령어가 하는 일은?', options: ['프로세스의 네트워크 대역폭을 측정', '프로세스(와 자식들)가 호출하는 네트워크 관련 시스템 콜을 실시간 추적', '프로세스의 네트워크 접근을 차단', '프로세스의 열린 포트를 출력'], answer: 1,
    explain: '`strace` 는 시스템 콜을 추적합니다. `-f` 자식 프로세스 포함, `-e trace=network` 로 `connect/sendto/recvfrom` 등만 필터, `-p` 로 실행 중인 프로세스에 부착. 오버헤드가 크므로 운영 환경에선 짧게 씁니다.',
    example: '앱이 어떤 IP/포트로 연결을 시도하다 타임아웃 나는지 소스코드 없이 확인할 때. 파일 접근 문제는 `-e trace=file` 로 어떤 경로를 열려다 ENOENT 가 나는지 봅니다.' },
  { diff: 'extreme', type: 'short', q: 'PID 1234 프로세스의 현재 cgroup(v2) 메모리 사용량을 확인하려면 어느 파일을 읽어야 하는가? (프로세스의 cgroup 경로가 `/sys/fs/cgroup/myapp` 라고 가정)', answer: '/sys/fs/cgroup/myapp/memory.current', accept: ['cat /sys/fs/cgroup/myapp/memory.current', 'memory.current'],
    explain: 'cgroup v2 에서 `memory.current` 는 현재 사용량, `memory.max` 는 한도, `memory.events` 에 `oom_kill` 카운트가 있습니다. 프로세스의 cgroup 은 `cat /proc/PID/cgroup` 으로 확인합니다.',
    example: '컨테이너 안에서 `free` 는 호스트 메모리를 보여주므로 실제 한도는 `/sys/fs/cgroup/memory.max` 를 읽어야 합니다. JVM 은 `-XX:+UseContainerSupport` 로 이를 인식합니다.' },
  { diff: 'extreme', type: 'ox', q: '`nice -n 19 cmd` 로 실행한 프로세스는 CPU 뿐 아니라 디스크 I/O 우선순위도 함께 낮아진다.', answer: false,
    explain: '`nice` 는 CPU 스케줄링 우선순위만 조정합니다. I/O 우선순위는 `ionice -c 3` (idle 클래스) 로 따로 설정해야 합니다. 단, CFQ/BFQ 스케줄러가 아니면 ionice 효과가 제한적입니다.',
    example: '운영 중인 DB 서버에서 백업 시 `nice -n 19 ionice -c 3 tar czf backup.tgz /var/lib/db` 로 서비스 영향을 최소화합니다.' },
  { diff: 'extreme', type: 'mcq', q: '프로세스가 D 상태(uninterruptible sleep)로 오래 멈춰 있고 `kill -9` 도 듣지 않는다. 가장 가능성 높은 원인은?', options: ['프로세스가 SIGKILL 을 핸들링 중', '커널 공간에서 I/O(예: NFS, 고장난 디스크) 완료를 기다리는 중', '프로세스가 좀비 상태', 'CPU 가 100% 점유되어 시그널 처리 불가'], answer: 1,
    explain: 'D 상태는 커널 내부 I/O 대기라 시그널이 전달되지 않습니다. NFS 서버 다운, 스토리지 장애가 전형적 원인입니다. `cat /proc/PID/stack` 이나 `wchan` 필드로 어디서 대기하는지 봅니다.',
    example: 'NFS 마운트가 끊긴 서버에서 `ls /mnt/nfs` 를 친 셸이 영원히 멈추는 사례. NFS 는 `soft,timeo=` 옵션이나 `umount -f -l` 로 대응합니다.' },
];
