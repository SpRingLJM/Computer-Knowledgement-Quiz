window.QUIZ_BANK.linux.push(
  /* ---------------- EASY ---------------- */
  { diff: 'easy', type: 'short', q: '긴 텍스트 파일 `app.log` 를 페이지 단위로 넘기며 보는(스크롤·검색 가능) 명령어는?', answer: 'less app.log', accept: ['more app.log', 'less ./app.log'],
    explain: '`less` 는 파일을 메모리에 다 올리지 않아 큰 로그도 빠릅니다. `/패턴` 검색, `G` 끝으로, `q` 종료, `F` 로 tail -f 처럼 따라가기.',
    example: '`journalctl` 이나 `git log` 도 기본 페이저로 less 를 씁니다. 큰 로그는 `cat` 대신 `less` 가 터미널을 살립니다.' },
  { diff: 'easy', type: 'short', q: '빈 파일 `.gitkeep` 을 만들거나 기존 파일의 수정 시각을 현재로 갱신하는 명령어는?', answer: 'touch .gitkeep', accept: ['touch ./.gitkeep'],
    explain: '`touch` 는 없으면 빈 파일을 만들고, 있으면 atime/mtime 만 갱신합니다.',
    example: '빈 디렉터리를 git 에 올리기 위해 `touch logs/.gitkeep`, Makefile 의존성 강제 재빌드에 `touch src/main.c`.' },
  { diff: 'easy', type: 'mcq', q: '디렉터리 `src` 를 하위 내용까지 통째로 `backup/` 에 복사하는 명령어는?', options: ['cp src backup/', 'cp -r src backup/', 'cp -a src', 'copy src backup/'], answer: 1,
    explain: '디렉터리 복사는 `-r`(재귀)이 필요합니다. `-a` 는 `-r` + 권한·시간·심볼릭 링크 보존(아카이브)이라 백업에 적합.',
    example: '설정 디렉터리 백업: `cp -a /etc/nginx /etc/nginx.bak.$(date +%F)`.' },
  { diff: 'easy', type: 'short', q: '현재 로그인한 사용자 이름을 출력하는 명령어는?', answer: 'whoami', accept: ['id -un', 'echo $USER'],
    explain: '`whoami` 는 유효 사용자명, `id` 는 UID/GID/그룹까지 출력합니다.',
    example: '`sudo -i` 후 정말 root 인지 `whoami` 로 확인, 스크립트에서 `[ "$(id -u)" -eq 0 ] || exit 1` 로 root 검사.' },
  { diff: 'easy', type: 'ox', q: '`history` 명령어는 현재 셸에서 이전에 실행한 명령 목록을 보여준다.', answer: true,
    explain: '`~/.bash_history` 에 저장됩니다. `!123` 으로 재실행, `Ctrl+R` 로 역검색. 비밀번호가 들어간 명령은 앞에 공백을 두면(`HISTCONTROL=ignorespace`) 기록되지 않습니다.',
    example: '`history | grep docker` 로 예전에 썼던 긴 docker 명령을 찾습니다.' },
  { diff: 'easy', type: 'short', q: '파일 `data.csv` 의 처음 20줄만 출력하는 명령어는?', answer: 'head -n 20 data.csv', accept: ['head -20 data.csv', 'head -n20 data.csv'],
    explain: '`head` 는 앞부분, `tail` 은 뒷부분. 기본 10줄. `-c` 로 바이트 단위.',
    example: '큰 CSV 의 헤더와 샘플만 확인: `head -n 5 data.csv`. 헤더 제외하고 처리: `tail -n +2 data.csv`.' },
  { diff: 'easy', type: 'mcq', q: '`python3` 실행 파일이 어느 경로에 있는지 찾는 명령어는?', options: ['find python3', 'which python3', 'locate -b python3', 'ls python3'], answer: 1,
    explain: '`which` 는 PATH 에서 첫 번째로 발견되는 실행 파일 경로를 보여줍니다. `type python3` 은 alias/함수/빌트인까지 구분해 줍니다.',
    example: 'pyenv/venv 가 제대로 활성화됐는지 `which python3` 로 경로 확인.' },
  { diff: 'easy', type: 'short', q: '두 파일 `a.conf` 와 `b.conf` 의 차이를 줄 단위로 비교하는 명령어는?', answer: 'diff a.conf b.conf', accept: ['diff -u a.conf b.conf', 'diff -y a.conf b.conf'],
    explain: '`-u` 는 unified 형식(git diff 와 같은 형태), `-y` 는 좌우 나란히. 차이가 없으면 아무것도 출력하지 않고 종료 코드 0.',
    example: '배포 전 설정 변경 확인: `diff -u /etc/nginx/nginx.conf nginx.conf.new`.' },
  { diff: 'easy', type: 'ox', q: '`ls -lh` 는 파일 크기를 K, M, G 단위로 사람이 읽기 쉽게 표시한다.', answer: true,
    explain: '`-h` (human-readable) 은 `ls`, `du`, `df`, `free` 등에 공통으로 있습니다. `-S` 로 크기순 정렬, `-t` 로 시간순.',
    example: '큰 파일 찾기: `ls -lhS /var/log | head`.' },
  { diff: 'easy', type: 'short', q: '`/etc` 아래에서 이름이 `.conf` 로 끝나는 파일을 모두 찾는 명령어는?', answer: 'find /etc -name "*.conf"', accept: ["find /etc -name '*.conf'", 'find /etc -type f -name "*.conf"', "find /etc -type f -name '*.conf'", 'find /etc -iname "*.conf"'],
    explain: '`find 경로 조건`. `-name` 은 대소문자 구분, `-iname` 은 무시. 와일드카드는 셸이 먼저 풀지 않도록 따옴표로 감쌉니다.',
    example: '`find . -name "*.py" -not -path "./venv/*"` 처럼 특정 경로 제외도 가능합니다.' },
  { diff: 'easy', type: 'mcq', q: '`ps aux | grep nginx` 결과에 grep 명령 자체가 함께 나오는 것을 피하는 방법은?', options: ['grep -v grep 을 붙이거나 `grep [n]ginx` 사용, 또는 `pgrep -a nginx`', 'sudo 로 실행', 'ps -ef 로 바꾸기', '피할 수 없음'], answer: 0,
    explain: '`[n]ginx` 정규식은 `nginx` 에 매칭되지만 명령줄 문자열 `[n]ginx` 자체에는 매칭되지 않는 트릭입니다. `pgrep` 이 더 깔끔합니다.',
    example: '스크립트에서 프로세스 존재 확인: `pgrep -x nginx > /dev/null && echo running`.' },
  { diff: 'easy', type: 'short', q: '시스템을 즉시 재부팅하는 명령어는?', answer: 'sudo reboot', accept: ['reboot', 'sudo systemctl reboot', 'systemctl reboot', 'sudo shutdown -r now', 'shutdown -r now'],
    explain: '`reboot`, `systemctl reboot`, `shutdown -r now` 모두 동일. `shutdown -h now` 는 종료, `shutdown -r +5` 는 5분 뒤 재부팅(사용자에게 알림).',
    example: '커널 업데이트 후 `needs-restarting -r` (RHEL) 또는 `/var/run/reboot-required` (Ubuntu) 확인 후 재부팅.' },
  { diff: 'easy', type: 'short', q: '현재 시스템의 호스트 이름을 출력하는 명령어는?', answer: 'hostname', accept: ['hostnamectl', 'cat /etc/hostname', 'uname -n'],
    explain: '`hostnamectl` 은 systemd 환경에서 호스트명 조회/변경(`hostnamectl set-hostname web01`)을 담당합니다.',
    example: '여러 서버에 SSH 로 들어가 작업할 때 프롬프트나 `hostname` 으로 어느 서버인지 확인하는 습관.' },
  { diff: 'easy', type: 'ox', q: '셸에서 `~` 는 현재 사용자의 홈 디렉터리를 뜻하며, `cd` 만 입력하면 홈으로 이동한다.', answer: true,
    explain: '`~alice` 는 alice 의 홈. `cd -` 는 직전 디렉터리로 돌아갑니다. `$HOME` 환경변수와 동일.',
    example: '`cp config.yaml ~/backup/`, `cd -` 로 두 디렉터리를 오가며 작업.' },

  /* ---------------- NORMAL ---------------- */
  { diff: 'normal', type: 'short', q: '현재 디렉터리의 각 항목 크기를 구해 큰 순서대로 정렬하는 파이프라인은?', answer: 'du -sh * | sort -rh', accept: ['du -sh * | sort -hr', 'du -sh ./* | sort -rh', 'du -sh * | sort -h -r', 'du -sh -- * | sort -rh'],
    explain: '`du -sh *` 로 항목별 합계, `sort -h` 는 K/M/G 단위를 이해하는 정렬, `-r` 역순. 숨김 파일 포함하려면 `du -sh .[!.]* *`.',
    example: '디스크 정리 시 `cd /var && du -sh * | sort -rh | head` 로 어느 디렉터리가 큰지 내려가며 추적합니다.' },
  { diff: 'normal', type: 'mcq', q: '`chmod 644 file` 의 결과 권한은?', options: ['rwxr--r--', 'rw-r--r--', 'rw-rw-r--', 'r--r--r--'], answer: 1,
    explain: '6 = rw-, 4 = r--. 일반 파일의 표준 권한(소유자 읽기/쓰기, 나머지 읽기). 디렉터리는 진입에 x 가 필요해 755 가 표준.',
    example: '웹 정적 파일 `644`, 스크립트 `755`, 비밀키 `600`, 공유 디렉터리 `775`.' },
  { diff: 'normal', type: 'short', q: '`www-data` 사용자 권한으로 `php artisan migrate` 를 실행하는 명령어는?', answer: 'sudo -u www-data php artisan migrate', accept: ['sudo -u www-data -- php artisan migrate', 'su -s /bin/bash www-data -c "php artisan migrate"', 'runuser -u www-data -- php artisan migrate'],
    explain: '`sudo -u 사용자 명령`. 서비스 계정으로 실행해야 생성되는 파일의 소유자가 올바릅니다. 서비스 계정은 보통 로그인 셸이 없어 `su - www-data` 는 실패합니다.',
    example: 'root 로 마이그레이션을 돌리면 캐시 파일이 root 소유가 되어 웹 서버가 쓰지 못하는 흔한 사고를 예방합니다.' },
  { diff: 'normal', type: 'short', q: '홈 디렉터리와 bash 셸을 가진 사용자 `alice` 를 생성하는 명령어는?', answer: 'useradd -m -s /bin/bash alice', accept: ['sudo useradd -m -s /bin/bash alice', 'useradd -s /bin/bash -m alice', 'adduser alice', 'sudo adduser alice', 'useradd -m alice -s /bin/bash'],
    explain: '`useradd` 는 저수준 명령으로 `-m` 없이는 홈이 생성되지 않습니다. Debian 계열의 `adduser` 는 대화형 래퍼로 홈·비밀번호까지 처리합니다. 비밀번호는 `passwd alice`.',
    example: '배포 계정 생성: `useradd -m -s /bin/bash deploy && mkdir -p ~deploy/.ssh && ...` 로 키 기반 로그인 설정.' },
  { diff: 'normal', type: 'short', q: '사용자 `alice` 를 기존 그룹을 유지한 채 `docker` 그룹에 추가하는 명령어는?', answer: 'usermod -aG docker alice', accept: ['sudo usermod -aG docker alice', 'usermod -a -G docker alice', 'sudo usermod -a -G docker alice', 'gpasswd -a alice docker', 'sudo gpasswd -a alice docker'],
    explain: '`-a`(append) 를 빼면 `-G` 가 보조 그룹 목록을 **교체**해 기존 그룹(sudo 등)에서 빠집니다. 변경은 재로그인 후 적용(`newgrp docker` 로 즉시 반영).',
    example: 'docker 설치 후 `sudo` 없이 쓰기 위한 표준 절차. docker 그룹은 사실상 root 권한이므로 신중히 부여합니다.' },
  { diff: 'normal', type: 'ox', q: '하드 링크는 디렉터리에는 만들 수 없고, 다른 파일시스템(파티션)의 파일을 가리킬 수도 없다.', answer: true,
    explain: '하드 링크는 같은 파일시스템의 inode 를 공유하므로 파티션을 넘을 수 없고, 디렉터리 순환을 막기 위해 디렉터리 하드링크는 금지됩니다. 이런 경우 심볼릭 링크를 씁니다.',
    example: '`ln /mnt/data/a.txt ~/a.txt` 가 "Invalid cross-device link" 로 실패하면 `ln -s` 로 대체.' },
  { diff: 'normal', type: 'short', q: '`ll` 을 입력하면 `ls -la` 가 실행되도록 별칭을 정의하는 명령어는?', answer: "alias ll='ls -la'", accept: ['alias ll="ls -la"', 'alias ll=ls -la', "alias ll='ls -al'"],
    explain: '`alias 이름=\'명령\'`. 현재 셸에만 유효하므로 영구화는 `~/.bashrc` 에 추가. `unalias ll` 로 제거, `alias` 만 치면 목록.',
    example: '`alias k=kubectl`, `alias gs=\'git status\'` 처럼 자주 쓰는 명령을 줄입니다. 스크립트에서는 alias 가 확장되지 않으니 함수를 씁니다.' },
  { diff: 'normal', type: 'mcq', q: '`cmd 2>/dev/null` 의 의미는?', options: ['표준 출력을 버린다', '표준 에러를 버린다', '둘 다 버린다', '표준 에러를 파일에 저장한다'], answer: 1,
    explain: 'fd 2 = stderr, `/dev/null` 은 블랙홀 장치. stdout 만 버리려면 `>/dev/null`, 둘 다는 `>/dev/null 2>&1` 또는 `&>/dev/null`.',
    example: '`find / -name "*.log" 2>/dev/null` 로 "Permission denied" 잡음을 숨기고 결과만 봅니다.' },
  { diff: 'normal', type: 'short', q: '`find` 로 찾은 `.log` 파일들을 `xargs` 를 통해 삭제하는 파이프라인은? (파일명에 공백이 있어도 안전하게)', answer: 'find . -name "*.log" -print0 | xargs -0 rm', accept: ["find . -name '*.log' -print0 | xargs -0 rm", 'find . -name "*.log" -print0 | xargs -0 rm -f', 'find . -name "*.log" | xargs rm', 'find . -name "*.log" -print0 | xargs -0 rm -v', 'find . -type f -name "*.log" -print0 | xargs -0 rm'],
    explain: '`-print0` 과 `xargs -0` 은 NUL 문자로 구분해 공백·개행이 든 파일명도 안전합니다. `xargs` 는 인수를 묶어 명령을 적게 실행해 `-exec rm {} \\;` 보다 빠릅니다.',
    example: '`git ls-files | xargs wc -l`, `cat urls.txt | xargs -n1 -P4 curl -sO` (병렬 4개 다운로드).' },
  { diff: 'normal', type: 'short', q: '디렉터리 `app/` 을 gzip 압축된 tar 파일 `app.tar.gz` 로 만드는 명령어는?', answer: 'tar -czf app.tar.gz app/', accept: ['tar czf app.tar.gz app/', 'tar -czvf app.tar.gz app/', 'tar czvf app.tar.gz app/', 'tar -zcf app.tar.gz app/', 'tar -zcvf app.tar.gz app/', 'tar -czf app.tar.gz app', 'tar czf app.tar.gz app', 'tar -cvzf app.tar.gz app/', 'tar cvzf app.tar.gz app/'],
    explain: '`c` 생성, `z` gzip, `f` 파일명. `-f` 바로 뒤에 파일명이 와야 하므로 `-czf` 순서를 지킵니다(`-fcz` 는 오류). `--exclude=node_modules` 로 제외.',
    example: '릴리스 아티팩트: `tar -czf release-${VERSION}.tar.gz --exclude=.git --exclude=node_modules .`' },
  { diff: 'normal', type: 'mcq', q: '`df -h` 는 여유 공간이 충분한데 파일 생성 시 "No space left on device" 가 난다. 확인할 명령어는?', options: ['free -h', 'df -i (inode 고갈)', 'du -sh /', 'lsblk'], answer: 1,
    explain: '파일시스템은 inode 수가 고정되어 있어 작은 파일이 수백만 개면 공간이 남아도 inode 가 고갈됩니다. `df -i` 로 IUse% 확인 후 세션 파일·캐시·메일 큐 등을 정리합니다.',
    example: 'PHP 세션 파일이나 도커 오버레이 레이어가 inode 를 고갈시키는 사례가 흔합니다.' },
  { diff: 'normal', type: 'short', q: '이름이 `nginx` 인 프로세스의 PID 들을 출력하는 명령어는?', answer: 'pgrep nginx', accept: ['pidof nginx', 'pgrep -x nginx', 'ps -C nginx -o pid='],
    explain: '`pgrep` 은 패턴 매칭(`-x` 정확히 일치, `-f` 전체 명령줄, `-u` 사용자), `pidof` 는 정확한 프로그램명. `pkill` 은 같은 방식으로 시그널 전송.',
    example: '`kill -HUP $(pgrep -x nginx | head -1)` 로 마스터 프로세스에 설정 리로드 시그널.' },
  { diff: 'normal', type: 'short', q: '백그라운드로 보낸 작업을 다시 포그라운드로 가져오는 셸 명령어는?', answer: 'fg', accept: ['fg %1', 'fg 1'],
    explain: '`Ctrl+Z` 로 일시정지 → `bg` 로 백그라운드 재개 → `jobs` 로 목록 → `fg %N` 으로 복귀. 셸이 종료되면 작업도 SIGHUP 을 받습니다.',
    example: '`vim` 편집 중 `Ctrl+Z` 로 나가 명령 몇 개 실행하고 `fg` 로 돌아오는 흐름.' },
  { diff: 'normal', type: 'ox', q: '터미널에서 `Ctrl+Z` 를 누르면 실행 중인 프로세스가 종료된다.', answer: false,
    explain: '`Ctrl+Z` 는 SIGTSTP 로 **일시정지**(Stopped)합니다. 종료는 `Ctrl+C`(SIGINT). 정지된 프로세스는 `jobs` 에 남아 있으며 `kill %1` 로 제거합니다.',
    example: '`Ctrl+Z` 로 멈춰둔 프로세스가 포트를 계속 점유해 "address already in use" 가 나는 경우가 있습니다.' },
  { diff: 'normal', type: 'short', q: '파일 `app.log` 의 100번째부터 120번째 줄만 출력하는 `sed` 명령어는?', answer: "sed -n '100,120p' app.log", accept: ['sed -n "100,120p" app.log', 'sed -n 100,120p app.log', 'sed -n 100,120p app.log'],
    explain: '`-n` 은 기본 출력 억제, `100,120p` 는 해당 범위만 출력. `awk \'NR>=100 && NR<=120\'` 또는 `head -120 | tail -21` 도 가능.',
    example: '에러 로그에서 스택트레이스 근처 줄만 뽑아 공유할 때 유용합니다.' },
  { diff: 'normal', type: 'mcq', q: 'SSH 키 쌍을 ed25519 알고리즘으로 생성하는 명령어는?', options: ['ssh-keygen -t ed25519', 'ssh-add ed25519', 'openssl genrsa ed25519', 'ssh-copy-id -t ed25519'], answer: 0,
    explain: 'ed25519 는 짧고 빠르며 현재 권장 알고리즘입니다. `-C "comment"` 로 주석, `-f` 로 파일 경로. 공개키를 서버에 넣는 것은 `ssh-copy-id user@host`.',
    example: '`ssh-keygen -t ed25519 -C "deploy@ci" -f ~/.ssh/deploy_key -N ""` 로 CI 용 키를 비밀번호 없이 생성.' },
  { diff: 'normal', type: 'short', q: '파일 `report.csv` 의 소유 그룹만 `analysts` 로 변경하는 명령어는?', answer: 'chgrp analysts report.csv', accept: ['sudo chgrp analysts report.csv', 'chown :analysts report.csv', 'chown .analysts report.csv'],
    explain: '`chgrp 그룹 파일` 또는 `chown :그룹 파일`. 소유자는 root 만 바꿀 수 있지만, 그룹은 파일 소유자가 자신이 속한 그룹으로 변경 가능합니다.',
    example: '팀 공유 디렉터리: `chgrp -R devteam /srv/shared && chmod -R g+rwX /srv/shared`.' },

  /* ---------------- HARD ---------------- */
  { diff: 'hard', type: 'short', q: 'TCP 8080 포트를 사용하는 프로세스를 찾아 바로 종료하는 `fuser` 명령어는?', answer: 'fuser -k 8080/tcp', accept: ['sudo fuser -k 8080/tcp', 'fuser -k -n tcp 8080', 'fuser -kn tcp 8080'],
    explain: '`fuser` 는 파일·소켓을 사용하는 프로세스를 찾습니다. `-k` 는 SIGKILL 전송(`-TERM` 으로 변경 가능). 대안: `kill $(lsof -t -i :8080)`.',
    example: '개발 서버 재시작 스크립트에서 이전 프로세스가 남아 있을 때 포트를 강제로 비웁니다.' },
  { diff: 'hard', type: 'mcq', q: '`/etc/passwd` 의 두 번째 필드가 `x` 인 이유는?', options: ['비밀번호가 없음', '실제 해시는 root 만 읽을 수 있는 `/etc/shadow` 에 저장되어 있음', '계정이 잠김', 'x 는 암호화된 비밀번호'], answer: 1,
    explain: '`/etc/passwd` 는 모든 사용자가 읽어야 하므로(UID→이름 변환) 해시를 분리했습니다. `/etc/shadow` 에는 해시, 만료일, 잠금 상태(`!` 접두)가 있습니다.',
    example: '계정 잠금: `usermod -L alice` (shadow 의 해시 앞에 `!` 추가), 해제: `usermod -U alice`.' },
  { diff: 'hard', type: 'short', q: '파일 `secret.env` 에 사용자 `alice` 에게만 추가로 읽기·쓰기 권한을 주는 ACL 명령어는?', answer: 'setfacl -m u:alice:rw secret.env', accept: ['setfacl -m u:alice:rw- secret.env', 'sudo setfacl -m u:alice:rw secret.env', 'setfacl -m user:alice:rw secret.env'],
    explain: 'ACL 은 소유자/그룹/기타를 넘어 특정 사용자·그룹별 권한을 부여합니다. `getfacl` 로 확인, `ls -l` 의 권한 뒤 `+` 가 ACL 존재 표시. `-x` 로 제거, `-d` 로 디렉터리 기본 ACL.',
    example: '로그 디렉터리를 소유자 변경 없이 모니터링 에이전트에 읽기 허용: `setfacl -R -m u:datadog:rX /var/log/app`.' },
  { diff: 'hard', type: 'short', q: '시스템 전체에서 SUID 비트가 설정된 실행 파일을 찾는 명령어는?', answer: 'find / -perm -4000 -type f', accept: ['find / -type f -perm -4000', 'find / -perm -4000 -type f 2>/dev/null', 'find / -type f -perm -4000 2>/dev/null', 'find / -perm -u=s -type f', 'find / -type f -perm -u=s', 'find / -perm /4000 -type f', 'sudo find / -perm -4000 -type f'],
    explain: 'SUID(4000) 파일은 실행 시 소유자(보통 root) 권한을 갖습니다. `passwd`, `sudo` 처럼 필요한 것 외의 SUID 는 권한 상승 벡터입니다. `-perm -4000` 은 "해당 비트 포함".',
    example: '보안 감사에서 예상 밖 SUID 바이너리를 찾고 `chmod u-s` 로 제거합니다. 컨테이너 이미지 빌드 시 `find / -perm /6000 -exec chmod a-s {} +` 로 일괄 제거하기도 합니다.' },
  { diff: 'hard', type: 'mcq', q: '`umask 022` 상태에서 새로 만든 일반 파일과 디렉터리의 기본 권한은?', options: ['파일 644, 디렉터리 755', '파일 666, 디렉터리 777', '파일 755, 디렉터리 755', '파일 600, 디렉터리 700'], answer: 0,
    explain: '파일 기본 666, 디렉터리 기본 777 에서 umask 비트를 뺍니다. 022 → 파일 644, 디렉터리 755. `umask 077` 이면 소유자만 접근(600/700).',
    example: '배포 스크립트가 만든 파일을 다른 사용자가 못 읽는다면 서비스 유닛의 `UMask=` 나 셸 umask 를 확인합니다.' },
  { diff: 'hard', type: 'short', q: '최근 SSH 로그인 실패 시도를 systemd 저널에서 확인하는 명령어는?', answer: 'journalctl -u ssh | grep -i failed', accept: ['journalctl -u sshd | grep -i failed', 'journalctl -u ssh | grep -i "failed password"', 'journalctl -u sshd | grep -i "failed password"', 'lastb', 'sudo lastb', 'grep -i "failed password" /var/log/auth.log', 'grep -i failed /var/log/auth.log', 'journalctl _COMM=sshd | grep -i failed', 'grep -i "failed password" /var/log/secure'],
    explain: 'Debian 은 `ssh` 유닛/`/var/log/auth.log`, RHEL 은 `sshd`/`/var/log/secure`. `lastb` 는 `/var/log/btmp` 기반 실패 로그인 목록. `last` 는 성공 로그인.',
    example: '무차별 대입 시도가 많으면 `fail2ban` 을 설치하거나, 비밀번호 인증을 끄고(`PasswordAuthentication no`) 키만 허용합니다.' },
  { diff: 'hard', type: 'short', q: '실행 중인 PID 4321 프로세스에 실제 적용된 리소스 제한(열린 파일 수 등)을 확인하는 명령어는?', answer: 'cat /proc/4321/limits', accept: ['prlimit --pid 4321', 'prlimit -p 4321', 'cat /proc/4321/limits | grep -i "open files"'],
    explain: '`ulimit` 은 현재 셸 기준이라 데몬에 적용된 값과 다를 수 있습니다. `/proc/PID/limits` 가 진실입니다. `prlimit --pid PID --nofile=65535:65535` 로 실행 중 변경도 가능.',
    example: '"Too many open files" 가 나는 프로세스의 실제 한도를 확인한 뒤 systemd 유닛의 `LimitNOFILE` 을 조정합니다.' },
  { diff: 'hard', type: 'ox', q: '`chattr +i file` 로 immutable 속성을 설정하면 root 조차도 그 파일을 수정·삭제·이름 변경할 수 없다 (속성을 해제하기 전까지).', answer: true,
    explain: 'ext4/xfs 의 파일 속성입니다. `lsattr` 로 확인, `chattr -i` 로 해제. `+a` 는 append-only(로그 파일 보호에 적합).',
    example: '`/etc/resolv.conf` 가 계속 덮어써질 때 `chattr +i` 로 고정하거나, 반대로 "root 인데 왜 삭제가 안 되지?" 의 원인이기도 합니다.' },
  { diff: 'hard', type: 'short', q: '`/data` 마운트 포인트의 파일시스템 종류와 마운트 옵션을 확인하는 명령어는?', answer: 'findmnt /data', accept: ['mount | grep /data', 'findmnt -T /data', 'mount | grep " /data "', 'cat /proc/mounts | grep /data', 'grep /data /proc/mounts'],
    explain: '`findmnt` 는 트리 형태로 마운트를 보여주는 현대적 도구. `noexec`, `ro`, `nosuid` 같은 옵션이 예상 밖 동작의 원인일 수 있습니다.',
    example: '`/tmp` 가 `noexec` 로 마운트되어 있어 임시 디렉터리에서 스크립트 실행이 "Permission denied" 로 실패하는 사례.' },
  { diff: 'hard', type: 'mcq', q: '메모리가 충분히 남아 있는데도 스왑 사용이 늘어 지연이 생긴다. 조정할 커널 파라미터는?', options: ['vm.overcommit_memory', 'vm.swappiness (낮출수록 스왑 회피)', 'net.core.somaxconn', 'fs.file-max'], answer: 1,
    explain: '`vm.swappiness` (0-100, 기본 60) 는 페이지 캐시 회수 대비 스왑 선호도입니다. DB 서버는 1~10 으로 낮추는 것이 일반적. `swapoff -a && swapon -a` 로 스왑을 비울 수 있습니다.',
    example: '`sysctl -w vm.swappiness=10` 후 `/etc/sysctl.d/99-swap.conf` 에 영구 기록. Kubernetes 노드는 스왑을 끄는 것이 기본 요구사항입니다.' },
  { diff: 'hard', type: 'short', q: '`/var/log` 아래에서 2024년 6월 1일 이후에 수정된 파일을 찾는 명령어는?', answer: 'find /var/log -newermt "2024-06-01"', accept: ["find /var/log -newermt '2024-06-01'", 'find /var/log -newermt 2024-06-01', 'find /var/log -type f -newermt "2024-06-01"', "find /var/log -type f -newermt '2024-06-01'", 'find /var/log -type f -newermt 2024-06-01'],
    explain: '`-newermt 날짜` 는 지정 시각보다 최근에 수정된 파일. `-mtime -N` 은 "N일 이내" 의 상대 표현. 두 날짜 사이는 `-newermt A ! -newermt B`.',
    example: '장애 시각 전후로 바뀐 설정 파일 추적: `find /etc -newermt "2024-06-01 14:00" ! -newermt "2024-06-01 15:00"`.' },
  { diff: 'hard', type: 'short', q: 'systemd 타이머(cron 대체) 목록과 다음 실행 시각을 보는 명령어는?', answer: 'systemctl list-timers', accept: ['systemctl list-timers --all', 'systemctl list-timers -a'],
    explain: 'systemd 타이머는 `.timer` + `.service` 쌍으로 동작하며, 로그가 journald 에 남고 `OnCalendar=`, `Persistent=true`(놓친 실행 보정) 등이 장점입니다.',
    example: '`logrotate.timer`, `apt-daily.timer` 가 언제 도는지 확인하거나, 백업 잡을 `OnCalendar=*-*-* 03:30:00` 타이머로 등록합니다.' },
  { diff: 'hard', type: 'mcq', q: '프로그램 실행 시 "error while loading shared libraries: libfoo.so.1: cannot open shared object file" 오류의 일반적인 해결책은?', options: ['프로그램을 재컴파일', '라이브러리 설치 후 `ldconfig`, 또는 `LD_LIBRARY_PATH` 에 경로 추가', '실행 권한 부여', 'root 로 실행'], answer: 1,
    explain: '동적 링커는 `/etc/ld.so.conf` 경로와 캐시(`ldconfig`)에서 .so 를 찾습니다. `ldd ./prog` 로 어떤 라이브러리가 "not found" 인지 확인합니다. `LD_LIBRARY_PATH` 는 임시 해결책입니다.',
    example: 'Alpine(musl) 이미지에서 glibc 용 바이너리를 실행할 때 나는 대표적 오류이기도 합니다.' },
  { diff: 'hard', type: 'short', q: '현재 실행 중인 커널 버전을 출력하는 명령어는?', answer: 'uname -r', accept: ['uname -a', 'cat /proc/version'],
    explain: '`uname -r` 커널 릴리스, `-a` 전체 정보, `-m` 아키텍처(x86_64/aarch64). 배포판 버전은 `cat /etc/os-release`.',
    example: '커널 모듈(드라이버)이나 eBPF 도구 호환성 확인, 업데이트 후 새 커널로 부팅됐는지 검증.' },
  { diff: 'hard', type: 'ox', q: '이미 실행 중인 백그라운드 작업은 `disown` 으로 현재 셸의 작업 테이블에서 분리해, 셸을 닫아도 SIGHUP 을 받지 않게 할 수 있다.', answer: true,
    explain: '`nohup` 을 미리 붙이지 못했을 때 사후 처리: `Ctrl+Z` → `bg` → `disown -h %1`. 출력은 여전히 터미널에 묶여 있으므로 리다이렉션이 안 된 경우 주의.',
    example: 'SSH 세션에서 시작한 긴 마이그레이션을 `disown` 한 뒤 접속을 끊고 퇴근하는 상황.' },
  { diff: 'hard', type: 'short', q: '실행 중인 PID 4321 프로세스의 환경변수를 한 줄에 하나씩 출력하는 명령어는?', answer: "cat /proc/4321/environ | tr '\\0' '\\n'", accept: ['cat /proc/4321/environ | tr "\\0" "\\n"', "tr '\\0' '\\n' < /proc/4321/environ", 'strings /proc/4321/environ', 'sudo cat /proc/4321/environ | tr "\\0" "\\n"', "sudo cat /proc/4321/environ | tr '\\0' '\\n'", 'xargs -0 -n1 < /proc/4321/environ', 'cat /proc/4321/environ | xargs -0 -n1'],
    explain: '`environ` 은 NUL 로 구분되어 있어 `tr` 로 개행으로 바꿉니다. 프로세스 시작 시점의 환경이라 이후 변경은 반영되지 않습니다. 같은 사용자 또는 root 만 읽을 수 있습니다.',
    example: '서비스가 엉뚱한 DB 에 붙을 때 실제 프로세스가 어떤 `DATABASE_URL` 로 떠 있는지 확인합니다.' },
  { diff: 'hard', type: 'mcq', q: '`top` 의 CPU 줄에서 `st` (steal) 값이 높을 때 의미는?', options: ['디스크 I/O 대기', '하이퍼바이저가 이 VM 에 CPU 를 할당하지 않고 다른 VM 이 쓰고 있음', '커널 시간이 많음', '인터럽트 과다'], answer: 1,
    explain: 'steal time 은 가상화 환경에서 물리 CPU 를 다른 게스트에 뺏긴 시간입니다. 클라우드의 버스터블(t 계열) 인스턴스에서 크레딧 소진 시나 오버커밋된 호스트에서 높게 나옵니다.',
    example: 'AWS t3 인스턴스에서 CPU 크레딧이 바닥나면 `st` 가 치솟고 앱이 느려집니다 → 크레딧 모니터링 또는 인스턴스 타입 변경.' },
  { diff: 'hard', type: 'short', q: '`rsync` 로 실제 전송 없이 어떤 파일이 복사·삭제될지 미리 보는 옵션은?', answer: '--dry-run', accept: ['-n', '-n / --dry-run', 'rsync -n', 'rsync --dry-run', '-n --dry-run', 'rsync -avn'],
    explain: '`-n`/`--dry-run` 과 `-v` 또는 `-i`(itemize) 를 함께 써서 변경 목록을 확인합니다. `--delete` 를 쓸 때는 반드시 먼저 dry-run 하는 습관이 사고를 막습니다.',
    example: '`rsync -avn --delete ./dist/ web:/var/www/ | head -50` 으로 삭제 목록을 확인한 뒤 `-n` 을 빼고 실행.' },

  /* ---------------- EXTREME ---------------- */
  { diff: 'extreme', type: 'mcq', q: '디스크 `%util` 이 100% 에 가까운데 어떤 프로세스가 I/O 를 일으키는지 찾으려면?', options: ['top 에서 CPU 정렬', 'iotop -o 또는 pidstat -d 1 로 프로세스별 읽기/쓰기 확인', 'df -h', 'lsblk'], answer: 1,
    explain: '`iotop -o` 는 실제 I/O 중인 프로세스만 표시(root 필요), `pidstat -d 1` 은 sysstat 패키지로 초당 프로세스별 kB_rd/kB_wr 을 보여줍니다. 장치 레벨은 `iostat -x 1`.',
    example: '새벽마다 API 가 느려지는 원인이 `updatedb`(mlocate) 나 백업 잡의 디스크 점유임을 `iotop` 으로 찾아 `ionice -c3` 를 적용한 사례.' },
  { diff: 'extreme', type: 'short', q: 'systemd 서비스 유닛에서 프로세스의 열린 파일 수 한도를 65535 로 지정하는 `[Service]` 섹션 설정 줄은?', answer: 'LimitNOFILE=65535',
    explain: 'systemd 서비스는 `/etc/security/limits.conf` 를 **읽지 않습니다**(PAM 로그인 세션용). 유닛 파일의 `Limit*` 지시어가 적용됩니다. 변경 후 `daemon-reload` + `restart`.',
    example: 'nginx, Elasticsearch, Node 서버의 "Too many open files" 대응. `systemctl show nginx -p LimitNOFILE` 로 현재 값 확인.' },
  { diff: 'extreme', type: 'mcq', q: '`dmesg` 에 "TCP: out of memory -- consider tuning tcp_mem" 이 반복된다. 의미와 대응은?', options: ['물리 메모리 부족 → RAM 증설', 'TCP 소켓 버퍼 총량이 `net.ipv4.tcp_mem` 한도를 초과 → 소켓 누수/대량 연결 확인 후 tcp_mem 상향', '디스크 부족', 'NIC 드라이버 오류'], answer: 1,
    explain: '`tcp_mem` 은 페이지 단위의 low/pressure/high 세 값입니다. 커넥션 폭주, 수신 버퍼를 비우지 않는 느린 소비자, 소켓 누수가 원인입니다. `ss -tm` 으로 소켓별 메모리 확인.',
    example: '프록시 서버에서 백엔드 응답을 읽지 않는 클라이언트(느린 다운로드)가 많아 발생 → 타임아웃 단축과 tcp_mem 조정.' },
  { diff: 'extreme', type: 'ox', q: '`:(){ :|:& };:` 같은 fork bomb 은 사용자별 최대 프로세스 수(`ulimit -u`, limits.conf 의 `nproc`)를 제한하면 시스템 전체 마비를 막을 수 있다.', answer: true,
    explain: 'fork bomb 은 프로세스를 기하급수적으로 생성해 PID 와 메모리를 고갈시킵니다. `nproc` 제한, systemd 의 `TasksMax=`, cgroup `pids.max` 가 방어선입니다. 컨테이너는 `--pids-limit`.',
    example: '멀티테넌트 서버에서 `* hard nproc 4096` 을 limits.conf 에 두고, 컨테이너 런타임에 `--pids-limit=512` 를 기본 적용합니다.' },
  { diff: 'extreme', type: 'short', q: '프로세스 크래시 시 코어 덤프 파일이 어디에 어떤 이름으로 저장되는지 결정하는 커널 파라미터를 확인하는 명령어는?', answer: 'sysctl kernel.core_pattern', accept: ['cat /proc/sys/kernel/core_pattern', 'sysctl -n kernel.core_pattern'],
    explain: '`core_pattern` 이 `|/usr/lib/systemd/systemd-coredump ...` 면 `coredumpctl` 로 관리되고, `core` 같은 파일명이면 작업 디렉터리에 저장됩니다. `ulimit -c unlimited` 도 필요합니다.',
    example: 'segfault 로 죽는 서비스 분석: `coredumpctl list` → `coredumpctl gdb PID`. 컨테이너에서는 호스트의 core_pattern 이 적용된다는 점에 주의.' },
  { diff: 'extreme', type: 'mcq', q: 'MongoDB/Redis/PostgreSQL 같은 DB 가 리눅스에서 간헐적 지연을 보이며, 문서에서 THP(Transparent Huge Pages) 비활성화를 권장한다. 이유는?', options: ['THP 가 메모리를 두 배로 씀', 'khugepaged 의 페이지 병합과 huge page 할당 시 메모리 압축(compaction)이 지연 스파이크를 만들고, fork 시 COW 비용이 커짐', 'THP 가 스왑을 강제함', 'THP 는 32비트 전용'], answer: 1,
    explain: '`cat /sys/kernel/mm/transparent_hugepage/enabled` 가 `[always]` 면 문제. `never` 또는 `madvise` 로 설정하고 부팅 시 적용되도록 systemd 유닛/커널 파라미터에 넣습니다.',
    example: 'Redis 는 시작 로그에 THP 경고를 찍으며, RDB 저장(fork) 시 지연이 커지는 사례가 흔합니다.' },
  { diff: 'extreme', type: 'short', q: 'cgroup v2 에서 `myapp` 그룹의 CPU 스로틀링 발생 횟수(`nr_throttled`)를 확인하려면 어떤 파일을 읽어야 하는가? (경로: /sys/fs/cgroup/myapp)', answer: 'cat /sys/fs/cgroup/myapp/cpu.stat', accept: ['/sys/fs/cgroup/myapp/cpu.stat', 'cpu.stat', 'cat /sys/fs/cgroup/myapp/cpu.stat | grep throttled'],
    explain: '`cpu.stat` 에 `nr_periods`, `nr_throttled`, `throttled_usec` 이 있습니다. `nr_throttled/nr_periods` 비율이 높으면 `cpu.max` 쿼터가 부족한 것입니다. cgroup v1 은 `cpu.stat` 이 `cpu,cpuacct` 컨트롤러 아래에 있습니다.',
    example: '컨테이너 CPU limit 로 인한 p99 지연 조사 시 이 파일을 주기적으로 읽어 스로틀 비율을 그래프로 확인합니다.' },
  { diff: 'extreme', type: 'mcq', q: '`/proc/sys/fs/file-nr` 의 세 숫자 `12345 0 9223372036854775807` 의 의미는?', options: ['열린 파일 수 / 닫힌 파일 수 / 최대 inode', '현재 할당된 파일 핸들 수 / 할당됐지만 미사용 / 시스템 전체 최대(fs.file-max)', 'PID 수 / 스레드 수 / 최대 PID', '읽기 / 쓰기 / 총 I/O'], answer: 1,
    explain: '시스템 전체 한도(`fs.file-max`)와 프로세스별 한도(`ulimit -n`/`LimitNOFILE`)는 별개입니다. 첫 값이 세 번째에 근접하면 커널 로그에 "VFS: file-max limit reached" 가 찍힙니다.',
    example: '수만 개 연결을 처리하는 프록시 노드에서 두 한도를 모두 올려야 하는 이유를 설명할 때 쓰는 파일입니다.' },
  { diff: 'extreme', type: 'ox', q: '`sync; echo 3 > /proc/sys/vm/drop_caches` 는 페이지 캐시·dentry·inode 캐시를 비우지만, 운영 서버에서 성능 문제 "해결책" 으로 정기 실행하는 것은 오히려 I/O 를 증가시켜 해롭다.', answer: true,
    explain: '페이지 캐시는 사용 가능한 메모리이며 필요 시 커널이 알아서 회수합니다. 캐시를 강제로 비우면 다음 접근이 모두 디스크로 갑니다. 벤치마크 재현 목적 외에는 쓰지 않습니다.',
    example: '`free` 의 buff/cache 가 크다고 cron 으로 drop_caches 를 돌려 DB 성능이 떨어진 사례. `available` 컬럼을 봐야 합니다.' }
);
